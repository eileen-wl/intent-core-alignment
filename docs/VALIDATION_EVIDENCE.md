# VALIDATION_EVIDENCE.md

**Project:** Intent Core Alignment System
**Purpose:** A concise, evidence-typed record of what has been validated, how, and with what limitations.
**Companion document:** `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §E covers the same manual validations in narrative form; this document is the structured table.

## Evidence types

- **Automated test** — a passing test in this repository's committed test suite. Reproducible by running `pytest` (`apps/api`) or `vitest run` (`apps/web`).
- **Repository evidence** — provable by inspecting committed code, migrations, or Git history directly.
- **Manual owner-supplied execution** — reported by the project owner as having been run against a real external system (real ftrack workspace, real DeepSeek API), or against a real locally-running process (a real `uvicorn` server, real HTTP requests, no `TestClient`) exercised outside the automated `pytest`/`vitest` suite. Not reproducible from the repository alone merely by re-running the test suite — it requires actually starting the described process(es) again; this project's own testing rules deliberately keep real external calls, and full real-process HTTP walkthroughs, out of the automated suite.
- **Deterministic local runtime execution** — a real running process and real HTTP requests (no `TestClient`), identical in mechanism to manual owner-supplied execution, but exercising only the deterministic provider path (`MODEL_PROVIDER=deterministic`) rather than a real external DeepSeek or ftrack call. Used when the acceptance goal is to prove the real application/service code path (not a model provider) end to end.
- **Manual owner-authorised real-provider execution** — a real running process, real HTTP requests (no `TestClient`), and a real external DeepSeek call, executed within an assistant-driven session but explicitly requested and authorised by the project owner turn-by-turn (fixture preparation, the exact real-provider call to make, and the verification steps were all specified by the owner in advance). Distinct from generic "Manual owner-supplied execution" only in recording that explicit turn-level authorisation; not independently reproducible from the repository alone.
- **Manual owner-supplied browser execution** — reported by the project owner as having interacted with a real, running frontend in a real browser session (clicks, form entry, page navigation), as opposed to a raw HTTP request or API call. Distinct from generic "Manual owner-supplied execution" only in recording that the interaction occurred through the UI itself, not a script or `curl`/HTTP client; not independently reproducible from the repository alone — no transcript, recording, or screenshot of the browser session is committed.
- **Manual owner-authorised real ftrack execution** — a real running local stack (`apps/api`, `services/worker`, `services/ftrack-connector`), real HTTP requests (no `TestClient`), no mocked ftrack session, and a real external ftrack workspace call, executed within an assistant-driven session but explicitly requested and authorised by the project owner turn-by-turn (each real-workspace step and each real reconciliation run were specified/approved by the owner in advance). Distinct from generic "Manual owner-supplied execution" only in recording that explicit turn-level authorisation, mirroring "Manual owner-authorised real-provider execution" above but for ftrack rather than DeepSeek; not independently reproducible from the repository alone without real, owner-held ftrack credentials.

No API keys, credentials, full environment-variable values, or other sensitive account details appear anywhere in this document.

## Validation table

| Validation name | Date / session time | Object / Shot used | Expected result | Observed result | Evidence type | Limitations |
|---|---|---|---|---|---|---|
| Real ftrack Project/Shot/Task sync | Reported during the ftrack integration work (see commit `2b27fca`) | Shot `bc0040` | Real ftrack Project/Shot/Task data appears in ICAS as `source=ftrack`; reconciliation worker completes without error | Reported as observed by the project owner | Manual owner-supplied execution | No committed transcript or log of the run exists in this repository; not independently re-verifiable from Git alone |
| Reconciliation worker (`reconcile_ftrack_shots`) — automated behaviour | N/A (test suite) | Synthetic fixture Shots | Cursor advances correctly; a second run against unchanged data is a no-op | Passing | Automated test | Only proves the polling/cursor logic against a mocked ftrack session, not that a real workspace was actually reached |
| Real ftrack controlled write-back | Reported alongside the sync validation above (commit `2b27fca`, ADR-0012) | Shot `bc0040` | Confirming a Core Anchor with `request_write_back=True` produces exactly one new ftrack Shot Note; no existing Note is overwritten; write-back is human-requested, not automatic | Reported as observed by the project owner | Manual owner-supplied execution | No committed transcript; the write-back content marker (`"[Intent Core Alignment System]"`) is repository-verifiable, but the actual ftrack-side Note creation is not |
| Write-back request/record creation — automated behaviour | N/A (test suite) | Synthetic fixture Shots | A `WritebackRecord` is created only when the Shot has a real `ExternalEntityLink`; a human-requested write-back enqueues the worker job | Passing | Automated test | Only proves the local record/enqueue logic; does not reach real ftrack |
| Real DeepSeek Alignment Assessment | Reported during Step 4b/ADR-0013 follow-up manual testing | A Version under Shot `bc0040` | `provider=deepseek`; `AgentRun.status=succeeded`; `AlignmentAssessment.alignment_state=significant_drift`; `envelope.requires_human_gate=true`; evidence field cites the confirmed Core Anchor, the Version description, and a Review Note; no Anchor or ftrack state modified | Reported as observed by the project owner | Manual owner-supplied execution | No committed transcript of the raw DeepSeek response exists in this repository (by design — it may contain content not appropriate to commit); the deterministic-provider path and the DeepSeek adapter's request-construction logic are separately covered by automated tests (next row) |
| DeepSeek adapter request/response handling — automated behaviour | N/A (test suite) | Synthetic fixture assessment payload | Adapter makes exactly one non-streaming JSON-mode call with the correct model/messages/`response_format`; empty-content responses raise `AgentGenerationError` | Passing | Automated test | Uses a mocked SDK client (`openai.OpenAI` stubbed) — never a real network request, by explicit project rule |
| Decision supersession | Reported alongside the DeepSeek validation, and separately covered by automated tests | Assessments under Shot `bc0040` | A later human Decision correctly sets `supersedes_decision_id` to the prior active Decision for the same Shot; the old Decision remains unmodified and independently readable | Reported as observed by the project owner (manual); also proven automatically (next row) | Manual owner-supplied execution + Automated test | The manual run demonstrates it against real generated data; the automated tests demonstrate the same logic against fixtures, including a 3-link chain and cross-Shot isolation |
| Decision supersession chain — automated behaviour | N/A (test suite) | Synthetic fixture assessments across 2 Shots | A 3-Decision chain supersedes only the current unsuperseded head, not always the first; a Decision on another Shot never joins the chain | Passing | Automated test | `apps/api/tests/test_alignment_assessment_decisions.py` |
| Browser validation of the Version/Assessment UI | Reported after Step 4d | A Version under Shot `bc0040` | Version fields, Review Notes, confirmed Core Anchor summary, Alignment Assessments, AI provenance (agent type/provider/run status/context snapshot), Human Gate, and supersession labels all render correctly in a real browser session | Reported as observed by the project owner | Manual owner-supplied execution | The same rendering logic is separately covered by 16 automated frontend tests (`VersionPage.test.tsx`) using a mocked `fetch`, not a real backend |
| Frontend rendering — automated behaviour | N/A (test suite) | Fixture data | Version description, Review Notes, every Assessment field, `requires_human_gate`, DeepSeek provenance, Accept/Reject gating by role, accept/reject request shape, no-second-decision-action, supersession labels, generate-button loading/refresh, loading/error states all render/behave correctly | Passing (43/43 across `ShotAnchorPage.test.tsx` + `VersionPage.test.tsx`) | Automated test | Mocked `fetch` throughout; never calls a real backend or DeepSeek |
| Step 1A-Backend Core Anchor semantic objects — automated behaviour | N/A (test suite) | Synthetic fixture Shots/revisions | 18 focused tests cover: creating all five collections (`constraints`/`variation_zones`/`drift_risks`/`references`/`open_questions`); PATCH replace/clear/omit semantics; `order_index` assignment from input array position; blank-required-value rejection; CG Supervisor/Artist authority denial; a service-level agent actor supplying collections without gaining confirm/reject authority; immutability after confirm/reject/supersede; no row-sharing between revisions; empty-array shape for childless revisions; `AuditEvent` before/after content; atomic failure of a replacement; no side effects on other entities. Full 206-test backend suite and 43-test frontend suite pass with no regression | Passing (18/18 focused, 206/206 backend, 43/43 frontend) | Automated test | `apps/api/tests/test_core_anchor_semantic_objects.py`; migration `0014` upgrade/downgrade/upgrade, `ruff check`, `ruff format --check`, `mypy`, OpenAPI export, TypeScript contract regeneration, and contracts/web `tsc --noEmit` all also passed (repository evidence, not a separate row here) |
| Step 1A-Backend Core Anchor semantic objects — real HTTP API acceptance | Reported after Step 1A-Backend implementation and validation on branch `feat/step1a-core-anchor-semantics`, committed `173f32e` | A synthetic Project/Shot/IntentBrief/Core Anchor draft created solely for this validation (not `bc0040`, not ftrack-linked) | All five semantic collections create/PATCH-replace/clear/omit correctly with server-generated ids/`order_index`/`created_at`; the resulting `AuditEvent` records normalized before/after content, not counts; a blank required value returns 422; Human CG Supervisor and Human Artist both receive 403; confirming the revision then attempting to PATCH it again returns 409 with all semantic content unchanged; a second revision created afterward returns five empty collections and shares no child row with the first; editing semantic content creates no `AgentRun`/`ContextSnapshot`/`ExecutionAnchor`/`ExecutionAnchorRevision`/`WritebackRecord`; confirming with `request_write_back=false` creates exactly one human `Decision` and no `WritebackRecord` | Reported as observed by the project owner, via a real `uvicorn` process and real HTTP requests (no `TestClient`) | Manual owner-supplied execution | Run against an isolated temporary SQLite database (never `apps/api/dev.db`, never the developer's PostgreSQL database) — not against a real external system, unlike the ftrack/DeepSeek rows above; no committed transcript of the raw HTTP traffic exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (previous row) |
| Step 1A-UI Core Anchor semantic editors — automated behaviour | N/A (test suite) | Fixture data (`ShotAnchorPage.test.tsx`) | 70 frontend tests pass: 54 in `ShotAnchorPage.test.tsx` (including the semantic-collection editors: view/add/edit/remove/move up/move down for all five collections, dirty-collection PATCH omission/clear/replace, order-from-array-position, blank-value rejection, CG/Artist read-only gating, save-success status show/clear on edit/Cancel/revision-change, pending "Saving…" state, failed-save value preservation, role-specific default Actor id with custom-id preservation) + 16 in `VersionPage.test.tsx` (unchanged); web TypeScript typecheck, ESLint, and Prettier all pass; the Next.js production build succeeds with all five existing routes built | Passing (70/70) | Automated test | Mocked `fetch` throughout; never calls a real backend or DeepSeek. No backend, migration, Python contract, generated contract, `VersionPage`, dependency, or documentation file was changed while implementing this slice. |
| Step 1A-UI Core Anchor semantic editors — browser acceptance | Reported after Step 1A-UI implementation and validation on branch `feat/step1a-core-anchor-semantics`, pending merge to main | The Shot Anchor page's Core Anchor Human Gate, real browser session | All five semantic editors rendered under the current Core Anchor draft; Human VFX Supervisor could add and edit sentence-length content; add/remove/reorder controls worked; Reference label, optional URI, and optional note rendered and saved; Open Question reordering was preserved; blank required content was blocked by client-side validation; Save persisted data and displayed "Changes saved."; a later edit cleared the prior success status; Cancel restored server-saved values; Human CG Supervisor saw a read-only "Draft details" view with actor id `cg-1`; Human Artist saw the corresponding read-only view with actor id `artist-1`; neither CG nor Artist received semantic or scalar editing controls; a custom Actor id was preserved across role changes in the same page session; a full refresh restored the current role's default Actor id; confirmation preserved all semantic collections and made them read-only; no ftrack write-back was requested; browser use did not modify repository files | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (previous row) |
| Step 1B Core Agent Intent Decomposition — automated behaviour | N/A (test suite) | Synthetic fixture Shots/briefs | Migration `0015` upgrade/downgrade/upgrade passes; deterministic-provider generation produces distinct, non-blank content per dimension; `ContextSnapshot` payload scoped to exactly `project`/`shot`/`intent_brief`/`tasks`; generation never creates a Core Anchor, revision, or Decision; the decomposition-to-draft action maps fields correctly with fresh child ids, requires Human VFX Supervisor, refuses to overwrite an existing draft, and makes no second model call; full 235-test backend suite and 84-test frontend suite (68 `ShotAnchorPage.test.tsx` + 16 `VersionPage.test.tsx`) pass with no regression | Passing (235/235 backend, 84/84 frontend) | Automated test | `apps/api/tests/test_intent_decomposition.py`, `apps/api/tests/test_core_anchor_draft_from_decomposition.py`; `ruff check`, `ruff format --check`, standard API/contracts `mypy` (61 source files) and all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration/typecheck, web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build all also passed (repository evidence, not a separate row here); a separate, broader ad hoc `mypy` invocation that also scanned `apps/api/tests` found eight pre-existing, test-only typing errors outside CI's scope — not Step 1B failures (see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D) |
| Step 1B — Real DeepSeek Intent Decomposition acceptance | Reported after Step 1B implementation and validation on branch `feat/step1b-intent-decomposition` | A dedicated local acceptance Shot, `D1_STEP1B_ACCEPTANCE_001` (not `bc0040`, not `D1_TEST_SHOT_001`, not ftrack-linked) | `provider=deepseek`, `model=deepseek-v4-flash`; exactly one real model call; `AgentRun.status=succeeded`; all seven dimensions have non-blank summary and rationale; candidate constraints, candidate variation zones, and contextual information are populated; uncertainties is an explicit empty list; `ContextSnapshot` payload contains exactly `project`/`shot`/`intent_brief`/`tasks`; no Core Anchor or `CoreAnchorRevision` is created during generation; no Decision, WritebackRecord, Role Agent run, or ftrack network call occurs | Reported as observed by the project owner, via a real running API process and real HTTP requests (no `TestClient`), with real DeepSeek credentials | Manual owner-supplied execution | Local evidence ids: Shot `1e433dca-5a9e-46c1-9f6d-25938af49efd`; IntentDecomposition `1850f1d9-fc52-4530-a0a3-8976a94183da`; AgentRun `9eabe0b9-b0dd-448c-849e-36d0318b730b`; ContextSnapshot `423629da-5f21-4f17-b128-f95cc4111564`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 1B — Decomposition-to-Core-Anchor browser acceptance | Reported after Step 1B implementation and validation on branch `feat/step1b-intent-decomposition` | The Shot Anchor page for the `D1_STEP1B_ACCEPTANCE_001` acceptance Shot, real browser session | The real DeepSeek decomposition appeared after Intent Brief and before Core Anchor, labelled "AI proposal — Core Agent"; provider, succeeded run status, and `ContextSnapshot` provenance were visible; all seven dimensions and rationales rendered; five candidate Must-preserve items and three candidate Allowed-variation items rendered; contextual information rendered; uncertainties displayed an explicit empty state; the Human VFX Supervisor could explicitly use the decomposition; a new editable `CoreAnchorRevision` draft was created with visible source-decomposition lineage; scalar fields mapped from the selected decomposition; five Constraints and three VariationZones were created; no DriftRisk, AnchorReference, or OpenQuestion was invented; generated draft content remained editable with working save feedback and persistence; the draft remained unconfirmed; no automatic Decision or ftrack write-back occurred; the existing-draft conflict check prevented a silent duplicate overwrite; browser actions did not modify repository files | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (the Step 1B automated-behaviour row above) |
| Step 1C Core Agent Context Reconstruction — automated behaviour | N/A (test suite) | Synthetic fixture Shots with full linked context (brief, decomposition, confirmed Core Anchor, Execution Anchor, accepted Alignment Assessment Decision, Version + ReviewNote) | Migration `0016` upgrade/downgrade/upgrade passes; deterministic-provider generation cites real ids from the supplied snapshot and honestly represents missing optional facts (empty `context_gaps` entries, no invented content); `ContextSnapshot` payload scoped to exactly `project`/`shot`/`intent_brief`/`intent_decompositions`/`core_anchor`/`execution_anchors`/`decisions`/`versions`; every evidence reference in a dedicated test resolves to a real id present in that same generation's `ContextSnapshot`; generation never creates, modifies, confirms, or rejects a Core Anchor, Execution Anchor, Decision, Alignment Assessment, Version, or ReviewNote; full 253-test backend suite and 94-test frontend suite (78 `ShotAnchorPage.test.tsx` + 16 `VersionPage.test.tsx`) pass with no regression | Passing (253/253 backend, 94/94 frontend) | Automated test | `apps/api/tests/test_context_reconstruction.py`; `ruff check`, `ruff format --check`, standard API/contracts `mypy` (63 source files) and all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration/typecheck (verified stable across repeated regeneration), web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build all also passed (repository evidence, not a separate row here) |
| Step 1C — Real DeepSeek Context Reconstruction acceptance | Reported after Step 1C implementation and validation on branch `feat/step1c-context-reconstruction` | The same dedicated local acceptance Shot used for Step 1B, `D1_STEP1B_ACCEPTANCE_001` (`1e433dca-5a9e-46c1-9f6d-25938af49efd`) | `provider=deepseek`, `model=deepseek-v4-flash`; exactly one real model call; `AgentRun.status=succeeded`; all required output sections (`context_summary`, `original_intent`, `current_creative_direction`, `execution_context`, `key_decisions`, `active_constraints`, `allowed_variations`, `unresolved_questions`, `context_gaps`) are present; 11 structured items validated with non-blank summary/rationale; 18 evidence references all resolve to real ids in the saved `ContextSnapshot`; the Core Anchor is correctly described as an unconfirmed draft, not a confirmed direction; no unsupported alignment, drift, pass/fail, role-judgment, or re-anchor claim appears; missing facts (no Execution Anchor, no Decision, no Version for this Shot) are represented through `execution_context` and an explicit 5-item `context_gaps` list; no `IntentBrief`/`IntentDecomposition`/`CoreAnchor`/`CoreAnchorRevision`/semantic-child/`ExecutionAnchor`/`Decision`/`AlignmentAssessment`/`Version`/`ReviewNote`/`WritebackRecord` was created, modified, confirmed, rejected, or deleted; only one `ContextSnapshot`, one Core Agent `AgentRun`, and one `ContextReconstruction` were created; no Role Agent run and no ftrack network call occurred | Reported as observed by the project owner, via a real running API process and real HTTP requests (no `TestClient`), with real DeepSeek credentials | Manual owner-supplied execution | Local evidence ids: Shot `1e433dca-5a9e-46c1-9f6d-25938af49efd`; ContextReconstruction `644b7e4b-4049-4645-bdab-428e36ed5543`; AgentRun `2cc69bd3-c4cd-4c38-b75d-bbd5cf6958c3`; ContextSnapshot `21b96f9c-5741-4c09-90f6-947302280926`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 1C — Context Reconstruction browser acceptance | Reported after Step 1C implementation and validation on branch `feat/step1c-context-reconstruction` | The Shot Anchor page for the `D1_STEP1B_ACCEPTANCE_001` acceptance Shot, real browser session | The Context reconstruction section appeared after Core Anchor and before Execution Anchors; the output was labelled "AI reconstruction — Core Agent"; provider, succeeded run status, and `ContextSnapshot` provenance were visible; the context summary rendered; original intent rendered with rationale and evidence; current creative direction correctly described the Core Anchor as an unconfirmed draft; execution context correctly stated that no Execution Anchors existed; key decisions displayed an explicit empty state; five active constraints and three allowed variations rendered; unresolved questions displayed an explicit empty state; five context gaps rendered; evidence references rendered beneath every structured conclusion; the Human VFX Supervisor saw the Generate action; the Human CG Supervisor saw the reconstruction read-only, without a Generate action; no edit, accept, reject, apply, HumanGate, or Decision control appeared; browser activity did not modify repository files | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (the Step 1C automated-behaviour row above) |
| Step 1D Minimal Persistent HumanGate — automated behaviour | N/A (test suite) | Synthetic fixture Shots/revisions | Migration `0017` upgrade/downgrade/upgrade passes (`human_gates` table, no backfill, no polymorphic target); every draft-creation path (direct manual, direct Core Agent generate, decomposition-to-draft) atomically opens exactly one pending `HumanGate` via the shared `create_draft_revision` choke point; a gate-persistence failure rolls back the draft and vice versa; `confirm_revision`/`reject_revision` load-or-compat-create the gate and resolve it atomically with the existing `Decision`, recording `human_gate.confirmed`/`human_gate.rejected` `AuditEvent`s; a pending gate can be resolved only once; no Agent, CG Supervisor, or Artist actor can resolve a gate; legacy pre-Step-1D revisions remain readable with `GET .../human-gate` returning 404; full 277-test backend suite and 102-test frontend suite (86 `ShotAnchorPage.test.tsx` + 16 `VersionPage.test.tsx`) pass with no regression | Passing (277/277 backend, 102/102 frontend); 4 dedicated atomicity cases passing (Decision-creation failure leaves the gate pending and the revision a draft; gate-resolution failure leaves no resolved revision and no orphan Decision; gate-creation failure leaves no successful draft; semantic/draft failure leaves no orphan gate) | Automated test | `apps/api/tests/test_human_gate.py` (24 tests); `ruff check`, `ruff format --check`, standard API/contracts `mypy` (64 source files) and all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration (stable/in sync), contracts+web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build all also passed (repository evidence, not a separate row here) |
| Step 1D — Pending HumanGate runtime preparation | Reported after Step 1D implementation and validation on branch `feat/step1d-persistent-human-gate` | A local acceptance Shot, via a real running API process (`MODEL_PROVIDER=deterministic`) | A draft created through the real draft-generation service path atomically opens exactly one pending `HumanGate` with `gate_type=core_anchor_confirmation`, `required_role=vfx_supervisor`; no `Decision` exists while the gate is pending; a `human_gate.opened` `AuditEvent` exists; no DeepSeek or ftrack call occurs; no `WritebackRecord`, Role Agent run, `ContextReconstruction`, or `IntentDecomposition` is created or changed | Reported as observed by the project owner, via a real running API process and real HTTP requests (no `TestClient`), deterministic provider only | Deterministic local runtime execution | Local evidence ids: Shot `310b242b-106c-4d5c-ae61-d24f8972f375`; CoreAnchorRevision `cfc4c95d-6606-4388-8919-e7037f7f9e36` (revision number 1, status `draft`); HumanGate `d9d66ffe-ed11-4ac4-a519-b368fe182a53`. No committed transcript of the raw HTTP traffic exists in this repository, consistent with this document's own conventions |
| Step 1D — Persistent HumanGate confirmation browser acceptance | Reported after Step 1D implementation and validation on branch `feat/step1d-persistent-human-gate` | The Shot Anchor page for the same local acceptance Shot, real browser session | The pending gate was visible with its id, required reviewer, opened timestamp, and target revision; the Human VFX Supervisor entered the rationale `"Human VFX Supervisor confirms the proposed Core Anchor for Step 1D acceptance."` and clicked Confirm exactly once; the revision became confirmed; the HumanGate status became Confirmed; "Resolved by" showed `vfx-1` / `vfx_supervisor`; a resolved timestamp displayed; the rationale was preserved; the linked Decision short id `6e710675` displayed; the Confirm/Reject controls disappeared after resolution; a full page refresh preserved the confirmed revision and the resolved gate; no draft remained; no ftrack write-back occurred; browser actions did not modify repository files | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (the Step 1D automated-behaviour row above) |
| Step 2 Lightweight Shared Agent Runtime — automated behaviour | N/A (test suite) | Synthetic fixture Shots/capabilities, plus a minimal fake Pydantic output type for isolated runtime tests | Migration `0018` upgrade/downgrade/upgrade passes (nullable `model_name`/`prompt_version` on `agent_runs`); `execute_agent` persists the ContextSnapshot, creates the AgentRun as `running` with agent_type/capability/provider/model_name/prompt_version recorded, resolves and invokes the configured provider, and only marks the AgentRun `succeeded` after the domain service's own `persist_result` returns; a provider or validation failure marks the AgentRun `failed` with a safe (credential-free) error while the ContextSnapshot remains; a domain-persistence failure also marks the AgentRun `failed` with no partial result; the shared Model Gateway resolves `deterministic`/`deepseek` correctly, dispatches exactly one non-streaming JSON-mode DeepSeek call through a mocked client, and never makes a real network call; the Prompt Registry has all four capabilities registered (`core_anchor_drafting.v1`/`intent_decomposition.v1`/`context_reconstruction.v1`/`alignment_assessment.v1`) with stable version labels and the correct structured output type each; all four existing capabilities (`core_anchor_drafting` including its decomposition-to-draft deterministic transform, `intent_decomposition`, `context_reconstruction`, `alignment_assessment`) were migrated onto the shared runtime with no behavioural change; full 301-test backend suite and 104-test frontend suite (88 `ShotAnchorPage.test.tsx` + 16 `VersionPage.test.tsx`) pass with no regression | Passing (301/301 backend, 104/104 frontend) | Automated test | `apps/api/tests/test_agent_runtime.py` (24 new tests); `ruff check`, `ruff format --check`, all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration (in sync), contracts+web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build all also passed (repository evidence, not a separate row here) |
| Step 2 — Real DeepSeek Context Reconstruction acceptance via the shared Agent Runtime | Reported after Step 2 implementation and validation on branch `feat/step2-lightweight-agent-runtime` | The existing Step 1D acceptance Shot, `D1_STEP1D_ACCEPTANCE_001` (`310b242b-106c-4d5c-ae61-d24f8972f375`) | `provider=deepseek`, `model_name=deepseek-v4-flash`; exactly one real model call, dispatched through `agents.runtime.execute_agent` and the shared Model Gateway; `AgentRun.status=succeeded`; `prompt_version=context_reconstruction.v1` recorded from the Prompt Registry; exactly one `ContextSnapshot` and one `ContextReconstruction` created; the output accurately reflected the confirmed Core Anchor and the linked Decision, cited only evidence actually present in the snapshot (correctly using `intent_brief` as the evidence source for constraints/variations rather than inventing `constraint`/`variation_zone` rows the confirmed revision does not have), and listed honest context gaps; the confirmed Core Anchor, the resolved HumanGate, and the linked Decision were all left unchanged; no `IntentDecomposition`, `AlignmentAssessment`, Role Agent run, or ftrack network call occurred | Reported as observed by the project owner, via a real running API process and real HTTP requests (no `TestClient`), with real DeepSeek credentials, against the real PostgreSQL database (migrated forward to `0018`) | Manual owner-supplied execution | Local evidence ids: ContextReconstruction `97b4cce5-1c59-4c49-862a-a407dfb1dfd3`; AgentRun `e370f7cd-dd14-4ebb-b53b-0402d9107e47`; ContextSnapshot `42640ca9-4b80-4c8d-ba74-c4be5e9a8c91`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 2 — Shared-runtime provenance browser acceptance | Reported after Step 2 implementation and validation on branch `feat/step2-lightweight-agent-runtime` | The Shot Anchor page for the `D1_STEP1D_ACCEPTANCE_001` acceptance Shot, real browser session | The newly generated Context Reconstruction rendered on the Shot Anchor page; its AI provenance display showed `provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: context_reconstruction.v1`, and `run status: succeeded`, alongside the ContextSnapshot id and its captured timestamp; no additional model call was triggered by this browser session; browser actions did not modify any repository file | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same provenance-display scenarios are separately covered by automated tests (the Step 2 automated-behaviour row above) |
| Step 3 VFX Supervisor Agent — automated behaviour | N/A (test suite) | Synthetic fixture Shots/Versions | Migration `0019` upgrade/downgrade/upgrade passes (immutable `vfx_supervisor_reviews`, linking Project/Shot/Version/ContextSnapshot/AgentRun, `agent_run_id` unique, no PATCH/DELETE/active-pointer); `agents.vfx_supervisor_review_service.generate_vfx_supervisor_review` builds a ContextSnapshot scoped to exactly one Version (project/shot/version/intent_brief/intent_decompositions/core_anchor+semantic children/context_reconstruction/alignment_assessment/review_notes/decisions/tasks/human_gate, no binary media, no raw ftrack data), routes through the shared `execute_agent` runtime, and validates that every `VFXReviewItem`/`VFXProposedFeedbackNote` evidence reference resolves to a real id in that same run's snapshot, rejecting (as a validation failure, before persistence) any invented id; the deterministic generator never invents a frame-level observation and always states the missing-media limitation; `agent_type=vfx_supervisor_agent`, `capability=creative_review` recorded correctly; generation is Human-VFX-Supervisor-only (CG Supervisor and Artist receive 403, agent actors forbidden), all three human roles may read; multiple immutable reviews may exist per Version, listed newest first; full 323-test backend suite and 117-test frontend suite (29 `VersionPage.test.tsx` + 88 `ShotAnchorPage.test.tsx`) pass with no regression | Passing (323/323 backend, 117/117 frontend) | Automated test | `apps/api/tests/test_vfx_supervisor_review.py` (22 new tests); `ruff check`, `ruff format --check`, all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration (in sync), contracts+web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build all also passed (repository evidence, not a separate row here) |
| Step 3 — Real DeepSeek VFX Supervisor Agent acceptance | Reported after Step 3 implementation and validation on branch `feat/step3-vfx-supervisor-agent` | A dedicated acceptance Version, `D1_STEP3_VFX_REVIEW_001` (`4f3fabe8-40f7-4a20-a419-c954b89d1ee3`), created via the real application API on the existing Step 1D/2 acceptance Shot (`310b242b-106c-4d5c-ae61-d24f8972f375`) | `provider=deepseek`, `model_name=deepseek-v4-flash`; exactly one real model call, dispatched through `agents.runtime.execute_agent` and the shared Model Gateway; `AgentRun.status=succeeded`; `prompt_version=vfx_supervisor_creative_review.v1` recorded from the Prompt Registry; exactly one `ContextSnapshot` and one `VFXSupervisorReview` created; all 18 evidence references across the output resolved to real ids in the saved `ContextSnapshot`; the output contained one strength, three creative concerns, three review priorities, two proposed feedback notes (each preserving both what should change and why it matters to the creative intent), two questions for the Human VFX Supervisor, and four evidence gaps that explicitly stated no actual image/video media was visually inspected; the output made no unsupported visual observation, pass/fail, definitive alignment/drift, Decision, role-blame, re-anchor recommendation, or Intent Signal; the confirmed Core Anchor, the resolved HumanGate, and the linked Decision were all left unchanged; no ftrack network call occurred | Reported as observed by the project owner, via a real running API process and real HTTP requests (no `TestClient`), with real DeepSeek credentials, against the real PostgreSQL database (migrated forward to `0019`) | Manual owner-authorised real-provider execution | Local evidence ids: Project `937c93c4-ac94-4fbe-b896-e13dc5f6b018`; Shot `310b242b-106c-4d5c-ae61-d24f8972f375`; Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b`; Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`; ReviewNote `70f71f49-e7fb-4fed-9507-23bdefdf9579`; VFXSupervisorReview `d94af31c-f59e-40ee-883f-1ee30ba7074e`; AgentRun `b1e5d753-d008-4376-85e4-81ffcbdd6251`; ContextSnapshot `2ca7281e-fd23-42e1-b3da-0e722af9422b`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 3 — VFX Supervisor Agent Version-page browser acceptance | Reported after Step 3 implementation and validation on branch `feat/step3-vfx-supervisor-agent` | The Version page for the `D1_STEP3_VFX_REVIEW_001` acceptance Version, real browser session | The "VFX Supervisor Agent review" section appeared on the target Version page, labelled "AI creative review — VFX Supervisor Agent"; the executive summary, creative-direction reading, strengths, creative concerns, review priorities, proposed feedback notes (each showing both feedback and underlying intent), questions for the Human VFX Supervisor, and evidence gaps (including the missing-media limitation) all rendered, alongside evidence references; AI provenance displayed `provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: vfx_supervisor_creative_review.v1`, `run status: succeeded`, and the ContextSnapshot id and timestamp; the Human VFX Supervisor saw the Generate action; the Human CG Supervisor saw the existing review read-only, without a Generate action; no edit, apply, accept, reject, Decision, or ReviewNote-create control appeared; no additional model call was triggered; browser actions did not modify any repository file | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same rendering scenarios are separately covered by automated tests (the Step 3 automated-behaviour row above) |
| Step 4 CG Supervisor Agent — automated behaviour | N/A (test suite) | Synthetic fixture Shots/Execution Anchor revisions | Migration `0020` upgrade/downgrade/upgrade passes (immutable `cg_supervisor_reviews`, linking Project/Shot/Task/ExecutionAnchorRevision/ContextSnapshot/AgentRun, `agent_run_id` unique, no PATCH/DELETE/active-pointer; `HumanGate` gains a nullable dual-target FK pair under a DB CHECK XOR constraint); `agents.cg_supervisor_review_service.generate_cg_supervisor_review` builds a compact ContextSnapshot scoped to one ExecutionAnchorRevision (singular, deduplicated, summary-only projections — no verbose nested evidence trees, no binary media, no raw ftrack data), routes through the shared `execute_agent` runtime with a capability-specific `max_output_tokens=8192`, and validates that every evidence reference resolves to a real id in that same run's snapshot, rejecting any invented id before persistence; every list/string field on the output contract carries a hard `Field` bound, independent of prompt-text guidance; a pre-persistence content-boundary validator rejects a missing inspection-unavailable/media disclosure in `evidence_gaps`, or any Core-Anchor-modification, re-anchor, HumanGate-resolution, or Decision-authority instruction elsewhere in the output; provider-failure diagnostics never leak raw response content into `AgentRun.error`; `agent_type=cg_supervisor_agent`, `capability=execution_review` recorded correctly; generation is Human-CG-Supervisor-only (agent actors and other human roles forbidden), all three human roles may read; full 419-test backend suite and 125-test frontend suite pass with no regression | Passing (419/419 backend, 125/125 frontend; 61/61 in the focused `test_cg_supervisor_review.py` suite) | Automated test | `apps/api/tests/test_cg_supervisor_review.py` (61 tests), `apps/api/tests/test_execution_anchor_human_gate.py`; `ruff check`, `ruff format --check`, and all four CI `mypy` scopes all re-verified passing during this documentation turn; migration `0020` upgrade/downgrade/upgrade was verified during implementation validation against test fixtures, not re-run against the real acceptance database in this documentation turn (to avoid disturbing the now-confirmed real Execution Anchor evidence below) — `alembic current` against the real database was independently confirmed at head `0020` (read-only check); OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build were verified passing during the preceding implementation-validation turn and remain valid, since no product/contract/generated file has changed since (confirmed via `git status --short`) |
| Step 4 — Real-provider truncation failures, root-caused and fixed | Reported during implementation and validation on branch `feat/step4-cg-supervisor-agent` | The Step 4 target Execution Anchor revision (`1561d08d-64cc-4550-b25d-27210ebbe2b5`, on the existing Step 1D/2/3 acceptance Shot `310b242b-106c-4d5c-ae61-d24f8972f375`, Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b`) | Two real (non-deterministic) `provider=deepseek` calls both fail with a truncated, unparseable structured-output response; the failure is safely diagnosed (no raw response content leaked) and both `AgentRun` rows remain `status=failed`, with no `CGSupervisorReview` created from either | First call: `AgentRun` `12ab6d3a-d1aa-40ec-aa1f-d86de7a904da` (`ContextSnapshot` `2dcbce05-4676-488e-81b6-c75d7dee8e29`, `max_output_tokens=4096`) truncated. Second call, after raising the budget: `AgentRun` `abbc3720-15c0-4fe7-bacb-3b3d777be4d1` (`ContextSnapshot` `721dd101-7c2c-4cb1-999d-e9086d7ce166`, `max_output_tokens=8192`) truncated *even earlier* than the first, proving token budget alone was not the root cause. Root-caused and fixed with a compact `ContextSnapshot` input, hard `Field` output-contract bounds, and safe `DeepSeekResponseDiagnostics` — see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §E item 18 | Manual owner-authorised real-provider execution | Both failed `AgentRun` rows are preserved unmodified as evidence, never deleted, rewritten, reused, or backfilled. No DeepSeek response content, prompt text, or raw JSON from either failed run is recorded anywhere in this repository, consistent with this document's conventions |
| Step 4 — Real DeepSeek CG Supervisor Agent acceptance, pre-content-boundary-hardening | Reported during implementation and validation on branch `feat/step4-cg-supervisor-agent`, after the truncation root-cause fix above | The Step 4 target Execution Anchor revision (`1561d08d-64cc-4550-b25d-27210ebbe2b5`) | `provider=deepseek`, `model=deepseek-v4-flash`; exactly one real model call; `AgentRun.status=succeeded`; a `CGSupervisorReview` is persisted, proving real-provider/runtime/persistence success structurally | Observed: `AgentRun` `8f892a77-5e95-463b-bb18-b5d75d835572`; `ContextSnapshot` `dbcbf946-0642-41c5-ba24-e64b5807105f`; `CGSupervisorReview` `f32cfd8d-4e32-4775-99c7-acd658d06412`. The output was structurally valid but exposed two content-boundary gaps: `evidence_gaps` did not explicitly disclose that footage/rendered frames/scene files were not directly inspected; one `proposed_execution_guidance` item recommended updating the Core Anchor, outside this capability's authority. Both gaps motivated the content-boundary hardening fix | Manual owner-authorised real-provider execution | This review is retained unmodified as historical evidence of the pre-hardening state — it is **not** the accepted final review (see the next row); it proved real-provider/runtime/persistence success but is not content-compliant. No committed transcript of the raw DeepSeek response exists in this repository |
| Step 4 — Final compliant real DeepSeek CG Supervisor Agent acceptance | Reported after Step 4 content-boundary hardening, on branch `feat/step4-cg-supervisor-agent` | The Step 4 target Execution Anchor revision (`1561d08d-64cc-4550-b25d-27210ebbe2b5`) | `provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=cg_supervisor_execution_review.v1`; exactly one new real model call, dispatched through `agents.runtime.execute_agent` and the shared Model Gateway; `AgentRun.status=succeeded`; exactly one new `ContextSnapshot` and one new `CGSupervisorReview` created; every evidence reference across the output resolved to one of the 13 ids present in the saved `ContextSnapshot`; `evidence_gaps` explicitly disclosed that ICAS had not directly inspected footage, moving-image media, rendered frames, still images, or scene/project/DCC files; no proposed guidance, priority, or question recommended Core Anchor modification, re-anchoring, HumanGate resolution, or Decision creation; the review was persisted only after both the evidence-resolution and content-boundary validators passed; no pass/fail, blame, or unsupported numeric value appeared; the confirmed Core Anchor, the existing resolved Core Anchor HumanGate/Decision, and the (at that point still-pending) Execution Anchor HumanGate were all left unchanged; no ftrack network call occurred | Observed exactly as expected: `AgentRun` `f716175c-7ebc-473d-a5f3-3ec1d640b931`; `ContextSnapshot` `6f10121c-2b65-420e-89c4-07aa4a9b9795`; `CGSupervisorReview` `6877fa03-fab2-4e70-92a2-d0ca185d20e2`. List counts: 2 actionable requirements, 2 technical concerns, 1 coordination concern, 2 implementation priorities, 2 proposed execution guidance items, 3 questions for the Human CG Supervisor, 3 evidence gaps — all within the hard contract bounds | Manual owner-authorised real-provider execution | Local evidence ids as above; ExecutionAnchor `5e76aaff-9c65-4ceb-939e-d815387b264d`; Project `937c93c4-ac94-4fbe-b896-e13dc5f6b018`; Shot `310b242b-106c-4d5c-ae61-d24f8972f375`; Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 4 — CG Agent and Execution Anchor HumanGate browser acceptance | Reported after Step 4 implementation and validation on branch `feat/step4-cg-supervisor-agent` | The Shot Anchor page for the Step 1D/2/3/4 shared acceptance Shot (`310b242b-106c-4d5c-ae61-d24f8972f375`), real browser session | The CG Human Gate UI (mirroring `CoreAnchorGate`) and the final compliant CG Supervisor Agent review both rendered correctly for Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5`; the Human CG Supervisor entered the rationale `"Human CG Supervisor confirms the proposed Execution Anchor for Step 4 acceptance."` and clicked Confirm; the revision became confirmed and the HumanGate became Confirmed; Confirm/Reject controls disappeared; the earlier duplicate revision (created by an unintended tooling double-submission during a prior acceptance turn, disclosed at the time and cleaned up through the normal reject endpoint, never direct database manipulation) remained visible as historical evidence, correctly labelled rejected; no additional model call occurred during browser review; browser actions did not modify any repository file | Reported as observed by the project owner, in a real browser session; independently verified against the real database after the fact: revision `1561d08d-64cc-4550-b25d-27210ebbe2b5` `status=confirmed` (`confirmed_by_actor_id=cg-1`, `confirmed_by_human_role=cg_supervisor`, `confirmed_at=2026-07-27T20:18:21.629130+00:00`); HumanGate `b6a204b0-fb01-40b8-932f-92063751a2a6` `status=confirmed` (`resolved_by_actor_id=cg-1`, `resolved_by_role=cg_supervisor`, `resolved_at=2026-07-27T20:18:21.645945+00:00`, matching rationale, `decision_id=6c71d097-6f7a-4171-83be-39c24df46db2`); Decision `6c71d097-6f7a-4171-83be-39c24df46db2` `decision_type=confirm_execution_anchor`, `owning_human_role=cg_supervisor`, `actor_human_role=cg_supervisor`; duplicate revision `07627b23-3ec1-4ff8-90e9-4901d89cec6d` confirmed still `status=rejected` | Manual owner-supplied browser execution | Not independently provable from Git history alone — the browser interaction itself is owner-supplied and unwitnessed by this repository; only the resulting database state (independently re-queried, read-only, for this row) is repository-verifiable. No committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same CG Human Gate rendering/confirm/reject scenarios are separately covered by automated tests (the Step 4 automated-behaviour row above) |
| Step 5 Artist Agent — automated behaviour | N/A (test suite) | Synthetic fixture Shots/Tasks/Versions/Execution Anchor revisions | Migration `0021` upgrade/downgrade/upgrade passes (immutable `artist_agent_guidances`, linking Project/Shot/Task/Version/confirmed ExecutionAnchorRevision/ContextSnapshot/AgentRun, `agent_run_id` unique, no PATCH/DELETE/active-pointer); `agents.artist_guidance_service.generate_artist_agent_guidance` requires an explicit `task_id` (missing → 422; cross-Shot Task → 404; no heuristic latest-Task inference anywhere in the code path), requires the supplied Task's Execution Anchor revision to be `confirmed` (missing/draft/rejected → 409 before any `ContextSnapshot`/`AgentRun` is created), builds a compact ContextSnapshot scoped to the explicit Task/target Version (singular, summary-only projections — no verbose nested evidence trees, no binary media, no raw ftrack data), routes through the shared `execute_agent` runtime with a capability-specific `max_output_tokens=6144`, and validates that every evidence reference resolves to a real id in that same run's snapshot, rejecting any invented id before persistence; every list/string field on the output contract carries a hard `Field` bound, independent of prompt-text guidance; a pre-persistence content-boundary validator rejects a missing inspection-unavailable/media/parameter disclosure in `evidence_gaps`, or any direct or indirect Anchor-modification instruction (including a known Anchor field name such as `allowed_refinements` without the word "Anchor") elsewhere in the output, while continuing to allow Anchor-evidence citations and CG/VFX coordination requests; the deterministic generator converts a Task with no recorded technical guidance into a Human-CG-Supervisor clarification question rather than an Anchor-edit instruction, and its output passes the same production validator used for real-provider output; provider-failure diagnostics never leak raw response content into `AgentRun.error`; `agent_type=artist_agent`, `capability=iteration_guidance` recorded correctly; generation is Human-Artist-only (agent actors and other human roles forbidden), all three human roles may read; full 496-test backend suite and 135-test frontend suite pass with no regression | Passing (496/496 backend, 135/135 frontend; 77/77 in the focused `test_artist_agent_guidance.py` suite) | Automated test | `apps/api/tests/test_artist_agent_guidance.py` (77 tests); `ruff check`, `ruff format --check`, and all four CI `mypy` scopes all passed; migration `0021` upgrade/downgrade/upgrade was verified during implementation validation against test fixtures and independently confirmed by upgrading the real acceptance database forward to head `0021`; OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, and repository Prettier all passed; the Next.js production build passed after retrying one transient Windows `.next/trace` `EPERM` file-lock error (an environment condition, not a code defect) |
| Step 5 — Artist Agent content-boundary failure-path acceptance | Reported during implementation and validation on branch `feat/step5-artist-agent` | Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b` ("Compositing Review") on the shared acceptance Shot (`310b242b-106c-4d5c-ae61-d24f8972f375`); Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`; confirmed Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5` | `provider=deepseek`; exactly one real model call; the response is structurally valid but is rejected by the pre-persistence content-boundary validator because it advises modifying an Anchor; `AgentRun.status=failed` with a short, sanitised reason only; `ContextSnapshot` is preserved; no `ArtistAgentGuidance` is persisted | Observed as expected: `AgentRun` `2cec22c3-9b07-4f12-bb04-ceddd0677747` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=artist_iteration_guidance.v1`, `status=failed`, `error="Artist Agent guidance exceeded its bounded advisory scope: instructs modifying an Anchor"`); `ContextSnapshot` `fe3ebaad-184f-4a33-97b1-f9c54b4be3d4`; no `ArtistAgentGuidance` row exists linked to this run | Manual owner-authorised real-provider execution | This `AgentRun` and `ContextSnapshot` are preserved unmodified as evidence, never deleted, rewritten, reused, retried, or backfilled — re-verified unchanged immediately before the subsequent hardening and final-acceptance turns. No DeepSeek response content, prompt text, or raw/partial JSON from this run is recorded anywhere in this repository. This finding directly motivated the Artist content-boundary hardening described in `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §E item 23 |
| Step 5 — Final compliant real DeepSeek Artist Agent acceptance | Reported after Step 5 content-boundary hardening, on branch `feat/step5-artist-agent` | Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b`; Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`; confirmed Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5` | `provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=artist_iteration_guidance.v1`; exactly one new real model call, dispatched through `agents.runtime.execute_agent` and the shared Model Gateway; `AgentRun.status=succeeded`; exactly one new `ContextSnapshot` and one new `ArtistAgentGuidance` created; the guidance is persisted before the `AgentRun` becomes `succeeded`; every evidence reference resolves to a real id in the saved `ContextSnapshot`; `evidence_gaps` explicitly discloses that ICAS has not directly inspected footage, moving-image media, rendered frames, still images, scene/project/DCC files, or numeric production parameters; no output recommends modifying either Anchor, re-anchoring, HumanGate resolution, Decision creation, or an official Version pass/fail/ranking judgment; all hard list/character/evidence-count bounds pass; the confirmed Core Anchor, the confirmed Execution Anchor, both resolved HumanGates, and the existing VFX/CG Supervisor reviews are left unchanged; no ftrack network call occurs | Observed exactly as expected: `ArtistAgentGuidance` `2fbd9c14-f47d-474b-b01d-0bb452c5f124`; `AgentRun` `70622310-d931-418a-b976-a0cea8661aec`; `ContextSnapshot` `3982b63d-14b1-4386-8bb7-7f1b914fec76`. List counts: 3 non-negotiables, 2 allowed variations, 3 feedback translations, 3 iteration priorities, 2 cross-department dependencies, 3 questions, 3 evidence gaps — all within the hard contract bounds; every feedback translation preserves a practical action, its underlying creative intent, and a self-check that directs the Artist to compare/verify rather than claiming ICAS inspected any media; cross-department dependencies and two of three questions explicitly target department-local-optimisation risk (coordinating push-in speed and contrast limits with the CG/camera departments rather than guessing locally) | Manual owner-authorised real-provider execution | Local evidence ids as above; Project `937c93c4-ac94-4fbe-b896-e13dc5f6b018`; Shot `310b242b-106c-4d5c-ae61-d24f8972f375`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 5 — Artist Agent Version-page browser acceptance | Reported after Step 5 implementation and validation on branch `feat/step5-artist-agent` | The Version page for Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`, real browser session | The "AI iteration guidance — Artist Agent" section appears on the target Version page; as the Human Artist, the explicit Task selector shows "Compositing Review" selected and a Generate control is visible; the persisted real DeepSeek guidance renders in full (executive summary, creative intent read, task goal, current iteration read, non-negotiables, allowed variations, each feedback translation's feedback/issue, practical action, underlying intent, and self-check, iteration priorities, cross-department dependencies, questions for the Human Supervisor, evidence gaps and evidence references); an advisory statement explains the guidance is based on recorded text metadata only, with no visual inspection; AI provenance displays `provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: artist_iteration_guidance.v1`, `run status: succeeded`, and the ContextSnapshot id and timestamp; the Human VFX Supervisor and Human CG Supervisor can read the same guidance but see no Generate control; no edit/apply/accept/reject/approve/ranking control appears anywhere; no additional model call is triggered; browser actions do not modify any repository file | Reported as observed by the project owner, in a real browser session | Manual owner-supplied browser execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same rendering scenarios are separately covered by automated tests (the Step 5 automated-behaviour row above) |
| Step 6 Cross-role Assessment — automated behaviour | N/A (test suite) | Synthetic fixture Shots/Tasks/Versions/Anchors/Role Agent outputs | Migration `0022` upgrade/downgrade/upgrade passes (immutable `cross_role_assessments`/`re_anchor_proposals`/`intent_signals`, linking Project/Shot/Task/Version/confirmed Core+Execution Anchor revisions/newest VFX+CG+Artist outputs/ContextSnapshot/AgentRun, no PATCH/DELETE/active-pointer); `agents.cross_role_assessment_service.generate_cross_role_assessment` requires an explicit `task_id` (no heuristic latest-Task inference), requires a confirmed Core Anchor revision, a confirmed Execution Anchor revision for that Task, and the newest VFX/CG/Artist outputs (each missing prerequisite → 409 before any `ContextSnapshot`/`AgentRun` is created), builds a compact ContextSnapshot, routes through the shared `execute_agent` runtime with a capability-specific `max_output_tokens=8192`, and validates evidence-id resolution, exactly-three-unique-role-perspectives, media/parameter disclosure, authority/content boundaries, `ReAnchorProposal` evidence-diversity, and unsupported-production-numeric values before any row is persisted; `derive_intent_signal` is a pure function producing a deterministic `low`/`medium`/`high` `attention_level` with explainable drivers; `agent_type=core_agent`, `capability=cross_role_assessment` recorded correctly; generation is Human-VFX-Supervisor-only (CG Supervisor and Artist receive 403, agent actors forbidden), all three human roles may read; multiple immutable assessments may exist per Version/Task context, listed newest first; full 663-test backend suite and 152-test frontend suite pass with no regression | Passing (663/663 backend, 152/152 frontend; 156/156 in the focused `test_cross_role_assessment.py` suite, 113/113 in the focused `ShotAnchorPage.test.tsx` suite) | Automated test | `apps/api/tests/test_cross_role_assessment.py` (156 tests); `ruff check`, `ruff format --check`, and all four CI `mypy` scopes passed; migration `0022` upgrade/downgrade/upgrade was independently confirmed by upgrading the real acceptance database forward to head `0022`; OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, and repository Prettier all passed; the Next.js production build passed |
| Step 6 — First real-provider structured-output failure and safe diagnostic hardening | Reported during implementation and validation on branch `feat/step6-cross-role-alignment` | Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b` ("Compositing Review"); Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`; confirmed Core Anchor revision `cfc4c95d-6606-4388-8919-e7037f7f9e36`; confirmed Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5` | `provider=deepseek`; exactly one real model call; a complete, non-empty, non-truncated response fails opaque Pydantic structured-output validation, with no way to distinguish a schema-conformance problem from any other decode failure; `AgentRun.status=failed`; `ContextSnapshot` is preserved; no `CrossRoleAssessment`, `ReAnchorProposal`, or `IntentSignal` is persisted | Observed as expected: `AgentRun` `8eda2584-73a8-448e-bdb4-05fc6c31bb1f` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=core_cross_role_assessment.v1`, `status=failed`); `ContextSnapshot` `de3acea9-869b-44cd-aaf4-5e8c16d0cdd8`; no result row exists linked to this run | Manual owner-authorised real-provider execution | This `AgentRun` and `ContextSnapshot` are preserved unmodified as evidence, never deleted, rewritten, reused, retried, or backfilled — re-verified unchanged immediately before every subsequent Step 6 turn. No DeepSeek response content, prompt text, or raw/partial JSON from this run is recorded anywhere in this repository. This finding motivated the shared Model Gateway's `StructuredOutputValidationDiagnostics` (`validation_stage=json_decode`\|`schema_validation`, plus up to 10 sanitised `field_path:error_type` entries for the latter — applies to every capability, not only this one) |
| Step 6 — Second real-provider failure (source_type enum) and contract-derived catalogue hardening | Reported during implementation and validation on branch `feat/step6-cross-role-alignment`, after the diagnostic hardening above | Same target as above | `provider=deepseek`; exactly one real model call; with the new diagnostics active, the response fails schema validation with specific, safe field-level detail; `AgentRun.status=failed`; `ContextSnapshot` is preserved; no result row is persisted | Observed as expected: `AgentRun` `88f92372-5a64-4704-9482-2e60d439f5e8` (`status=failed`, diagnostics identifying `agreements.0.evidence.1.source_type:literal_error` and `re_anchor_proposal.evidence.3.source_type:literal_error` — the invalid alias values themselves never appear in `AgentRun.error`); `ContextSnapshot` `d0fc132a-3325-4357-913f-9f35fedc397c` | Manual owner-authorised real-provider execution | This `AgentRun` and `ContextSnapshot` are preserved unmodified as evidence, never deleted, rewritten, reused, retried, or backfilled. This finding motivated deriving the prompt's allowed `source_type` catalogue directly from the contract's own `CrossRoleEvidenceSourceType` Literal (via `typing.get_args`), placing it beside every evidence shape, and forbidding generic aliases |
| Step 6 — Third real-provider failure (genuine token-budget truncation) and capability-specific budget fix | Reported during implementation and validation on branch `feat/step6-cross-role-alignment`, after the source_type hardening above | Same target as above | `provider=deepseek`; exactly one real model call; the response produces zero schema-validation errors but is genuinely truncated at the configured token budget; `AgentRun.status=failed`; `ContextSnapshot` is preserved; no result row is persisted | Observed as expected: `AgentRun` `95235b28-5905-4630-8cde-27e6d6ae36fc` (`status=failed`, `validation_stage=json_decode`, `finish_reason=length`, `configured_max_output_tokens=7168`, `completion_tokens=7168` exactly — confirmed genuine truncation, not a schema-conformance issue); `ContextSnapshot` `15f91a2d-bed0-42f9-9bfd-ccc31c4d1e98` | Manual owner-authorised real-provider execution | This `AgentRun` and `ContextSnapshot` are preserved unmodified as evidence, never deleted, rewritten, reused, retried, or backfilled. This finding motivated a narrow, capability-specific fix: `cross_role_assessment`'s `max_output_tokens` raised from `7168` to `8192` in the Prompt Registry only — no other capability's budget, contract, prompt version, model, runtime, or persistence semantics changed |
| Step 6 — First technically successful real-provider result (historical; motivated numeric hardening) | Reported during implementation and validation on branch `feat/step6-cross-role-alignment`, after the token-budget fix above | Same target as above | `provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=core_cross_role_assessment.v1`; exactly one real model call; `AgentRun.status=succeeded`; a `CrossRoleAssessment`, `ReAnchorProposal`, and `IntentSignal` are persisted, proving real-provider/runtime/persistence success structurally | Observed: `CrossRoleAssessment` `5a6d16d5-2c85-4f22-8057-cbebc77ecc66`; `ReAnchorProposal` `fe177507-a3f6-43fe-b705-5ac9c7c3ad93`; `IntentSignal` `2bcae5a5-76e7-41e5-978f-e9bc8294adbf`; `AgentRun` `ee7b9225-af37-4c59-ae8f-e120fb26917e`; `ContextSnapshot` `81479ee2-5279-4473-8d56-0d60f17c9740`. Technical persistence, evidence resolution, `ReAnchorProposal` evidence-diversity, `IntentSignal` derivation, authority, and side-effect validation all passed. A subsequent owner browser review found the advisory `re_anchor_proposal` had invented two production thresholds not present in any cited evidence: "push-in speed must not exceed 0.5x normal conversation pace" and "contrast may increase up to 15% above baseline" | Manual owner-authorised real-provider execution + Manual owner-supplied browser execution (finding) | This result is retained unmodified as historical evidence — it is **not** deleted, rewritten, corrected, or invalidated, and remains readable via the list endpoint (newest-first, so it now sorts after the final compliant result below). It proved real-provider/runtime/persistence success but is not content-compliant. Both invented values directly motivated the unsupported-production-numeric-value hardening in the next row |
| Step 6 — Unsupported production-numeric-value hardening | Reported during implementation and validation on branch `feat/step6-cross-role-alignment`, after the finding above | N/A (code + test suite; no real provider call made while implementing this hardening) | A targeted, code-level detector added to the content-boundary validator rejects a production-specific numeric expression (percentage, multiplier/ratio, frame count, fps, duration, exposure stop/EV, nits/IRE, pixel, angle, or a range of any of these) anywhere in generated text unless the exact value-and-unit already appears in evidence the *same output item* cites (no unrelated-snapshot-object credit, no arithmetic/unit-semantic conversion); rejection uses one fixed, sanitised error category, never the invented value; the deterministic generator already demonstrates the desired compliant behaviour with no fixture-only exception added | Passing: focused Cross-role Assessment tests grew from 117 to 156 (16 detection-category tests, 6 evidence-support tests including field-level/proposal-level isolation, and failure-path/deterministic-generator reconfirmation tests); full backend suite grew from 624 to 663; `ruff`/`mypy`/Prettier/`git diff --check` all reconfirmed passing | Automated test | `apps/api/tests/test_cross_role_assessment.py`; no real DeepSeek or ftrack call was made while implementing this hardening; the fix is a narrow content-boundary addition, not a redesign of Step 6, the public contract, migration `0022`, the API, or the UI |
| Step 6 — Final compliant real-provider acceptance | Reported after the numeric hardening above, on branch `feat/step6-cross-role-alignment` | Same target as above | `provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=core_cross_role_assessment.v1`, `max_output_tokens=8192`; exactly one new real model call, dispatched through `agents.runtime.execute_agent` and the shared Model Gateway; `AgentRun.status=succeeded`; exactly one new `ContextSnapshot`, `CrossRoleAssessment`, and `IntentSignal`, and one new `ReAnchorProposal`, created in the locked persistence order; every evidence reference resolves to a real id in the saved `ContextSnapshot`; every `source_type` is an exact contract Literal; no unsupported production-numeric expression appears anywhere; the `ReAnchorProposal` (when present) cites the current Core Anchor plus at least two distinct role categories, is supported by a cross-role tension or local-optimum risk, and remains advisory only; `derive_intent_signal` re-run against the persisted assessment reproduces the persisted `IntentSignal` exactly; the confirmed Anchors, both resolved HumanGates, and the existing VFX/CG/Artist outputs are left unchanged; no ftrack network call occurs | Observed exactly as expected: `CrossRoleAssessment` `9ef47a2e-f51e-472f-a455-03b49c8d139b`; `ReAnchorProposal` `757682c6-b23a-42bf-a2a5-cb95314deb27`; `IntentSignal` `f412927c-de95-4eda-9b2c-965025355539`; `AgentRun` `8f11ceaf-ab54-4c11-8ca5-fcaeaf0e4a29` (`result_revision_id=null`, `error=null`); `ContextSnapshot` `9da4f5f7-04e9-47a8-a737-f7735d4e3130` (`created_at=2026-07-28T01:47:33.828174Z`). List counts: 3 role perspectives (vfx_supervisor/cg_supervisor/artist order), 2 agreements, 1 cross-role tension, 1 local-optimum risk, 2 unresolved dependencies, 2 human coordination priorities, 4 evidence gaps — all within the hard contract bounds; `0.5x`, `15%`, and other frame/fps/duration/exposure-style examples were explicitly confirmed absent from the entire output; the `IntentSignal` was `attention_level=high`, `label=human_review_required`, `re_anchor_proposal_present=true`, full three-role coverage, one `anchor_clarity_gap` driver | Manual owner-authorised real-provider execution | Local evidence ids as above; Project `937c93c4-ac94-4fbe-b896-e13dc5f6b018`; Shot `310b242b-106c-4d5c-ae61-d24f8972f375`; Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b`; Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 6 — Cross-role Assessment Shot-page browser acceptance, including the history-clarity fix | Reported after Step 6 implementation and validation on branch `feat/step6-cross-role-alignment` | The Shot page for Task "Compositing Review" / Version "D1_STEP3_VFX_REVIEW_001", real browser session | The newest, compliant assessment (final compliant row above) renders first, labelled "Latest assessment" with its created timestamp, fully expanded — executive summary, all three role perspectives, agreements, tensions, local-optimum risks, dependencies, priorities, evidence gaps, evidence, the Re-anchor Proposal, the Intent Signal, and AI provenance all render, with no invented `0.5x`/`15%` value and no apply/accept/reject/materialise/confirm/create-draft control anywhere; the older, historically-flawed assessment moves under a collapsed-by-default "Previous assessments (N)" section carrying an explicit "these are immutable historical outputs... not the current assessment" disclaimer, individually collapsed with its own heading/timestamp, still deliberately inspectable on expansion but never presented as part of the latest assessment; zero/single-assessment states remain correct; the Human VFX Supervisor still sees Generate, Human CG Supervisor/Artist remain read-only; no additional model call is triggered; browser actions do not modify any repository file | Reported as observed by the project owner, in a real browser session | Manual owner-supplied browser execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same rendering/collapse scenarios are separately covered by automated tests (the Step 6 automated-behaviour row above) |

## Step 8 — Real ftrack Version/ReviewNote Reconciliation (dated 2026-08-02)

**Recorded 2026-08-02, branch `feat/step8c89-real-ftrack-acceptance`, HEAD `1440685` at time of recording.** Additive only — no row above this section was altered, deleted, or reinterpreted. This section records Step 8's real controlled-workspace ftrack Version/ReviewNote reconciliation acceptance; the full evidence trail lives in `docs/step-8/03_STEP_8C_REAL_FTRACK_ACCEPTANCE.md` and `docs/step-8/04_STEP_8_COMPLETION_BASELINE.md`.

Four distinct evidence layers are combined below, kept explicitly distinguishable rather than blended into one claim:

- **Real-workspace evidence** — what the real ftrack API actually returned when queried (read-only) against the controlled trial workspace (`bristol-l.ftrackapp.com`, the same workspace as Step 8A/8B).
- **Local-database evidence** — what a direct, read-only SQL query against the real local Postgres database showed after each real reconciliation run.
- **Automated-test evidence** — the existing, passing `services/worker`/`services/ftrack-connector`/`apps/api` pytest suites and the `apps/web` Vitest suite (mocked ftrack/HTTP throughout, per this project's own testing rule), already covering the reconciliation logic and the CG/Artist task-scoped filtering logic in isolation.
- **Owner visual evidence** — the project owner directly opening real, running pages in a real browser session.

### Validation table additions

| Validation name | Date / session time | Object / Shot used | Expected result | Observed result | Evidence type | Limitations |
|---|---|---|---|---|---|---|
| Step 8C-8 — Real ftrack Version/ReviewNote reconciliation acceptance | This session, on branch `feat/step8c89-real-ftrack-acceptance` | Controlled trial workspace `bristol-l.ftrackapp.com` (projects `sync`/`napo`); real Shot `bc0040` (evidentiary continuity with Step 8A/8B) among 9 real Shots linked | A Project/Shot/Task bootstrap (the pre-existing `reconcile_ftrack_shots` job, real read-only ftrack access) links at least one real Shot; the real `reconcile_ftrack_versions_and_notes` job then performs a complete per-linked-Shot AssetVersion/Note sweep, creating real `Version`/`ReviewNote` rows via the trusted internal sync API, each with exactly one `ExternalEntityLink`; a second identical run is a true no-op; no manual/pre-existing row is mutated; no ftrack entity is created, updated, or deleted | **Real-workspace evidence:** real read-only ftrack access succeeded; the Project/Shot/Task bootstrap linked 9 real Shots and 9 real Tasks (one Task per Shot, per that job's own "first new Task" shape); the first Version/Note reconciliation run reported `linked_shots_examined=9`, `asset_versions_discovered=32`, `direct_notes_discovered=40`, `review_session_object_notes_discovered=10`, `api_created=82`, `api_conflicts_or_failures=0`. **Local-database evidence:** `Version` count `2 → 34` (32 ftrack + 2 pre-existing manual, unchanged); `ReviewNote` count `1 → 51` (50 ftrack + 1 pre-existing manual, unchanged); every synced `Version`/`ReviewNote` had exactly one `ExternalEntityLink`; zero partial domain/link rows before or after; the 3 pre-existing manual rows (2 Version, 1 ReviewNote) were confirmed byte-identical (same ids, values, `created_at`) before and after. A second, immediately-following run reported `api_created=0`, `api_already_exists=82`, with `Version`/`ReviewNote`/`ExternalEntityLink` counts unchanged and zero duplicates — the locked no-op-on-repeat-sync contract confirmed empirically, not only by design | Manual owner-authorised real ftrack execution | Controlled/demo trial workspace, not DNEG production (unchanged claim boundary from Step 8A/8B); the Project/Shot/Task bootstrap linked only one real Task per Shot in this pass, so this real dataset does not itself demonstrate a Version excluded because it resolves to a *different* real Task under the same Shot — that exact rule remains supported by automated-test evidence only (next row); deleted `ReviewSessionObject`s referenced by a Note's `parent_id` cannot be reconstructed (a normal, expected ftrack cleanup pattern, not a defect); a ftrack-side content edit after first sync is not detected (no real modification-timestamp field exists on either entity, confirmed exhaustively in Step 8B); full detail in `docs/step-8/03_STEP_8C_REAL_FTRACK_ACCEPTANCE.md` |
| Step 8C-6/8C-7 — CG/Artist Task-scoped Version filtering — automated behaviour | N/A (test suite) | Synthetic fixture Versions/ReviewNotes | `filterVersionsForTask`/`isVersionInTaskScope` include a Version when its `task_id` matches the current Task or is null, and exclude a Version whose `task_id` is a *different* real Task; the same rule holds through the CG/Artist data loaders, the selected-Version/ReviewNote lookup, and Artist Feedback History's event filtering; VFX's Shot-wide page applies no Task filter; chronological ordering everywhere uses `source_created_at ?? created_at`; external-author provenance renders as source provenance, never an ICAS Human role | Passing (64/64 in the focused new/changed suite; 844/844 across the full `apps/web` Vitest suite) | Automated test | Uses synthetic fixtures throughout, including a fixture Version explicitly linked to a *different* Task id than the one under test — this is the automated proof for the specific cross-Task-exclusion scenario the real dataset above could not itself visually demonstrate; `apps/web/src/lib/taskScopedVersions.ts` + its test file, `apps/web/src/features/{cg,artist}/**/data.test.ts` |
| Step 8C-9 — Owner visual validation of real synced data | This session, on branch `feat/step8c89-real-ftrack-acceptance` | The four page/URL types below, all reachable from the real data synced in the row above | VFX Shot Versions, CG Task Version Review, Artist Current Version, and Artist Feedback History all render the real synced data correctly, with ftrack external authors shown as source provenance (never an ICAS Human role), and role-guarded routing redirects correctly | **VFX Shot Versions** (`bc0040`): page opened successfully; five real Versions visible; Versions from multiple real production contexts (multiple ftrack Tasks) visible together, as intended for the Shot-wide page; ReviewNotes loaded; external authors shown as source provenance. **CG Task Version Review** (`Animation` Task under `S1020`): page opened successfully; Version list, selected Version, and ReviewNotes loaded; Task/Shot/source metadata correct; external authors shown as "Source author", never an ICAS Human role. **Artist Current Version** (same Task): page opened successfully; selected Version and ReviewNotes loaded; metadata and author provenance correct. **Artist Feedback History** (same Task): page opened successfully; Version and Review Note events loaded in the timeline; no obvious duplicate records observed; known limitation confirmed still present — `ArtistFeedbackEventRead` has no `external_author_name` field, so these timeline events show System provenance rather than the real ftrack author's name. **Role guarding:** direct access to a role-scoped route without that role redirected to role selection with `returnTo`; selecting the correct role returned the user to the originally requested page | Manual owner-supplied browser execution | Not independently provable from Git history alone; no committed screenshot/transcript exists in this repository, consistent with this document's own conventions; does not visually demonstrate cross-Task exclusion, for the same real-dataset reason named in the row above; full detail and exact URLs in `docs/step-8/03_STEP_8C_REAL_FTRACK_ACCEPTANCE.md` §14 |

### Step 8 verdict (summary)

Real, authenticated, read-only ftrack access; a real Project/Shot/Task bootstrap; two real Version/ReviewNote reconciliation runs (first-run creation, second-run true no-op); complete database-integrity verification (link uniqueness, no partial rows, manual-row preservation); and owner visual validation of all four named page types are all now recorded. Combined with the pre-existing automated-test evidence for the CG/Artist Task-scoped filtering logic (which the real dataset's own single-Task-per-Shot bootstrap could not itself visually exercise), Step 8's ftrack Version/ReviewNote sync capability has real-workspace, local-database, automated-test, and owner-visual evidence at every layer. See `docs/step-8/04_STEP_8_COMPLETION_BASELINE.md` for the full closure record and `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` for Step 8's roadmap status.

## What this table does not claim

- It does not claim DNEG production data was used anywhere (`docs/PRODUCT_SCOPE.md` §14.6's claim boundary applies: the ftrack validation used a controlled test workspace).
- It does not claim the manual validations are reproducible by a third party from this repository alone — they required real, owner-held ftrack and DeepSeek credentials that are never committed.
- It does not claim the Cross-role Assessment capability, its optional Re-anchor Proposal, or its Intent Signal are merged to `main` — they are implemented and real-DeepSeek-validated on `feat/step6-cross-role-alignment`, complete on that feature branch, pending commit, push, PR, and merge; see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D for the exact branch/merge state. The VFX Supervisor Agent's `creative_review` capability, the CG Supervisor Agent's `execution_review` capability, and the Artist Agent's `iteration_guidance` capability remain the three real, validated, merged Role Agent capabilities; Cross-role Assessment is a fifth Core Agent capability, not a fourth Role Agent.
- It does not claim Step 2 (Lightweight Shared Agent Runtime) is still unmerged — it is merged to `main` via PR #11, merge commit `bd2f670` (`ac9e6c0` refactor: add lightweight shared agent runtime); see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D for the exact merge state.
- It does not claim Step 1 as a whole is still unmerged. Step 1A, Step 1B, Step 1C, and Step 1D are all implemented, validated, and merged to `main` — Step 1A (PR #7, `2d1101f`), Step 1B (PR #8, `cd24eaf`), Step 1C (PR #9, `e306087`), Step 1D (PR #10, `b1f3747`). Step 1 as a whole (1A + 1B + 1C + 1D) is complete and merged.
- It does not claim the shared Agent Runtime (`agents.runtime.execute_agent`, the shared Model Gateway, or the Prompt Registry) is itself an Agent, a Role Agent, or a fifth Core Agent capability — it is shared execution infrastructure that the four existing Core Agent capabilities (`core_anchor_drafting`/`intent_decomposition`/`context_reconstruction`/`alignment_assessment`) and all three Role Agent capabilities (`creative_review`, `execution_review`, `iteration_guidance`) were all migrated onto or built onto with no behavioural change to Core Agent, and it absorbs no domain-specific logic or authority of its own.
- It does not claim Step 3 (VFX Supervisor Agent) is still unmerged — it is merged to `main` via PR #12, merge commit `d68569e` (`e986ba5` feat: add vfx supervisor agent); see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D for the exact merge state.
- It does not claim Step 4 (CG Supervisor Agent + Execution Anchor CG Human Gate UI) is still unmerged — it is merged to `main` via PR #13, merge commit `6aa3031` (`2b89ac2` feat: add cg supervisor agent and execution anchor gate); see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D for the exact merge state.
- It does not claim Step 5 (Artist Agent) is still unmerged — it is merged to `main` via PR #14, merge commit `93fb730` (`81a79be` feat: add artist agent iteration guidance); see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D for the exact merge state.
- It does not claim Step 6 (Cross-role Assessment + Re-anchor Proposal + Intent Signal) is merged to `main` — it is implemented and real-DeepSeek-validated on `feat/step6-cross-role-alignment`, complete on that feature branch, pending commit, push, PR, and merge; Step 7 is the immediate next locked step, gated on Step 6's merge (see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §H/§I). **[Note added 2026-08-01: Step 7 has since been completed on branch `recovery/step7c2-functional-clean` — see the "Step 7 — Global Functional Baseline" section below and `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §J. This sentence is left as originally written; it accurately describes the state at the time Step 6 was recorded.]**
- It does not claim the first, historically-flawed Cross-role Assessment result (`5a6d16d5-2c85-4f22-8057-cbebc77ecc66`) was deleted, rewritten, corrected, or invalidated because its advisory Re-anchor Proposal invented unsupported numeric values — it remains an immutable historical record, superseded in the newest-first list by the final compliant result, not removed.
- It does not claim the VFX Supervisor Agent is the Core Agent or the Human VFX Supervisor, that its `VFXSupervisorReview` output is authoritative, that it can confirm/reject a Core Anchor or Version, resolve a `HumanGate`, create a `Decision`, create an actual `ReviewNote`, or write to ftrack, or that it ever claimed to have visually inspected image or video media — its output is advisory only and explicitly records the missing-media limitation in `evidence_gaps`.
- It does not claim the CG Supervisor Agent is the Core Agent, the VFX Supervisor Agent, or the Human CG Supervisor, that its `CGSupervisorReview` output is authoritative, that it can confirm/reject an Execution Anchor revision, resolve a `HumanGate`, create a `Decision`, or write to ftrack, or that it ever claimed to have inspected footage, rendered frames, or scene/project/DCC files — its output is advisory only, explicitly records that limitation in `evidence_gaps`, and is additionally checked by a code-level content-boundary validator (not merely prompt-text guidance) before persistence.
- It does not claim the Artist Agent is the Core Agent, the VFX Supervisor Agent, the CG Supervisor Agent, or the Human Artist, that its `ArtistAgentGuidance` output is authoritative, that it can establish/modify/confirm/reject/replace/supersede either Anchor, resolve a `HumanGate`, create a `Decision`, create an actual `ReviewNote`, or write to ftrack, or that it ever claimed to have inspected footage, rendered frames, scene/project/DCC files, or numeric production parameters — its output is advisory only, explicitly records that limitation in `evidence_gaps`, and is additionally checked by a code-level content-boundary validator (covering both direct and indirect Anchor-modification instructions, not merely prompt-text guidance) before persistence.
- It does not claim `Version` gained a real `task_id` foreign key — the Artist Agent's explicit `task_id` request-body field is a per-generation context selection made by the caller, not a schema change, and is not claimed as a permanent Version-to-Task ownership relationship. **[Note added for Step 8: this sentence is left as originally written; it accurately described the state at the time Step 5 was recorded. Step 8 (migration `0024`) has since added a real, nullable `Version.task_id` foreign key — see the "Step 8" section below and `docs/DOMAIN_MODEL.md`. The Artist Agent's own request-body `task_id` field remains unchanged by that addition — it was, and remains, a per-generation context selection, not itself the schema column.]**
- It does not claim a `HumanGate` can be resolved by any Agent, or that any generic create/update/delete/resolve/list/workflow API, or any generic public "execute agent" API, was added — each capability's own small set of endpoints remains the only user-facing surface.
- It does not claim the real Step 8 dataset visually demonstrated a Version being excluded because it resolves to a *different* real ftrack Task under the same Shot — the real bootstrap linked only one Task per Shot, so no real Shot in that dataset had Versions resolved to two different real Task ids simultaneously; that exact rule is supported by automated-test evidence only (see the Step 8 section above).
- It does not claim every real ftrack Note is importable as a `ReviewNote`, that every `ReviewSessionObject` a Note references still exists, or that a ftrack-side content edit made after first sync is ever reflected in ICAS — all three are named, permanent limitations of the locked Step 8 sync contract (ADR-0014), not defects.
- It does not claim `Version`/`ReviewNote` ftrack identity can be submitted through the public `VersionCreate`/`ReviewNoteCreate` contracts, or that any `Version`/`ReviewNote` `SyncCursor` exists — ftrack identity for these two entities enters only through the separately-authenticated trusted internal sync endpoints (`POST /internal/sync/versions`/`/review-notes`), and Version/Note reconciliation performs a complete per-linked-Shot re-sweep on every run with no cursor of any kind (ADR-0014 Decision 4).
- It does not claim `external_author_id` is ever sourced from a ftrack username, email, or display name — it is always the real ftrack stable account id (`AssetVersion.user.id`/`Note.author.id`); `external_author_name` is a separate, independently-nullable display fallback.

## Security note (Step 1B, Step 1C, Step 1D, Step 2, Step 3, Step 4, Step 5, Step 6)

Recorded as durable repository fact, verified via Git metadata, ahead of each real-provider or real-runtime acceptance run in the validation table above:

- the local `.env` file is Git-ignored;
- the local `.env` file is not tracked by Git;
- the local `.env` file has never appeared in Git history;
- no Step 1B, Step 1C, Step 1D, Step 2, Step 3, Step 4, Step 5, or Step 6 source, test, or generated file contains a copied real credential.

Step 1D's own runtime-preparation row above used the deterministic provider only (`MODEL_PROVIDER=deterministic`) and made no real DeepSeek or ftrack call; it is included here because it still exercised the local `.env`-handling discipline this section records, not because it required real external credentials. Step 2's, Step 3's, Step 4's, Step 5's, and Step 6's real DeepSeek acceptance rows above each made one or more real DeepSeek calls, using the same local `.env`-handling discipline as Step 1B/1C's own real-provider acceptance rows; the recorded evidence never displays the API key, `Authorization` header, or any other client configuration. Step 4's two failed real-provider attempts, Step 5's one failed real-provider attempt, and Step 6's three failed real-provider attempts are recorded with safe, sanitised diagnostics or a fixed sanitised reason string only (never the raw response content, prompt text, partial JSON, or invented numeric value).

This note records the repository-verifiable result only. It does not claim that any credential exposed earlier during this work is safe — external key rotation, where it occurred, was handled by the project owner outside this repository.

## Known non-blocking observations (Step 1A)

Observed during the Step 1A-Backend and Step 1A-UI validation passes above. None is a newly discovered product defect; all are recorded as observed, intentional prototype behaviour.

**A. Extra input fields (Step 1A-Backend).** Client-supplied fields such as `id`, `order_index`, `created_at`, and `created_by_actor_id` on a semantic-item input are currently *ignored* by the Pydantic input schemas rather than rejected. The server still generates its own UUID, derives `order_index` from array position, stamps its own `created_at`, and never accepts client-supplied provenance as authoritative. This is observed input-contract behaviour, not blocking for this prototype slice.

**B. SQLite `created_at` representation (Step 1A-Backend).** During the isolated-SQLite acceptance pass, `created_at` values were serialized without an explicit UTC offset. This was also observed on pre-existing `Project`/`Shot` objects (not introduced by migration `0014`), and is a known `DateTime(timezone=True)` round-trip characteristic of the SQLite dialect specifically (see migration `0010`'s own handling of a related PostgreSQL-timestamp issue). The production PostgreSQL path remains the intended source of truth for timezone-aware behaviour.

---

## Step 7 — Global Functional Baseline (dated 2026-08-01)

**Recorded 2026-08-01, branch `recovery/step7c2-functional-clean`, HEAD `dbf2a30` at time of recording.** Additive only — no row above this section was altered, deleted, or reinterpreted. This section closes two evidence gaps identified by `docs/step-7/20_STEP_7C_GLOBAL_FUNCTIONAL_AUDIT.md` and records that audit's own verdict.

### Validation table additions

| Validation name | Date / session time | Object / Shot used | Expected result | Observed result | Evidence type | Limitations |
|---|---|---|---|---|---|---|
| Step 7C-4 — Real DeepSeek Execution Anchor drafting acceptance | Reported by the project owner during Step 7C-4 implementation, on branch `recovery/step7c2-functional-clean` | A CG Supervisor Workspace Task's Execution Anchor draft, created via the real application API | `provider=deepseek`; `model=deepseek-v4-flash`; `prompt_version=cg_execution_anchor_drafting.v1`; `AgentRun.status=succeeded`; a real `ContextSnapshot` recorded; all eight supported Execution Anchor fields (`technical_boundaries`, `parameter_ranges`, `delivery_conditions`, `production_ready_criteria`, `downstream_dependencies`, `publish_requirements`, `allowed_refinements`, `escalation_conditions`) persisted onto a new `ExecutionAnchorRevision` with `status="draft"`; nothing auto-confirmed — Human CG Supervisor confirmation still required to move the revision past draft status | Reported as observed by the project owner: a succeeded `AgentRun`, a real `ContextSnapshot`, and a Draft with all eight fields populated, exactly as expected | Manual owner-supplied execution | No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; no specific `AgentRun`/`ContextSnapshot`/`ExecutionAnchorRevision` id was supplied for this documentation pass, unlike several earlier rows in this table — this row exists specifically to close the durable-evidence gap the global functional audit identified (implementation and provider validation both already existed; only the committed record did not); the automated DeepSeek-path test for this capability (`test_execution_anchor_deepseek_generation.py`) remains explicitly mocked-only, as intended, and is not itself evidence of a real call |
| Step 7C-5 — Real DeepSeek Artist Guidance acceptance (`artist_iteration_guidance.v2`) | This session, during the Step 7C-5 DeepSeek-generation fix, on branch `recovery/step7c2-functional-clean` | Task "Compositing Review", Version `D1_STEP3_VFX_REVIEW_001`, on the D1 demo Shot (seeded via `/internal/demo/ensure-d1-scenario` after a local dev-database schema reset performed earlier in the same work) | `provider=deepseek`; `model=deepseek-v4-flash`; `prompt_version=artist_iteration_guidance.v2`; `AgentRun.status=succeeded`; a real `ContextSnapshot` recorded; a new `ArtistAgentGuidance` persisted citing the correct confirmed Core Anchor and Execution Anchor revisions; confirming a newer Execution Anchor revision correctly flips `guidance_state` to `outdated`; Regenerate produces a new current row; the two real pre-fix failures (root-caused to the configured model's internal reasoning tokens consuming the entire completion budget before any visible content, `finish_reason="length"` with empty content) created zero orphan `ArtistAgentGuidance` rows | Observed exactly as expected: first real success `AgentRun 31cfa3ad-5d3d-448e-8a9e-b3440c0e0de4` (`ContextSnapshot 2ecdbf46-df14-4b55-adfd-f95936bb641c`, `ArtistAgentGuidance a486ad69-a928-4fac-a05c-6a9d2a744440`), ~14s round trip, real non-deterministic content citing the correct Core/Execution Anchor revision ids in its evidence references. After confirming a new Execution Anchor revision, `guidance_state` flipped to `outdated`; Regenerate succeeded (`AgentRun a3a15b8c-...`, new row `f46f2290-2853-4b15-8102-1c8e46730a71`, citing the new revision), and `guidance_state` returned to `current`. Total `ArtistAgentGuidance` row count for the Version matched exactly before/after (seed row + 2 real generations = 3), confirming the two earlier real pre-fix failures left no orphan rows | Manual owner-authorised real-provider execution, performed within this assistant-driven session | No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; performed against a freshly-reset local dev database (schema drop/recreate + re-seed), not the same acceptance database used by Steps 1-6's rows above — the seed and ids are therefore specific to this session's own database state, not the shared `937c93c4-...`/`310b242b-...` acceptance Shot used throughout Steps 1-6 |

### Global functional-audit verdict (summary)

`docs/step-7/20_STEP_7C_GLOBAL_FUNCTIONAL_AUDIT.md` (read-only audit, corrected 2026-08-01) found no functional blocker across the VFX Supervisor, CG Supervisor, and Artist workspaces; the nine Agent capabilities; the Review Inbox work-item architecture in all three roles; the Core Anchor / Execution Anchor / HumanGate / Decision chain; or role-aware deep-link and `returnTo` security. Of the nine Agent capabilities, only `core_anchor_drafting` remains without any DeepSeek code path at all (deterministic-only by design, not by gap); every other capability now has either historic or newly-recorded real-provider evidence, per the two rows added above and the six pre-existing Steps 1-6 capabilities. Final audit recommendation: functionally ready for visual refinement, independent of scheduling — see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §J.6 for the recorded decision to sequence visual refinement after Step 8 and before Step 9, and `docs/step-7/21_STEP_7_COMPLETION_BASELINE.md` for the complete Step 7 closure record.

**C. Actor-id role-switch prototype behaviour (Step 1A-UI).** Role defaults are `vfx_supervisor` → `vfx-1`, `cg_supervisor` → `cg-1`, `artist` → `artist-1`. When Role changes, an untouched/default Actor id updates to the new role's default; a manually entered custom Actor id is preserved across Role changes within the same mounted page session. A full browser refresh remounts the page and restores the current role's default Actor id — no identity persistence, authentication, `localStorage`, or URL state was added. This is intentional prototype interaction behaviour, not a persistent identity system, and is not a defect.
