# VALIDATION_EVIDENCE.md

**Project:** Intent Core Alignment System
**Purpose:** A concise, evidence-typed record of what has been validated, how, and with what limitations.
**Companion document:** `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §E covers the same manual validations in narrative form; this document is the structured table.

## Evidence types

- **Automated test** — a passing test in this repository's committed test suite. Reproducible by running `pytest` (`apps/api`) or `vitest run` (`apps/web`).
- **Repository evidence** — provable by inspecting committed code, migrations, or Git history directly.
- **Manual owner-supplied execution** — reported by the project owner as having been run against a real external system (real ftrack workspace, real DeepSeek API), or against a real locally-running process (a real `uvicorn` server, real HTTP requests, no `TestClient`) exercised outside the automated `pytest`/`vitest` suite. Not reproducible from the repository alone merely by re-running the test suite — it requires actually starting the described process(es) again; this project's own testing rules deliberately keep real external calls, and full real-process HTTP walkthroughs, out of the automated suite.

No API keys, credentials, full environment-variable values, or other sensitive account details appear anywhere in this document.

## Validation table

| Validation name | Date / session time | Object / Shot used | Expected result | Observed result | Evidence type | Limitations |
|---|---|---|---|---|---|---|
| Real ftrack Project/Shot/Task sync | Reported during the ftrack integration work (see commit `2b27fca`) | Shot `bc0040` | Real ftrack Project/Shot/Task data appears in ICAS as `source=ftrack`; reconciliation worker completes without error | Reported as observed by the project owner | Manual owner-supplied execution | No committed transcript or log of the run exists in this repository; not independently re-verifiable from Git alone |
| Reconciliation worker (`reconcile_ftrack_shots`) — automated behaviour | N/A (test suite) | Synthetic fixture Shots | Cursor advances correctly; a second run against unchanged data is a no-op | Passing | Automated test | Only proves the polling/cursor logic against a mocked ftrack session, not that a real workspace was actually reached |
| Real ftrack controlled write-back | Reported alongside the sync validation above (commit `2b27fca`, ADR-0012) | Shot `bc0040` | Confirming a Core Anchor with `request_write_back=True` produces exactly one new ftrack Shot Note; no existing Note is overwritten; write-back is human-requested, not automatic | Reported as observed by the project owner | Manual owner-supplied execution | No committed transcript; the write-back content marker (`"[Intent Core Alignment System]"`) is repository-verifiable, but the actual ftrack-side Note creation is not |
| Write-back request/record creation — automated behaviour | N/A (test suite) | Synthetic fixture Shots | A `WritebackRecord` is created only when the Shot has a real `ExternalEntityLink`; a human-requested write-back enqueues the worker job | Passing | Automated test | Only proves the local record/enqueue logic; does not reach real ftrack |
| Real DeepSeek Alignment Assessment | Reported during Step 4b/ADR-0013 follow-up manual testing | A Version under Shot `bc0040` | `provider=deepseek`; `AgentRun.status=succeeded`; `AlignmentAssessment.alignment_state=significant_drift`; `envelope.requires_human_gate=true`; evidence field cites the confirmed Core Anchor, the Version description, and a Review Note; no Anchor or ftrack state modified | Reported as observed by the project owner | Manual owner-supplied execution | No committed transcript of the raw DeepSeek response exists in this repository (by design — it may contain content not appropriate to commit); the deterministic-provider path and the DeepSeek adapter's request-construction logic are separately covered by automated tests (next row) |
| DeepSeek adapter request/response handling — automated behaviour | N/A (test suite) | Synthetic fixture assessment payload | Adapter makes exactly one non-streaming JSON-mode call with the correct model/messages/`response_format`; empty-content responses raise `AgentGenerationError` | Passing | Automated test | Uses a mocked SDK client (`openai.OpenAI` stubbed) — never a real network request, by explicit project rule |
| Decision supersession | Reported alongside the DeepSeek validation, and separately covered by automated tests | Assessments under Shot `bc0040` | A later human Decision correctly sets `supersedes_decision_id` to the prior active Decision for the same Shot; the old Decision remains unmodified and independently readable | Reported as observed by the project owner (manual); also proven automatically (next row) | Manual owner-supplied execution + Automated test | The manual run demonstrates it against real generated data; the automated tests demonstrate the same logic against fixtures, including a 3-link chain and cross-Shot isolation |
| Decision supersession chain — automated behaviour | N/A (test suite) | Synthetic fixture assessments across 2 Shots | A 3-Decision chain supersedes only the current unsuperseded head, not always the first; a Decision on another Shot never joins the chain | Passing | Automated test | `apps/api/tests/test_alignment_assessment_decisions.py` |
| Browser validation of the Version/Assessment UI | Reported after Step 4d | A Version under Shot `bc0040` | Version fields, Review Notes, confirmed Core Anchor summary, Alignment Assessments, AI provenance (agent type/provider/run status/context snapshot), Human Gate, and supersession labels all render correctly in a real browser session | Reported as observed by the project owner | Manual owner-supplied execution | The same rendering logic is separately covered by 16 automated frontend tests (`VersionPage.test.tsx`) using a mocked `fetch`, not a real backend |
| Frontend rendering — automated behaviour | N/A (test suite) | Fixture data | Version description, Review Notes, every Assessment field, `requires_human_gate`, DeepSeek provenance, Accept/Reject gating by role, accept/reject request shape, no-second-decision-action, supersession labels, generate-button loading/refresh, loading/error states all render/behave correctly | Passing (43/43 across `ShotAnchorPage.test.tsx` + `VersionPage.test.tsx`) | Automated test | Mocked `fetch` throughout; never calls a real backend or DeepSeek |
| Step 1A-Backend Core Anchor semantic objects — automated behaviour | N/A (test suite) | Synthetic fixture Shots/revisions | 18 focused tests cover: creating all five collections (`constraints`/`variation_zones`/`drift_risks`/`references`/`open_questions`); PATCH replace/clear/omit semantics; `order_index` assignment from input array position; blank-required-value rejection; CG Supervisor/Artist authority denial; a service-level agent actor supplying collections without gaining confirm/reject authority; immutability after confirm/reject/supersede; no row-sharing between revisions; empty-array shape for childless revisions; `AuditEvent` before/after content; atomic failure of a replacement; no side effects on other entities. Full 206-test backend suite and 43-test frontend suite pass with no regression | Passing (18/18 focused, 206/206 backend, 43/43 frontend) | Automated test | `apps/api/tests/test_core_anchor_semantic_objects.py`; migration `0014` upgrade/downgrade/upgrade, `ruff check`, `ruff format --check`, `mypy`, OpenAPI export, TypeScript contract regeneration, and contracts/web `tsc --noEmit` all also passed (repository evidence, not a separate row here) |
| Step 1A-Backend Core Anchor semantic objects — real HTTP API acceptance | Reported after Step 1A-Backend implementation and validation on branch `feat/step1a-core-anchor-semantics`, committed `173f32e` | A synthetic Project/Shot/IntentBrief/Core Anchor draft created solely for this validation (not `bc0040`, not ftrack-linked) | All five semantic collections create/PATCH-replace/clear/omit correctly with server-generated ids/`order_index`/`created_at`; the resulting `AuditEvent` records normalized before/after content, not counts; a blank required value returns 422; Human CG Supervisor and Human Artist both receive 403; confirming the revision then attempting to PATCH it again returns 409 with all semantic content unchanged; a second revision created afterward returns five empty collections and shares no child row with the first; editing semantic content creates no `AgentRun`/`ContextSnapshot`/`ExecutionAnchor`/`ExecutionAnchorRevision`/`WritebackRecord`; confirming with `request_write_back=false` creates exactly one human `Decision` and no `WritebackRecord` | Reported as observed by the project owner, via a real `uvicorn` process and real HTTP requests (no `TestClient`) | Manual owner-supplied execution | Run against an isolated temporary SQLite database (never `apps/api/dev.db`, never the developer's PostgreSQL database) — not against a real external system, unlike the ftrack/DeepSeek rows above; no committed transcript of the raw HTTP traffic exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (previous row) |
| Step 1A-UI Core Anchor semantic editors — automated behaviour | N/A (test suite) | Fixture data (`ShotAnchorPage.test.tsx`) | 70 frontend tests pass: 54 in `ShotAnchorPage.test.tsx` (including the semantic-collection editors: view/add/edit/remove/move up/move down for all five collections, dirty-collection PATCH omission/clear/replace, order-from-array-position, blank-value rejection, CG/Artist read-only gating, save-success status show/clear on edit/Cancel/revision-change, pending "Saving…" state, failed-save value preservation, role-specific default Actor id with custom-id preservation) + 16 in `VersionPage.test.tsx` (unchanged); web TypeScript typecheck, ESLint, and Prettier all pass; the Next.js production build succeeds with all five existing routes built | Passing (70/70) | Automated test | Mocked `fetch` throughout; never calls a real backend or DeepSeek. No backend, migration, Python contract, generated contract, `VersionPage`, dependency, or documentation file was changed while implementing this slice. |
| Step 1A-UI Core Anchor semantic editors — browser acceptance | Reported after Step 1A-UI implementation and validation on branch `feat/step1a-core-anchor-semantics`, pending merge to main | The Shot Anchor page's Core Anchor Human Gate, real browser session | All five semantic editors rendered under the current Core Anchor draft; Human VFX Supervisor could add and edit sentence-length content; add/remove/reorder controls worked; Reference label, optional URI, and optional note rendered and saved; Open Question reordering was preserved; blank required content was blocked by client-side validation; Save persisted data and displayed "Changes saved."; a later edit cleared the prior success status; Cancel restored server-saved values; Human CG Supervisor saw a read-only "Draft details" view with actor id `cg-1`; Human Artist saw the corresponding read-only view with actor id `artist-1`; neither CG nor Artist received semantic or scalar editing controls; a custom Actor id was preserved across role changes in the same page session; a full refresh restored the current role's default Actor id; confirmation preserved all semantic collections and made them read-only; no ftrack write-back was requested; browser use did not modify repository files | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (previous row) |
| Step 1B Core Agent Intent Decomposition — automated behaviour | N/A (test suite) | Synthetic fixture Shots/briefs | Migration `0015` upgrade/downgrade/upgrade passes; deterministic-provider generation produces distinct, non-blank content per dimension; `ContextSnapshot` payload scoped to exactly `project`/`shot`/`intent_brief`/`tasks`; generation never creates a Core Anchor, revision, or Decision; the decomposition-to-draft action maps fields correctly with fresh child ids, requires Human VFX Supervisor, refuses to overwrite an existing draft, and makes no second model call; full 235-test backend suite and 84-test frontend suite (68 `ShotAnchorPage.test.tsx` + 16 `VersionPage.test.tsx`) pass with no regression | Passing (235/235 backend, 84/84 frontend) | Automated test | `apps/api/tests/test_intent_decomposition.py`, `apps/api/tests/test_core_anchor_draft_from_decomposition.py`; `ruff check`, `ruff format --check`, standard API/contracts `mypy` (61 source files) and all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration/typecheck, web TypeScript typecheck, ESLint, repository Prettier check, and the Next.js production build all also passed (repository evidence, not a separate row here); a separate, broader ad hoc `mypy` invocation that also scanned `apps/api/tests` found eight pre-existing, test-only typing errors outside CI's scope — not Step 1B failures (see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D) |
| Step 1B — Real DeepSeek Intent Decomposition acceptance | Reported after Step 1B implementation and validation on branch `feat/step1b-intent-decomposition` | A dedicated local acceptance Shot, `D1_STEP1B_ACCEPTANCE_001` (not `bc0040`, not `D1_TEST_SHOT_001`, not ftrack-linked) | `provider=deepseek`, `model=deepseek-v4-flash`; exactly one real model call; `AgentRun.status=succeeded`; all seven dimensions have non-blank summary and rationale; candidate constraints, candidate variation zones, and contextual information are populated; uncertainties is an explicit empty list; `ContextSnapshot` payload contains exactly `project`/`shot`/`intent_brief`/`tasks`; no Core Anchor or `CoreAnchorRevision` is created during generation; no Decision, WritebackRecord, Role Agent run, or ftrack network call occurs | Reported as observed by the project owner, via a real running API process and real HTTP requests (no `TestClient`), with real DeepSeek credentials | Manual owner-supplied execution | Local evidence ids: Shot `1e433dca-5a9e-46c1-9f6d-25938af49efd`; IntentDecomposition `1850f1d9-fc52-4530-a0a3-8976a94183da`; AgentRun `9eabe0b9-b0dd-448c-849e-36d0318b730b`; ContextSnapshot `423629da-5f21-4f17-b128-f95cc4111564`. No committed transcript of the raw DeepSeek response exists in this repository, consistent with this document's conventions; see the security note below for how the local `.env` was handled during this run |
| Step 1B — Decomposition-to-Core-Anchor browser acceptance | Reported after Step 1B implementation and validation on branch `feat/step1b-intent-decomposition` | The Shot Anchor page for the `D1_STEP1B_ACCEPTANCE_001` acceptance Shot, real browser session | The real DeepSeek decomposition appeared after Intent Brief and before Core Anchor, labelled "AI proposal — Core Agent"; provider, succeeded run status, and `ContextSnapshot` provenance were visible; all seven dimensions and rationales rendered; five candidate Must-preserve items and three candidate Allowed-variation items rendered; contextual information rendered; uncertainties displayed an explicit empty state; the Human VFX Supervisor could explicitly use the decomposition; a new editable `CoreAnchorRevision` draft was created with visible source-decomposition lineage; scalar fields mapped from the selected decomposition; five Constraints and three VariationZones were created; no DriftRisk, AnchorReference, or OpenQuestion was invented; generated draft content remained editable with working save feedback and persistence; the draft remained unconfirmed; no automatic Decision or ftrack write-back occurred; the existing-draft conflict check prevented a silent duplicate overwrite; browser actions did not modify repository files | Reported as observed by the project owner, in a real browser session | Manual owner-supplied execution | Not independently provable from Git history alone; no committed transcript/screenshot exists in this repository, consistent with this document's own conventions; the same scenarios are separately covered by automated tests (the Step 1B automated-behaviour row above) |

## What this table does not claim

- It does not claim DNEG production data was used anywhere (`docs/PRODUCT_SCOPE.md` §14.6's claim boundary applies: the ftrack validation used a controlled test workspace).
- It does not claim the manual validations are reproducible by a third party from this repository alone — they required real, owner-held ftrack and DeepSeek credentials that are never committed.
- It does not claim any Role Agent (VFX Supervisor Agent, CG Supervisor Agent, Artist Agent) was validated — none exists yet (see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §C).
- It does not claim Step 1C (Core Agent Context Reconstruction) or Step 1D (minimal persistent HumanGate) exist or were validated — neither has started; Step 1A (Backend and UI) and Step 1B (Core Agent Intent Decomposition) are the only complete sub-slices of Step 1 so far, both pending merge to `main` (see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §H/§I).
- It does not claim Step 1 as a whole is complete — Step 1C and Step 1D remain not started.
- It does not claim Step 1A or Step 1B has been merged to `main`. Step 1A-Backend is committed as `173f32e`; Step 1A-UI is implemented and validated on `feat/step1a-core-anchor-semantics`; Step 1B is implemented and validated on `feat/step1b-intent-decomposition`. Both Step 1A and Step 1B are complete on their respective feature branches, pending merge to `main`; see `docs/IMPLEMENTATION_STATUS_AND_ROADMAP.md` §D for the exact branch/merge state.

## Security note (Step 1B)

Recorded as durable repository fact, verified via Git metadata, ahead of the real DeepSeek acceptance run in the validation table above:

- the local `.env` file is Git-ignored;
- the local `.env` file is not tracked by Git;
- the local `.env` file has never appeared in Git history;
- no Step 1B source, test, or generated file contains a copied real credential.

This note records the repository-verifiable result only. It does not claim that any credential exposed earlier during this work is safe — external key rotation, where it occurred, was handled by the project owner outside this repository.

## Known non-blocking observations (Step 1A)

Observed during the Step 1A-Backend and Step 1A-UI validation passes above. None is a newly discovered product defect; all are recorded as observed, intentional prototype behaviour.

**A. Extra input fields (Step 1A-Backend).** Client-supplied fields such as `id`, `order_index`, `created_at`, and `created_by_actor_id` on a semantic-item input are currently *ignored* by the Pydantic input schemas rather than rejected. The server still generates its own UUID, derives `order_index` from array position, stamps its own `created_at`, and never accepts client-supplied provenance as authoritative. This is observed input-contract behaviour, not blocking for this prototype slice.

**B. SQLite `created_at` representation (Step 1A-Backend).** During the isolated-SQLite acceptance pass, `created_at` values were serialized without an explicit UTC offset. This was also observed on pre-existing `Project`/`Shot` objects (not introduced by migration `0014`), and is a known `DateTime(timezone=True)` round-trip characteristic of the SQLite dialect specifically (see migration `0010`'s own handling of a related PostgreSQL-timestamp issue). The production PostgreSQL path remains the intended source of truth for timezone-aware behaviour.

**C. Actor-id role-switch prototype behaviour (Step 1A-UI).** Role defaults are `vfx_supervisor` → `vfx-1`, `cg_supervisor` → `cg-1`, `artist` → `artist-1`. When Role changes, an untouched/default Actor id updates to the new role's default; a manually entered custom Actor id is preserved across Role changes within the same mounted page session. A full browser refresh remounts the page and restores the current role's default Actor id — no identity persistence, authentication, `localStorage`, or URL state was added. This is intentional prototype interaction behaviour, not a persistent identity system, and is not a defect.
