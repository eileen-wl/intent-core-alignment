# IMPLEMENTATION_STATUS_AND_ROADMAP.md

**Project:** Intent Core Alignment System
**Document type:** Repository status baseline and locked future roadmap
**Status:** Source of truth for "what is actually implemented" and "what comes next, in what order"
**Baseline established:** Step 0A, against branch `chore/step0-baseline-roadmap`, evaluated at commit `babb916`

This document exists because `docs/TEAM_WORKFLOW.md` §2 requires shared knowledge to live in the repository, not in personal conversations. It replaces any informal, conversational description of "the roadmap" with one committed document. It must be kept current as future steps land — this is a living status document, not a one-time snapshot.

---

## A. Canonical architecture

ICAS is built around exactly four Agents, organised as:

```
Core Agent
    +
Role Layer
    ├── VFX Supervisor Agent
    ├── CG Supervisor Agent
    └── Artist Agent
    +
Dashboard (shared human control surface)
```

This structure is fixed. It must not be reinterpreted, and no future step may collapse the Role Layer into the Core Agent or treat the Core Agent as a stand-in for any Role Agent.

### Authority table

| Actor | Kind | Advisory or authoritative | May confirm/reject an Anchor or Decision? | May write to ftrack? |
|---|---|---|---|---|
| **Core Agent** | Agent | Advisory only | No | No |
| **VFX Supervisor Agent** | Agent (implemented, Step 3; merged to `main`) | Advisory only | No | No |
| **CG Supervisor Agent** | Agent (implemented, Step 4; merged to `main`) | Advisory only | No | No |
| **Artist Agent** | Agent (implemented, Step 5; merged to `main`) | Advisory only | No | No |
| **Human VFX Supervisor** | Human | Authoritative | Yes — Core Anchor confirm/reject; Alignment Assessment accept/reject | Yes — Core Anchor write-back, human-requested |
| **Human CG Supervisor** | Human | Authoritative | Yes — Execution Anchor confirm/reject (backend + frontend Human Gate UI) | Within CG authority (not yet implemented) |
| **Human Artist** | Human | Non-authoritative (cannot confirm/modify Anchors) | No | No |

This table is a direct restatement of `docs/ROLE_PERMISSIONS.md` §2–3, not a new policy. It is included here so the current implementation status (section C onward) can be checked against it without cross-referencing multiple documents.

---

## B. Critical terminology boundaries

The following statements are non-negotiable and must be treated as guardrails for every future implementation step and every future status report:

- **Core Agent != VFX Supervisor Agent.** Core Agent is the system-level intent control centre (`docs/AGENT_CONTRACTS.md` §4). It is not a generic name for "the VFX side" of the system, and it is not a placeholder that stands in for the VFX Supervisor Agent until the latter is built.
- **Human Gate != Role Agent.** A human (VFX Supervisor, CG Supervisor, or Artist) confirming, rejecting, accepting, or requesting write-back through a Human Gate is a human-authority workflow. It does not prove that the corresponding Role Agent exists. Today's Human Gates (Core Anchor confirm/reject, Alignment Assessment accept/reject) are exercised entirely by **humans**, not by any Agent.
- **ContextSnapshot != Context Reconstruction.** `ContextSnapshot` (implemented, `agents/models.py`) is runtime provenance — an immutable record of exactly what local data one Agent Run was given. It is not the Core Agent's documented "Context Reconstruction" product capability (`docs/AGENT_CONTRACTS.md` §4, `docs/PRODUCT_SCOPE.md` §6.3), which is a structured, human-readable summary object with its own output shape. No such summary object exists yet.
- **Core Agent Alignment Assessment != VFX Supervisor Agent's role-specific creative review.** The implemented `alignment_assessment` capability is Core Agent's own, general-purpose capability (`docs/AGENT_CONTRACTS.md` §4: "Alignment Assessment... Produces: structured Alignment Assessment"). It is a different, documented capability from the VFX Supervisor Agent's "creative-alignment Assessment" (`docs/AGENT_CONTRACTS.md` §5), which is specifically story/emotion/rhythm/visual-focus dimensioned and does not exist in code.
- **Execution Anchor backend != CG Supervisor Agent (historical distinction, now superseded by Step 4).** The Execution Anchor confirm/reject/draft backend (`intent/execution_anchor_service.py`) is fully implemented and is exercised by the **human** CG Supervisor; confirming/rejecting an Execution Anchor revision remains exclusively human, never an Agent, regardless of Step 4. A permission allowlist entry exists that *would* allow a `cg_supervisor_agent`-typed actor to create a draft, but no such actor is ever constructed by any production code path. Step 4 adds a genuinely separate, real CG Supervisor Agent capability — `execution_review` (`agents/cg_supervisor_review_service.py`) — that produces an advisory `CGSupervisorReview` for an existing revision; generation itself still requires a **human** CG Supervisor actor (`require_human_role`), and no production code path ever constructs an agent-kind actor for either drafting or review generation.
- **AgentType reservation != Agent implementation.** `vfx_supervisor_agent`, `cg_supervisor_agent`, and `artist_agent` are all valid values of the `AgentType` `Literal` (`apps/api/src/intent_core_api/workflow/actors.py`). A name existing in this type, in a document, in a code comment, in a test helper, or in a permission allowlist is **not** evidence that the corresponding Agent is implemented. The only valid evidence of implementation is: a real generator/service function, a real capability constant, a real prompt or deterministic behaviour, a reachable production code path that calls `build_agent_actor(...)` with that type, a persisted `AgentRun` row using that type, an API endpoint, and — where applicable — a UI surface.

---

## C. Current real Agent count

**The repository currently implements exactly four (4) real Agents: Core Agent, and three independent Role Agents, VFX Supervisor Agent, CG Supervisor Agent, and Artist Agent.**

Evidence:

- `build_agent_actor()` (`apps/api/src/intent_core_api/workflow/actors.py:91`) is the only function in the codebase capable of constructing an `agent`-kind `ActorContext`.
- It is called with the literal `"core_agent"` from `agents/core_agent_service.py`, and with the literal `"vfx_supervisor_agent"` from `agents/vfx_supervisor_review_service.py` (Step 3) — no other `AgentType` value is ever constructed by production code.
- The CG Supervisor Agent (Step 4) and the Artist Agent (Step 5) are both real, distinct exceptions to that pattern: their `execution_review`/`iteration_guidance` capabilities produce real `AgentRun` rows with `agent_type="cg_supervisor_agent"`/`agent_type="artist_agent"`, but generation is triggered exclusively by a **human** CG Supervisor or **human** Artist actor (`require_human_role`, `agents/cg_supervisor_review_service.py`/`agents/artist_guidance_service.py`) — no production code path constructs an agent-kind `cg_supervisor_agent` or `artist_agent` actor for either draft creation or generation; `build_agent_actor("cg_supervisor_agent", ...)`/`build_agent_actor("artist_agent", ...)` remain test-only, proving the permission system's tolerance, not a production invocation.
- Every `AgentRun` row this codebase can currently produce therefore has `agent_type="core_agent"`, `agent_type="vfx_supervisor_agent"`, `agent_type="cg_supervisor_agent"`, or `agent_type="artist_agent"`.

Per-Agent status:

| Agent | Status | Evidence |
|---|---|---|
| **Core Agent** | Implemented (5 of ~6 documented capabilities) | `agents/core_agent_service.py` (`core_anchor_drafting`), `agents/alignment_assessment_service.py` (`alignment_assessment`), `agents/intent_decomposition_service.py` (`intent_decomposition`, Step 1B — merged to `main`, commit `fc233bd`), `agents/context_reconstruction_service.py` (`context_reconstruction`, Step 1C — merged to `main`, commit `34a7ceb`), and `agents/cross_role_assessment_service.py` (`cross_role_assessment`, Step 6 — implemented and real-provider validated on `feat/step6-cross-role-alignment`, pending merge to `main`); all five use `ContextSnapshot`/`AgentRun`; deterministic provider for all five, real DeepSeek provider proven for Alignment Assessment, Intent Decomposition, Context Reconstruction, and Cross-role Assessment. `cross_role_assessment` is a fifth **capability** of the same Core Agent, not a fifth Agent — see section D's Step 6 row for the full capability description, contract, hardening history, and real-provider evidence. Step 1D's minimal persistent `HumanGate` domain object (not a capability — Core Agent may open a gate via a draft but never resolves one) is merged to `main` via PR #10, merge commit `b1f3747` (`c35c3dc` feat: add persistent core anchor human gate). All five capabilities' execution (snapshot/run/provider-dispatch/finalize) runs through the shared Agent Runtime (`agents/runtime.py`, `agents/model_gateway.py`, `agents/prompt_registry.py`, Step 2), merged to `main` via PR #11, merge commit `bd2f670` (`ac9e6c0` refactor: add lightweight shared agent runtime); this is a shared execution seam, not a capability or a new Agent. |
| **VFX Supervisor Agent** | Implemented (1 capability: `creative_review`), merged to `main` via PR #12, merge commit `d68569e` (`e986ba5` feat: add vfx supervisor agent) | `agents/vfx_supervisor_review_service.py`, migration `0019_vfx_supervisor_review.py`, Step 3. A real, independent Role Agent distinct from the Core Agent and from the human VFX Supervisor: it reads only local textual evidence for one Version (never image/video media) and produces an immutable, advisory `VFXSupervisorReview` — it cannot confirm/reject a Core Anchor or Version, cannot resolve a HumanGate, cannot create an authoritative `Decision`, cannot create an actual `ReviewNote`, and cannot write to ftrack. Runs through the same shared Agent Runtime as Core Agent (deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance — see section E). |
| **CG Supervisor Agent** | Implemented (1 capability: `execution_review`), merged to `main` via PR #13, merge commit `6aa3031` (`2b89ac2` feat: add cg supervisor agent and execution anchor gate) | `agents/cg_supervisor_review_service.py`, migration `0020_cg_supervisor_review_and_execution_gate.py`, Step 4. A real, independent Role Agent distinct from the Core Agent, the VFX Supervisor Agent, and the human CG Supervisor: it reads only local textual evidence for one `ExecutionAnchorRevision` (never image/video/scene/DCC-file media) and produces an immutable, advisory `CGSupervisorReview` — it cannot confirm/reject an Execution Anchor revision, cannot resolve a HumanGate, cannot create an authoritative `Decision`, and cannot write to ftrack; a code-level content-boundary validator (not just prompt guidance) rejects any output that would recommend modifying/superseding/re-anchoring the Core Anchor, resolving a HumanGate, or issuing a Decision before it can be persisted. Runs through the same shared Agent Runtime as Core Agent and the VFX Supervisor Agent (deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance — see section E). See section H for the exact Execution Anchor Human Gate UI that now accompanies this Agent. |
| **Artist Agent** | Implemented (1 capability: `iteration_guidance`), merged to `main` via PR #14, merge commit `93fb730` (`81a79be` feat: add artist agent iteration guidance) | `agents/artist_guidance_service.py`, migration `0021_artist_agent_guidance.py`, Step 5. A real, independent Role Agent distinct from the Core Agent, the VFX Supervisor Agent, the CG Supervisor Agent, and the human Artist: it reads only local textual evidence for one Version, scoped to an explicit, caller-supplied Task (never image/video/scene/DCC-file media, never a heuristically-inferred Task) and produces an immutable, advisory `ArtistAgentGuidance` — it cannot establish/modify/confirm/reject/replace/supersede either Anchor, cannot resolve a HumanGate, cannot create an authoritative `Decision`, cannot create an actual `ReviewNote`, and cannot write to ftrack; a code-level content-boundary validator (not just prompt guidance) rejects any output that would add content to, or otherwise modify, either Anchor — including indirect/field-name-based instructions — before it can be persisted. Runs through the same shared Agent Runtime as Core Agent, the VFX Supervisor Agent, and the CG Supervisor Agent (deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance — see section E). |

---

## D. Completed implementation inventory

Commit hashes below are taken directly from `git log --oneline` on branch `chore/step0-baseline-roadmap` at the time this document was written. They are exact, not assumed.

| Slice | Commit | Correct architectural owner/category | What it proves | What it does not prove |
|---|---|---|---|---|
| A1 Core Anchor workflow | `1553a43` feat: implement A1 primary intent workflow | Human authority / shared domain (Intent module) | A full Core Anchor (`CoreAnchor`/`CoreAnchorRevision`) draft→confirm/reject→supersede lifecycle exists, gated to the human VFX Supervisor | Does not prove Core Agent or VFX Supervisor Agent exists — A1's draft creation is human-authored at this stage |
| A2 Execution Anchor workflow | `f2eac72` feat: implement A2 execution anchor workflow | Human authority / shared domain (Intent module) | A full Execution Anchor lifecycle exists, gated to the human CG Supervisor, including the stale-propagation cascade from Core Anchor confirmation | Does not prove CG Supervisor Agent exists; does not prove a frontend Human Gate UI exists for this workflow (it does not, see section G) |
| ftrack sync/write-back infrastructure | `2b27fca` feat: implement ftrack sync and writeback workflow | Workflow Connector (ftrack integration) | Real Project/Shot/Task sync and controlled, human-requested Core Anchor write-back both work end-to-end | At the time this row was recorded, did not prove Version/ReviewNote sync from ftrack; that capability was implemented and real-validated in Step 8 (see Section K) — this row is left describing the state as of `2b27fca` and does not itself cover Step 8's later work. Does not prove any Agent capability |
| D0 contracts pipeline | `daf05fb` feat: generate TypeScript contracts from OpenAPI | Shared runtime / infrastructure | OpenAPI → generated TypeScript contracts pipeline works and is re-run at every subsequent slice | Does not prove any product capability by itself |
| UTC timestamp fix | `91070b3` fix: align timestamp columns with UTC | Shared runtime / infrastructure | A real Postgres timezone-awareness defect was found and fixed | Not an Agent- or Human-Gate-related change |
| D1 Shot Anchor page | `ad5dff4` feat: implement D1 Shot Anchor page | UI / presentation layer | The first real frontend page exists, proving apps/web ↔ apps/api ↔ Postgres works end-to-end | Does not prove any Agent or Human Gate exists yet at this commit — those were added by later slices on top of this page |
| B1 Core Agent drafting | `2756322` feat: implement B1 Core Agent draft generation | Core Agent capability | The first real Agent capability (`core_anchor_drafting`), with a deterministic provider, `ContextSnapshot`, and `AgentRun`, attributed to `agent_type="core_agent"` | Does not prove any Role Agent exists; does not prove a real (non-deterministic) model provider for this specific capability — it still has none |
| A3 Human Gate UI | `320af92` feat: implement A3 Human Gate UI | Human authority / Human Gate (UI) | A real Core Anchor confirm/reject UI (`CoreAnchorGate`) exists, gated in the frontend to the VFX Supervisor role selection and enforced authoritatively by the backend | Does not prove VFX Supervisor Agent exists — this is a human interaction surface, not an Agent |
| ContextSnapshot + AgentRun | `65ad62d` feat: add context snapshots and agent runs | Shared Agent Runtime foundation | Immutable input/output provenance now exists for Core Agent runs | Does not itself constitute "Context Reconstruction" (see section B) and does not prove any additional Agent exists |
| Version + ReviewNote | `c6c7d6e` feat: add versions and review notes | Shared domain object | Manual-creation-only `Version`/`ReviewNote` objects exist with full lineage fields | Does not prove ftrack sync for these objects (not implemented) |
| Alignment Assessment | `05ec741` feat: add alignment assessment capability | Core Agent capability | A second real Core Agent capability, with a deterministic provider **and a real DeepSeek provider**, correctly attributed to `agent_type="core_agent"` per `docs/AGENT_CONTRACTS.md` §4 | Does not prove VFX Supervisor Agent's own, distinct "creative-alignment Assessment" capability exists — it does not |
| Alignment Assessment Decisions | `8917efb` feat: add alignment assessment decisions | Human authority / Human Gate | Human VFX Supervisor accept/reject for Alignment Assessment, plus real Decision supersession (`Decision.supersedes_decision_id`) is now wired and tested | Does not prove any Agent resolves this gate — `decision_service.record_decision` hard-rejects any non-human actor |
| Version / Assessment UI | `babb916` feat: add alignment assessment review UI | UI / presentation layer | Shot page Versions list, Version detail page, Assessment display, generation button, Human Gate UI, and supersession labelling all exist and are tested | Does not prove any Role Agent, Dashboard, or role-aware view exists — this is a single generic page, not the multi-role Dashboard described in `docs/PRODUCT_SCOPE.md` §13 |
| Step 1A-Backend Core Anchor semantic objects | `173f32e` feat: add core anchor semantic objects | Human authority / shared domain (Intent module) | Five ordered, revision-owned semantic-child tables (`Constraint`, `VariationZone`, `DriftRisk`, `AnchorReference`, `OpenQuestion`; migration `0014_core_anchor_semantic_objects.py`) exist, each belonging to exactly one `CoreAnchorRevision`, writable only while that revision is `draft`, immutable once confirmed/rejected/superseded, returned as empty arrays when absent, with real-HTTP-validated PATCH replace/omit/clear semantics and meaningful (not count-only) before/after `AuditEvent` content | Does not prove Step 1A-UI exists on its own (see next row); does not prove Core Agent Intent Decomposition, Core Agent Context Reconstruction, or a persistent `HumanGate` object exist (Step 1B/1C/1D, all still not started); does not prove any Role Agent exists |
| Step 1A-UI Core Anchor semantic editors | Merged to `main` via PR #7, merge commit `2d1101f` (`4e544dd` feat: add core anchor semantic editors; `cc18fc4` style: format intent module readme) | UI / presentation layer | The existing Shot Anchor page's Core Anchor Human Gate (`CoreAnchorGate`) now lets the Human VFX Supervisor view/add/edit/remove/move up/move down/save/cancel all five semantic collections (Must preserve/Allowed variation/High-risk drift points/References/Open questions) on a draft revision, inside the existing draft workflow — no new route, no new endpoint; preserves the backend's dirty-collection PATCH contract (omitted = unchanged, empty = clear, populated = replace) and uses the server-returned revision as the new frontend source of truth; a successful save shows an accessible "Changes saved." status, Save shows "Saving…" and is disabled while pending, a failed save preserves unsaved content, and Cancel restores the last server-known state; Human CG Supervisor and Human Artist see a read-only "Draft details" view (all scalar fields and all five semantic collections, no edit controls) instead of a disabled form; confirmed revisions render all five collections read-only | Does not prove Core Agent Intent Decomposition, Core Agent Context Reconstruction, or a persistent `HumanGate` object exist beyond what is separately recorded below; does not prove any Role Agent exists |
| Step 1B Core Agent Intent Decomposition | Merged to `main` via PR #8, merge commit `cd24eaf` (`fc233bd` feat: add core agent intent decomposition) | Core Agent capability + Human authority workflow | A third real Core Agent capability (`intent_decomposition`, migration `0015_intent_decomposition.py`) structures an `IntentBrief` into an immutable `IntentDecomposition` (core intent summary, anchor-relevant content, exactly seven dimensions each with summary+rationale, candidate constraints, candidate variation zones, contextual information, uncertainties) with its own `ContextSnapshot`/`AgentRun`, deterministic provider for automated tests and a real DeepSeek provider for manual validation; generating a decomposition never creates or modifies a Core Anchor; a new, explicitly human-triggered action (`create_core_anchor_draft_from_decomposition`, Human VFX Supervisor only) maps a chosen decomposition's fields into a fresh, fully editable `CoreAnchorRevision` draft, stamping `source_intent_decomposition_id` for lineage, without a second model call and without creating a `Decision`; the existing direct Core Anchor generation path remains backend-compatible but is demoted from the primary UI position | Does not prove Core Agent Context Reconstruction or a persistent `HumanGate` object exist beyond what is separately recorded below; does not prove any Role Agent exists; does not prove Step 1 as a whole is complete |
| Step 1C Core Agent Context Reconstruction | Merged to `main` via PR #9, merge commit `e306087` (`34a7ceb` feat: add core agent context reconstruction) | Core Agent capability | A fourth real Core Agent capability (`context_reconstruction`, migration `0016_context_reconstruction.py`) produces an immutable, model-generated interpretation of the exact local facts recorded in a fresh `ContextSnapshot` (project/shot identity, current IntentBrief, all IntentDecompositions, current draft and/or confirmed Core Anchor revision with its semantic children and decomposition lineage, Execution Anchor state, relevant recorded human Decisions, Version/ReviewNote metadata) — answering "why are we doing it this way?" without ever judging Version alignment/drift, role performance, or recommending a re-anchor; every structured conclusion (`original_intent`, `current_creative_direction`, `execution_context`, and each `key_decisions`/`active_constraints`/`allowed_variations`/`unresolved_questions` entry) cites one or more evidence references back to real ids in that same snapshot; deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance | Does not prove a persistent `HumanGate` object exists beyond what is separately recorded below; does not prove any Role Agent exists; does not prove Step 1 as a whole is complete |
| Step 1D Minimal Persistent HumanGate | Merged to `main` via PR #10, merge commit `b1f3747` (`c35c3dc` feat: add persistent core anchor human gate) | Human authority / shared domain (Intent module) | A first-class, minimal persistent `human_gates` table (migration `0017_persistent_human_gate.py`) records that an authoritative human review is required for one `CoreAnchorRevision` draft and how it was resolved; every current draft-creation path (`create_draft_revision`, the single choke point all of direct Core Agent generation, decomposition-to-draft, and manual draft creation already funnel through) opens exactly one pending gate in the same transaction as the draft; the existing Core Anchor confirm/reject actions remain the sole authoritative write path — each now loads or legacy-compatibility-creates the gate, runs the existing lifecycle, and atomically resolves the gate (status, resolved actor/role/actor-type/timestamp/rationale, linked `Decision` id) in the same commit as the existing `Decision`; `Decision` remains the sole authoritative human record, HumanGate only links to it; a pre-Step-1D revision with no gate row remains readable, returns 404 from the new `GET .../human-gate` endpoint, and gets its missing gate created-and-resolved atomically the first time it is confirmed or rejected | Does not prove any Role Agent exists; does not prove a shared Agent Runtime (Step 2) exists |
| Step 2 Lightweight Shared Agent Runtime | Merged to `main` via PR #11, merge commit `bd2f670` (`ac9e6c0` refactor: add lightweight shared agent runtime) | Shared runtime / infrastructure (not an Agent, not a Role Agent) | A small shared execution seam (`agents/runtime.py`: `AgentExecutionSpec`/`execute_agent`; `agents/model_gateway.py`: shared OpenAI-compatible DeepSeek call path, settings-driven provider/base URL/model/key, sanitised errors; `agents/prompt_registry.py`: static registry of `core_anchor_drafting.v1`/`intent_decomposition.v1`/`context_reconstruction.v1`/`alignment_assessment.v1`) now owns the ContextSnapshot-persist → AgentRun(running) → provider-dispatch → structured-output-validate → domain-result-persist → AgentRun(succeeded/failed) lifecycle previously duplicated independently in each of the four existing Core Agent capabilities (`core_anchor_drafting` including its decomposition-to-draft deterministic transform, `intent_decomposition`, `context_reconstruction`, `alignment_assessment`), all migrated onto it with no behavioural change; migration `0018_agent_runtime_metadata.py` adds nullable `model_name`/`prompt_version` to `AgentRun` (null for deterministic runs, populated only for an actually-executed DeepSeek run); a real DeepSeek Context Reconstruction call was made through the shared runtime against the existing Step 1D acceptance Shot (`310b242b-106c-4d5c-ae61-d24f8972f375`), producing `AgentRun` `e370f7cd-dd14-4ebb-b53b-0402d9107e47` (`provider=deepseek`, `model_name=deepseek-v4-flash`, `prompt_version=context_reconstruction.v1`, `status=succeeded`), `ContextSnapshot` `42640ca9-4b80-4c8d-ba74-c4be5e9a8c91`, and `ContextReconstruction` `97b4cce5-1c59-4c49-862a-a407dfb1dfd3`, with the confirmed Core Anchor, resolved HumanGate, and linked Decision left unchanged | Does not add a generic public "execute agent" API — the four existing capability endpoints remain the only user-facing surface; does not prove any Role Agent exists (see the Step 3 row below) |
| Step 3 VFX Supervisor Agent | Merged to `main` via PR #12, merge commit `d68569e` (`e986ba5` feat: add vfx supervisor agent) | Role Agent capability (first of three planned; independent from Core Agent) | The first real, independent Role Agent (`agent_type=vfx_supervisor_agent`, `capability=creative_review`; migration `0019_vfx_supervisor_review.py`) produces an immutable `VFXSupervisorReview` for one Version, using only local textual evidence (Project/Shot/Version metadata, IntentBrief, IntentDecompositions, confirmed Core Anchor with semantic children, newest Context Reconstruction, newest Alignment Assessment, existing ReviewNotes, relevant Decisions, Task/Execution Anchor context, and HumanGate resolution facts where available) — it never performs image/video analysis and must honestly record missing media in `evidence_gaps`; every evidence reference is validated to resolve to an id present in that same run's `ContextSnapshot`, with invented ids rejected before persistence; it runs through the same shared Agent Runtime as Core Agent (Step 2), registered in the Prompt Registry as `vfx_supervisor_creative_review.v1`; generation is Human-VFX-Supervisor-only (CG Supervisor and Artist both receive 403; agent actors are forbidden), all three human roles may read; a real DeepSeek call was made against the existing Step 1D/2 acceptance Shot (`310b242b-106c-4d5c-ae61-d24f8972f375`) with a dedicated acceptance Version (`D1_STEP3_VFX_REVIEW_001`, `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`), producing `AgentRun` `b1e5d753-d008-4376-85e4-81ffcbdd6251` (`provider=deepseek`, `model_name=deepseek-v4-flash`, `prompt_version=vfx_supervisor_creative_review.v1`, `status=succeeded`), `ContextSnapshot` `2ca7281e-fd23-42e1-b3da-0e722af9422b`, and `VFXSupervisorReview` `d94af31c-f59e-40ee-883f-1ee30ba7074e`, with the confirmed Core Anchor, resolved HumanGate, and linked Decision left unchanged | Does not prove CG Supervisor Agent or Artist Agent exists; does not add a generic public "execute agent" API — the three new capability endpoints (generate/get/list) remain the only user-facing surface for this capability |
| Step 4 CG Supervisor Agent + Execution Anchor CG Human Gate UI | Merged to `main` via PR #13, merge commit `6aa3031` (`2b89ac2` feat: add cg supervisor agent and execution anchor gate) | Role Agent capability (second of three planned; independent from Core Agent and VFX Supervisor Agent) + UI / Human Gate | The second real, independent Role Agent (`agent_type=cg_supervisor_agent`, `capability=execution_review`; migration `0020_cg_supervisor_review_and_execution_gate.py`) produces an immutable `CGSupervisorReview` for one `ExecutionAnchorRevision`, using only local textual evidence — it never performs image/video/scene/DCC-file inspection and must explicitly disclose that limitation; every evidence reference resolves to an id present in that run's own `ContextSnapshot`; the output contract carries hard `Field` length/count bounds (not just prompt guidance) and a pre-persistence content-boundary validator rejects any output recommending Core Anchor modification/re-anchoring, HumanGate resolution, or Decision creation. It runs through the same shared Agent Runtime as Core Agent and the VFX Supervisor Agent, registered in the Prompt Registry as `cg_supervisor_execution_review.v1` with a capability-specific `max_output_tokens=8192`. Generation is Human-CG-Supervisor-only, all three human roles may read. Migration `0020` also extends `HumanGate` with a nullable dual-target (`core_anchor_revision_id` / `execution_anchor_revision_id`) FK pair under a DB CHECK XOR constraint, and the frontend gained a CG Human Gate UI (confirm/reject an Execution Anchor revision, mirroring `CoreAnchorGate`). Two early real-provider calls truncated before valid JSON (`AgentRun` `12ab6d3a-d1aa-40ec-aa1f-d86de7a904da` at a 4096-token budget, then `abbc3720-15c0-4fe7-bacb-3b3d777be4d1` even earlier at 8192 tokens); the root cause was addressed with a three-part fix (compact `ContextSnapshot` input, hard output-contract bounds, safe non-leaking diagnostics), after which a real call succeeded structurally but exposed two content-boundary gaps (`CGSupervisorReview` `f32cfd8d-4e32-4775-99c7-acd658d06412`, retained as evidence), which content-boundary hardening then closed; a second, fully compliant real call followed (`CGSupervisorReview` `6877fa03-fab2-4e70-92a2-d0ca185d20e2`, `AgentRun` `f716175c-7ebc-473d-a5f3-3ec1d640b931`, `ContextSnapshot` `6f10121c-2b65-420e-89c4-07aa4a9b9795`). The project owner then confirmed the resulting Execution Anchor revision (`1561d08d-64cc-4550-b25d-27210ebbe2b5`) in a real browser session, resolving HumanGate `b6a204b0-fb01-40b8-932f-92063751a2a6` and creating Decision `6c71d097-6f7a-4171-83be-39c24df46db2`. See sections E, H, and I below and `docs/VALIDATION_EVIDENCE.md` for the full automated, real-DeepSeek, and browser evidence | Does not prove Artist Agent exists; does not add a generic public "execute agent" API — the review capability's own endpoints (generate/get/list) and the Execution Anchor confirm/reject endpoints remain the only user-facing surface |
| Step 5 Artist Agent | Merged to `main` via PR #14, merge commit `93fb730` (`81a79be` feat: add artist agent iteration guidance) | Role Agent capability (third of three planned; independent from Core Agent, VFX Supervisor Agent, and CG Supervisor Agent) | The third real, independent Role Agent (`agent_type=artist_agent`, `capability=iteration_guidance`; migration `0021_artist_agent_guidance.py`) produces an immutable `ArtistAgentGuidance` for one Version, translating upstream creative/technical intent and existing supervisor feedback into practical, Artist-facing terms — `executive_summary`, `creative_intent_read`, `task_goal`, `current_iteration_read`, `non_negotiables`, `allowed_variations`, `feedback_translations` (each with `feedback_or_issue`/`practical_action`/`underlying_intent`/`self_check`/priority/evidence), `iteration_priorities`, `cross_department_dependencies`, `questions_for_human_supervisor`, and `evidence_gaps`. Because `Version` has no `task_id` of its own (a Shot may have several Tasks and several Versions with no join between them), generation requires an explicit `task_id` in the request body — the Human Artist selects the Task context; no heuristic latest-Task inference exists anywhere in the code path; the supplied Task and target Version must share a Shot, and the supplied Task's Execution Anchor must already be `confirmed` (a missing, draft, or rejected Execution Anchor is rejected with a 409 before any `ContextSnapshot` or `AgentRun` is created). It runs through the same shared Agent Runtime as the other three Agents, registered in the Prompt Registry as `artist_iteration_guidance.v1` with a capability-specific `max_output_tokens=6144`. Every list/string field on the output contract carries a hard `Field` bound (`executive_summary`≤650 chars; `non_negotiables`/`allowed_variations`/`feedback_translations`/`iteration_priorities`/`questions_for_human_supervisor`≤3 items; `cross_department_dependencies`≤2 items; `evidence_gaps`≤5 items; each evidence list 1–2 references). A pre-persistence content-boundary validator rejects, before any `ArtistAgentGuidance` row is created: a missing explicit inspection-unavailable + media/scene/parameter disclosure in `evidence_gaps`; and any instruction — direct or indirect (add/include/record/document/encode/formalise/capture/populate content in an Anchor, or naming a known Anchor field such as `allowed_refinements` even without the word "Anchor") — to modify either Anchor, re-anchor, resolve a HumanGate, issue a Decision, or officially judge/rank a Version; citing an Anchor as evidence, describing it as incomplete/ambiguous, and asking the Human Artist to seek clarification from the Human CG Supervisor all remain explicitly allowed. A first real DeepSeek call (`AgentRun` `2cec22c3-9b07-4f12-bb04-ceddd0677747`, `ContextSnapshot` `fe3ebaad-184f-4a33-97b1-f9c54b4be3d4`) was correctly rejected pre-persistence for advising an Anchor edit; this motivated content-boundary hardening (explicit prompt MAY/NEVER structure, broadened indirect-modification/field-name detection, a deterministic missing-guidance → CG-clarification-question behaviour), after which a second, fully compliant real call succeeded (`ArtistAgentGuidance` `2fbd9c14-f47d-474b-b01d-0bb452c5f124`, `AgentRun` `70622310-d931-418a-b976-a0cea8661aec`, `ContextSnapshot` `3982b63d-14b1-4386-8bb7-7f1b914fec76`). The project owner then verified the full result in a real browser session. See sections E, H, and I below and `docs/VALIDATION_EVIDENCE.md` for the full automated, real-DeepSeek, and browser evidence | Does not add a generic public "execute agent" API — the guidance capability's own endpoints (generate/get/list) remain the only user-facing surface; does not prove `Version` gained a real `task_id` foreign key — the explicit-context design deliberately does not claim one |
| Step 6 Cross-role Assessment + Re-anchor Proposal + Intent Signal | Implemented and real-DeepSeek-validated on `feat/step6-cross-role-alignment`, pending commit/push/PR/merge to `main` | Core Agent capability (fifth of Core Agent's own documented capabilities; not a fifth Agent) | A new Core Agent capability (`agent_type=core_agent`, `capability=cross_role_assessment`; migration `0022_cross_role_alignment.py`) synthesises the confirmed Core Anchor, the confirmed Execution Anchor, the newest VFX Supervisor Agent review, the newest CG Supervisor Agent review, and the newest Artist Agent guidance for one explicit Task/Version pair into an immutable `CrossRoleAssessment` — `executive_summary`, `shared_intent_read`, exactly three `role_perspectives` in fixed order (`vfx_supervisor`, `cg_supervisor`, `artist`), `agreements`, `cross_role_tensions`, `local_optimum_risks`, `unresolved_dependencies`, `human_coordination_priorities`, an optional advisory `re_anchor_proposal`, and `evidence_gaps` — helping humans see where the three roles agree, where they disagree, where one department may be locally optimising at the expense of shared creative intent, what remains unresolved, and how urgently human review is warranted. Reusing the Step 5 explicit-context decision, generation requires an explicit `task_id` in the request body (no heuristic latest-Task inference); the supplied Task and target Version must share a Shot; a confirmed `CoreAnchorRevision`, a confirmed `ExecutionAnchorRevision` for that Task, a `VFXSupervisorReview`, a `CGSupervisorReview`, and an `ArtistAgentGuidance` must all already exist, each rejected with a 409 before any `ContextSnapshot`/`AgentRun` is created if missing. A deterministic, code-derived `IntentSignal` (`low`/`medium`/`high` `attention_level`, explainable `drivers` each pointing to a real `assessment_section`/`assessment_item_index`) is derived from the persisted assessment by a pure function (`derive_intent_signal`) — it is not itself an Agent and creates no separate `AgentRun`. The optional `ReAnchorProposal` is bounded advisory evidence only — persisted only when its evidence spans at least two of the three role-output categories, cites the current confirmed Core Anchor revision, and is supported by a cross-role tension or local-optimum risk in the same response; it has no approval state, no HumanGate/Decision linkage, and no apply/materialise endpoint. It runs through the same shared Agent Runtime as the other four capabilities, registered in the Prompt Registry as `core_cross_role_assessment.v1` with a capability-specific `max_output_tokens=8192` (raised from an initial `7168` — see the real-provider hardening history below). A pre-persistence, code-level content-boundary validator rejects any output that would declare an official pass/fail, a definitive aligned/misaligned or drift verdict, role blame, a Version ranking, an Anchor-modification instruction outside `re_anchor_proposal`, a HumanGate-resolution or Decision-authority instruction, a claim of direct media inspection, or — added during this Step's own hardening — an unsupported production-specific numeric value (a percentage, multiplier/ratio, frame count, fps, duration, exposure stop/EV, or spatial measurement not present, value-and-unit-exact, in evidence the same output item cites). Generation is Human-VFX-Supervisor-only (CG Supervisor and Artist receive 403, agent actors forbidden); all three human roles may read; multiple immutable assessments may exist per Version/Task context, listed newest first; no PATCH/DELETE/apply/approve/generic-execute endpoint. Real-provider acceptance required four real DeepSeek calls across three rounds of hardening: (1) an opaque Pydantic-validation failure, motivating safe `validation_stage`/field-path-and-error-type diagnostics (`AgentRun` `8eda2584-73a8-448e-bdb4-05fc6c31bb1f`, `ContextSnapshot` `de3acea9-869b-44cd-aaf4-5e8c16d0cdd8`); (2) two `source_type` `literal_error`s the new diagnostics correctly pinpointed, motivating a contract-Literal-derived source_type catalogue placed beside every evidence shape in the prompt (`AgentRun` `88f92372-5a64-4704-9482-2e60d439f5e8`, `ContextSnapshot` `d0fc132a-3325-4357-913f-9f35fedc397c`); (3) genuine output-token truncation at the original `7168` budget, fixed by a capability-specific-only rise to `8192` (`AgentRun` `95235b28-5905-4630-8cde-27e6d6ae36fc`, `ContextSnapshot` `15f91a2d-bed0-42f9-9bfd-ccc31c4d1e98`); (4) a technically successful result whose advisory `re_anchor_proposal` invented two unsupported production thresholds ("push-in speed must not exceed 0.5x normal conversation pace", "contrast may increase up to 15% above baseline") not present in any cited evidence — `CrossRoleAssessment` `5a6d16d5-2c85-4f22-8057-cbebc77ecc66`, `ReAnchorProposal` `fe177507-a3f6-43fe-b705-5ac9c7c3ad93`, `IntentSignal` `2bcae5a5-76e7-41e5-978f-e9bc8294adbf`, `AgentRun` `ee7b9225-af37-4c59-ae8f-e120fb26917e`, `ContextSnapshot` `81479ee2-5279-4473-8d56-0d60f17c9740`, retained unmodified as historical evidence, not deleted, rewritten, or corrected — which motivated the unsupported-production-numeric-value hardening (a value may be repeated only when the exact value and unit are present in evidence the same output item cites; otherwise the output must ask a Human Supervisor to establish it, never invent it). A final compliant real DeepSeek call then passed every validator, including the new numeric-evidence check: `CrossRoleAssessment` `9ef47a2e-f51e-472f-a455-03b49c8d139b`, `ReAnchorProposal` `757682c6-b23a-42bf-a2a5-cb95314deb27`, `IntentSignal` `f412927c-de95-4eda-9b2c-965025355539`, `AgentRun` `8f11ceaf-ab54-4c11-8ca5-fcaeaf0e4a29`, `ContextSnapshot` `9da4f5f7-04e9-47a8-a737-f7735d4e3130`. The existing Shot page gained a "Cross-role assessment — Core Agent" section (explicit Task/Version selectors, Human-VFX-Supervisor-only Generate, full structured rendering including the Intent Signal and optional Re-anchor Proposal, no apply/accept/reject/materialise/confirm/create-draft control anywhere) plus a history-clarity fix distinguishing the newest ("Latest assessment", fully expanded) from older immutable records (collapsed by default under "Previous assessments (N)", each individually expandable, with an explicit "these are immutable historical outputs... not the current assessment" disclaimer) — no new route, Dashboard, or dependency. See sections E, H, and I below and `docs/VALIDATION_EVIDENCE.md` for the full automated, real-DeepSeek, and browser evidence | Does not prove Step 6 has been merged to `main`; does not add a generic public "execute agent" API — the capability's own endpoints (generate/get/list, plus dedicated get endpoints for the proposal and signal) remain the only user-facing surface; does not prove a fifth Agent exists — `cross_role_assessment` is a Core Agent capability, exactly as `core_anchor_drafting`/`intent_decomposition`/`context_reconstruction`/`alignment_assessment` already are |

---

## E. Manual validation evidence supplied by the project owner

The following validations were reported by the project owner as having been executed manually, outside the automated test suite. They are recorded here because they are relevant project history, but **they are not independently provable from Git history or repository state alone** — no artifact of their execution (e.g., a logged transcript, a screenshot, an exported response body) is committed to this repository. They are recorded as owner-supplied claims, not as repository-verified facts. See `docs/VALIDATION_EVIDENCE.md` for the structured evidence table and explicit evidence-type labelling.

1. **Real ftrack sync** — real Project/Shot/Task data was synchronised from a real ftrack workspace; Shot `bc0040` appeared in ICAS with `source=ftrack`; the reconciliation worker job (`reconcile_ftrack_shots`) completed successfully.
2. **Real ftrack controlled write-back** — a confirmed Core Anchor revision produced exactly one ftrack Shot Note on target Shot `bc0040`; no existing Note was overwritten; the write-back was triggered by a human VFX Supervisor's Decision/request (`request_write_back=True` on confirm), not automatically.
3. **Real DeepSeek Alignment Assessment** — a real (non-deterministic) call was made with `provider=deepseek`; the resulting `AgentRun.status=succeeded`; the produced `AlignmentAssessment.alignment_state=significant_drift`; `envelope.requires_human_gate=true`; the evidence field referenced the confirmed Core Anchor, the Version description, and a Review Note; no Anchor or ftrack state was modified by the call.
4. **Decision supersession** — a later human Decision was recorded that superseded a prior active Decision (`supersedes_decision_id` populated correctly); the old Decision remained unmodified and independently readable via `GET /assessments/{id}/decisions`.
5. **Browser validation** — the Version page correctly rendered Version fields, Review Notes, the confirmed Core Anchor summary, Alignment Assessments, AI provenance (agent type/provider/run status/context snapshot), the Human Gate, and the supersession labels ("Superseded by a later decision" / "Supersedes decision `<id>`").
6. **Step 1A-Backend Core Anchor semantic objects — real HTTP API acceptance.** Unlike items 1–5 above, this validation is *self-supplied within this repository's own tooling*, not an external-system claim — but it still used a real running process and real HTTP requests, not `TestClient`, so it is recorded with the same discipline. A real `uvicorn` process was started against an isolated, temporary SQLite database (never `apps/api/dev.db`, never the developer's PostgreSQL database), with `MODEL_PROVIDER=deterministic` and no ftrack credentials loaded. Against that process: a synthetic Project/Shot/IntentBrief/Core Anchor draft was created; all five semantic collections (`constraints`/`variation_zones`/`drift_risks`/`references`/`open_questions`) were created with server-generated ids/`order_index`/`created_at`; a PATCH correctly distinguished an omitted collection (unchanged), an explicit empty list (cleared), and a populated list (fully replaced); the resulting `AuditEvent` recorded normalized before/after content, not counts; a blank required value returned 422; Human CG Supervisor and Human Artist both received 403; confirming the revision and then attempting to PATCH it again returned 409 with all semantic content unchanged; a second revision created afterward returned five empty collections and shared no child row with the first; editing semantic content created no `AgentRun`/`ContextSnapshot`/`ExecutionAnchor`/`ExecutionAnchorRevision`/`WritebackRecord`; confirming with `request_write_back=false` created exactly one human `Decision` and no `WritebackRecord`. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the exact scenario list.
7. **Step 1A-UI Core Anchor semantic editors — browser acceptance.** The project owner manually verified, in a real browser session against the Step 1A-UI implementation on `feat/step1a-core-anchor-semantics`: all five semantic editors rendered under the current Core Anchor draft; the Human VFX Supervisor could add and edit sentence-length content, and the add/remove/reorder controls worked; the Reference editor's label, optional URI, and optional note all rendered and saved correctly; Open Question reordering was preserved; blank required content was blocked by client-side validation; Save persisted the data and displayed "Changes saved."; making a further edit cleared that prior success status; Cancel restored the last server-saved values; the Human CG Supervisor saw a read-only "Draft details" view with actor id `cg-1`, and the Human Artist saw the corresponding read-only view with actor id `artist-1` — neither received any semantic or scalar editing control; a manually-entered custom Actor id was preserved across role changes within the same page session, and a full browser refresh restored the current role's default Actor id; confirming the draft preserved all semantic collections and rendered them read-only; no ftrack write-back was requested; and browser use did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the full 17-item scenario list. As with item 6, this is not independently provable from Git history alone.
8. **Step 1B — Real DeepSeek Intent Decomposition acceptance.** A real (non-deterministic) `provider=deepseek`, `model=deepseek-v4-flash` call was made against a dedicated local acceptance Shot (`D1_STEP1B_ACCEPTANCE_001`), exactly one model call; `AgentRun.status=succeeded`; all seven dimensions had non-blank summary and rationale; candidate constraints, candidate variation zones, and contextual information were all populated; uncertainties was an explicit empty list; the `ContextSnapshot` payload contained exactly `project`/`shot`/`intent_brief`/`tasks`; no Core Anchor or `CoreAnchorRevision` was created during generation; no `Decision`, `WritebackRecord`, Role Agent run, or ftrack network call occurred. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry, the local evidence ids, and the security note covering the local `.env` handling during this acceptance run.
9. **Step 1B — Decomposition-to-Core-Anchor browser acceptance.** The project owner manually verified, in a real browser session: the real DeepSeek decomposition appeared after Intent Brief and before Core Anchor, labelled "AI proposal — Core Agent"; provider, succeeded run status, and `ContextSnapshot` provenance were visible; all seven dimensions and rationales rendered; five candidate Must-preserve items and three candidate Allowed-variation items rendered; contextual information rendered; uncertainties displayed an explicit empty state; the Human VFX Supervisor explicitly used the decomposition, creating a new editable `CoreAnchorRevision` draft with visible source-decomposition lineage; scalar fields mapped from the selected decomposition; exactly five Constraints and three VariationZones were created, with no DriftRisk, AnchorReference, or OpenQuestion invented; the generated draft content remained editable, with working save feedback and persistence; the draft remained unconfirmed, with no automatic Decision or ftrack write-back; the existing-draft conflict check prevented a silent duplicate overwrite; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the full 20-item scenario list. As with items 6 and 7, this is not independently provable from Git history alone.
10. **Step 1C — Real DeepSeek Context Reconstruction acceptance.** A real (non-deterministic) `provider=deepseek`, `model=deepseek-v4-flash` call was made against the same dedicated local acceptance Shot used for item 8 (`1e433dca-5a9e-46c1-9f6d-25938af49efd`, `D1_STEP1B_ACCEPTANCE_001`), exactly one model call; `AgentRun.status=succeeded`; all required output sections (`context_summary`, `original_intent`, `current_creative_direction`, `execution_context`, `key_decisions`, `active_constraints`, `allowed_variations`, `unresolved_questions`, `context_gaps`) were present; 11 structured items were validated with non-blank summary/rationale; 18 evidence references all resolved to real ids in the saved `ContextSnapshot`; the output correctly described the Core Anchor as an unconfirmed draft, not a confirmed direction; the output made no unsupported alignment, drift, pass/fail, role-judgment, or re-anchor claim; missing facts (no Execution Anchor, no Decision, no Version) were represented honestly through `execution_context` and an explicit `context_gaps` list, not omitted or invented; no `IntentBrief`/`IntentDecomposition`/`CoreAnchor`/`CoreAnchorRevision`/semantic-child/`ExecutionAnchor`/`Decision`/`AlignmentAssessment`/`Version`/`ReviewNote`/`WritebackRecord` was created, modified, confirmed, rejected, or deleted; only one `ContextSnapshot`, one Core Agent `AgentRun`, and one `ContextReconstruction` were created; no Role Agent run and no ftrack network call occurred. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the local evidence ids.
11. **Step 1C — Context Reconstruction browser acceptance.** The project owner manually verified, in a real browser session: the Context reconstruction section appeared after Core Anchor and before Execution Anchors; the output was labelled "AI reconstruction — Core Agent"; provider, succeeded run status, and `ContextSnapshot` provenance were visible; the context summary rendered; original intent rendered with rationale and evidence; current creative direction correctly described the Core Anchor as an unconfirmed draft; execution context correctly stated that no Execution Anchors existed; key decisions displayed an explicit empty state; five active constraints and three allowed variations rendered; unresolved questions displayed an explicit empty state; five context gaps rendered; evidence references rendered beneath every structured conclusion; the Human VFX Supervisor saw the Generate action; the Human CG Supervisor saw the reconstruction read-only, without a Generate action; no edit, accept, reject, apply, HumanGate, or Decision control appeared anywhere on the reconstruction; browser activity did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the full 17-item scenario list. As with items 6, 7, and 9, this is not independently provable from Git history alone.
12. **Step 1D — Pending HumanGate runtime preparation.** A dedicated local acceptance Shot (`D1_STEP1D_ACCEPTANCE_001`) was created via real HTTP requests against a real running API process (no `TestClient`, no direct row insertion); a `CoreAnchorRevision` draft was created through the real deterministic Core Agent draft-generation service path (`provider=deterministic`, never DeepSeek); this created exactly one pending `HumanGate` (`gate_type=core_anchor_confirmation`, `required_role=vfx_supervisor`, all resolution fields null) in the same transaction as the draft; no `Decision` existed while pending; a `human_gate.opened` `AuditEvent` was recorded; no `WritebackRecord`, Role Agent run, `ContextReconstruction` change, or `IntentDecomposition` change occurred; no ftrack network call occurred. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the local evidence ids.
13. **Step 1D — Persistent HumanGate confirmation browser acceptance.** The project owner manually verified, in a real browser session: Revision #1 initially showed a pending persisted HumanGate with its gate id, required reviewer, opened timestamp, and target revision visible; after entering a rationale and clicking Confirm once, the revision became confirmed, the HumanGate status became Confirmed, "Resolved by" showed `vfx-1` and `vfx_supervisor`, the resolved timestamp and the submitted rationale were both displayed, a linked Decision short id (`6e710675`) was displayed, and Confirm/Reject controls disappeared; a page refresh preserved the confirmed revision and resolved HumanGate; no draft remained awaiting review; no ftrack write-back occurred; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the full 15-item scenario list. As with items 6, 7, 9, and 11, this is not independently provable from Git history alone.
14. **Step 2 — Real DeepSeek Context Reconstruction acceptance via the shared Agent Runtime.** A real (non-deterministic) `provider=deepseek`, `model=deepseek-v4-flash` call was made through the new `agents.runtime.execute_agent`/shared Model Gateway/Prompt Registry path against the existing Step 1D acceptance Shot (`310b242b-106c-4d5c-ae61-d24f8972f375`, `D1_STEP1D_ACCEPTANCE_001`), as a Human VFX Supervisor actor (`vfx-1`); exactly one model call; `AgentRun.status=succeeded` with `prompt_version=context_reconstruction.v1` recorded from the Prompt Registry; exactly one `ContextSnapshot` and one `ContextReconstruction` were created; the output accurately reflected the confirmed Core Anchor, the linked Decision, and honest context gaps, citing only evidence actually present in the snapshot (no invented Constraint/VariationZone rows where the confirmed revision's own semantic collections were empty); the confirmed Core Anchor, the resolved HumanGate, and the linked Decision were all left unchanged; no `IntentDecomposition`, `AlignmentAssessment`, Role Agent run, or ftrack network call occurred. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the local evidence ids.
15. **Step 2 — Shared-runtime provenance browser acceptance.** The project owner manually verified, in a real browser session: the newly generated Context Reconstruction rendered on the Shot Anchor page; its AI provenance display showed `provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: context_reconstruction.v1`, and `run status: succeeded`, alongside the ContextSnapshot id and its captured timestamp; no additional model call was triggered by this browser session; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry. As with the other browser-acceptance items above, this is not independently provable from Git history alone.
16. **Step 3 — Real DeepSeek VFX Supervisor Agent acceptance.** A real (non-deterministic) `provider=deepseek`, `model=deepseek-v4-flash` call was made through the shared Agent Runtime/Model Gateway/Prompt Registry against a dedicated acceptance Version (`D1_STEP3_VFX_REVIEW_001`, `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`) created via the real application API on the existing Step 1D/2 acceptance Shot; exactly one model call; `AgentRun.status=succeeded` with `prompt_version=vfx_supervisor_creative_review.v1`; all 18 evidence references across the output resolved to real ids in the saved `ContextSnapshot`; the output contained one strength, three creative concerns, three review priorities, two proposed feedback notes (each preserving both what should change and why it matters to the creative intent), two questions for the Human VFX Supervisor, and four evidence gaps that explicitly stated no actual image/video media was visually inspected; the output made no unsupported visual observation, pass/fail, definitive alignment/drift, Decision, role-blame, re-anchor recommendation, or Intent Signal; the confirmed Core Anchor, the resolved HumanGate, and the linked Decision were all left unchanged; no ftrack network call occurred. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the local evidence ids.
17. **Step 3 — VFX Supervisor Agent Version-page browser acceptance.** The project owner manually verified, in a real browser session: the "VFX Supervisor Agent review" section appeared on the target Version page, labelled "AI creative review — VFX Supervisor Agent"; the executive summary, creative-direction reading, strengths, creative concerns, review priorities, proposed feedback notes (each showing both feedback and underlying intent), questions for the Human VFX Supervisor, and evidence gaps (including the missing-media limitation) all rendered, alongside evidence references; AI provenance displayed `provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: vfx_supervisor_creative_review.v1`, `run status: succeeded`, and the ContextSnapshot id and timestamp; the Human VFX Supervisor saw the Generate action; the Human CG Supervisor saw the existing review read-only, without a Generate action; no edit, apply, accept, reject, Decision, or ReviewNote-create control appeared; no additional model call was triggered; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry. As with the other browser-acceptance items above, this is not independently provable from Git history alone.
18. **Step 4 — Two real-provider truncation failures, root-caused and fixed.** Two real (non-deterministic) `provider=deepseek` calls for the new CG Supervisor Agent `execution_review` capability were made and both failed with a truncated, unparseable structured-output response before any output bound or diagnostics hardening existed: `AgentRun` `12ab6d3a-d1aa-40ec-aa1f-d86de7a904da` (`ContextSnapshot` `2dcbce05-4676-488e-81b6-c75d7dee8e29`, `max_output_tokens=4096`), and, after raising the capability-specific budget, `AgentRun` `abbc3720-15c0-4fe7-bacb-3b3d777be4d1` (`ContextSnapshot` `721dd101-7c2c-4cb1-999d-e9086d7ce166`, `max_output_tokens=8192`) — the second call truncated *even earlier* than the first, proving the token budget alone was not the root cause. Both `AgentRun` rows remain `status=failed` and unmodified; no `CGSupervisorReview` was ever created from either. The root cause was addressed with a three-part fix: (1) a substantially more compact `ContextSnapshot` input (singular, deduplicated, summary-only projections rather than verbose nested evidence trees); (2) hard `Field` length/count bounds on the `CGSupervisorReviewOutput` contract itself, so the schema — not just prompt text — caps how large a compliant response can be; (3) safe, sanitised `DeepSeekResponseDiagnostics` (finish reason, token counts, response character count, whether content was empty) surfaced through `AgentGenerationError`, replacing a prior code path that let a raw `pydantic.ValidationError` (embedding a snippet of the actual truncated response) leak into `AgentRun.error` — a genuine content-leak defect found and fixed during this work, not merely a truncation fix. No DeepSeek response content, prompt text, or raw JSON from either failed run is recorded anywhere in this repository.
19. **Step 4 — Real DeepSeek CG Supervisor Agent acceptance, pre-content-boundary-hardening.** After the fix in item 18, a real DeepSeek call succeeded structurally: `AgentRun` `8f892a77-5e95-463b-bb18-b5d75d835572` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=cg_supervisor_execution_review.v1`, `status=succeeded`), `ContextSnapshot` `dbcbf946-0642-41c5-ba24-e64b5807105f`, and `CGSupervisorReview` `f32cfd8d-4e32-4775-99c7-acd658d06412` were created against the Step 4 target Execution Anchor revision (see item 20 below), proving real-provider/runtime/persistence success end to end. It exposed two content-boundary gaps, motivating the hardening in item 20: the `evidence_gaps` list did not explicitly disclose that footage, rendered frames, or scene/project files were not directly inspected; and one `proposed_execution_guidance` item recommended updating the Core Anchor, which is outside this capability's authority. This review is retained unmodified as historical evidence of the pre-hardening state; it is not the accepted final review (see item 20).
20. **Step 4 — Content-boundary hardening and final compliant real DeepSeek CG Supervisor Agent acceptance.** Both gaps from item 19 were closed with prompt-boundary instructions plus a new pre-persistence, code-level `_validate_content_boundaries` check (not just prompt guidance) that rejects, before any `CGSupervisorReview` is persisted: a missing explicit inspection-unavailable + media/scene disclosure in `evidence_gaps`; and any instruction to modify/supersede/re-anchor the Core Anchor, resolve a HumanGate, or create an authoritative Decision, found via a bounded, testable term/pattern list (`_forbidden_authority_reason`) in `proposed_execution_guidance`, `implementation_priorities`, or `questions_for_human_cg_supervisor`. Discussing the Core Anchor as evidence, or asking the Human CG Supervisor to coordinate with the Human VFX Supervisor, remain explicitly allowed. A second, new real DeepSeek call was then made and passed both checks: `AgentRun` `f716175c-7ebc-473d-a5f3-3ec1d640b931` (`status=succeeded`), `ContextSnapshot` `6f10121c-2b65-420e-89c4-07aa4a9b9795`, and `CGSupervisorReview` `6877fa03-fab2-4e70-92a2-d0ca185d20e2` — its `evidence_gaps` explicitly stated that ICAS had not directly inspected footage, moving-image media, rendered frames, still images, or scene/project/DCC files; no proposed guidance, priority, or question recommended Core Anchor modification, re-anchoring, HumanGate resolution, or Decision creation. The confirmed Core Anchor and its resolved HumanGate/Decision were left unchanged; no ftrack network call occurred. This is the accepted, final CG Supervisor Agent review for Step 4 acceptance. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the local evidence ids.
21. **Step 4 — Execution Anchor CG Human Gate browser acceptance.** The project owner manually verified, in a real browser session, both the CG Human Gate UI (mirroring `CoreAnchorGate`) and the CG Supervisor Agent review rendering for Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5`, then used the UI to confirm the revision as the Human CG Supervisor with the rationale `"Human CG Supervisor confirms the proposed Execution Anchor for Step 4 acceptance."` Database verification after the browser session confirms the final state: revision `1561d08d-64cc-4550-b25d-27210ebbe2b5` is `status=confirmed` (`confirmed_by_actor_id=cg-1`, `confirmed_by_human_role=cg_supervisor`, `confirmed_at=2026-07-27T20:18:21.629130+00:00`); HumanGate `b6a204b0-fb01-40b8-932f-92063751a2a6` is `status=confirmed` (`resolved_by_actor_id=cg-1`, `resolved_by_role=cg_supervisor`, `resolved_at=2026-07-27T20:18:21.645945+00:00`, same rationale, `decision_id=6c71d097-6f7a-4171-83be-39c24df46db2`); Decision `6c71d097-6f7a-4171-83be-39c24df46db2` is `decision_type=confirm_execution_anchor`, `entity_id=1561d08d-64cc-4550-b25d-27210ebbe2b5`, `owning_human_role=cg_supervisor`, `actor_human_role=cg_supervisor`, with a `created_at` matching the gate's `resolved_at` to the millisecond. A duplicate revision, `07627b23-3ec1-4ff8-90e9-4901d89cec6d`, created earlier by an unintended double-submission during a prior acceptance turn (disclosed and cleaned up through the normal reject endpoint at the time, never by direct database manipulation), remains `status=rejected` and continues to render on the page as historical audit evidence, not as part of the accepted revision. No ftrack write-back occurred; no additional model call occurred during this browser review; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the full scenario list. As with the other browser-acceptance items above, this is owner-supplied and not independently provable from Git history alone.
22. **Step 5 — Artist Agent content-boundary failure-path acceptance.** The first real DeepSeek call for the new Artist Agent `iteration_guidance` capability, targeted at the confirmed Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5` for Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b` (`5a50131b-...`, "Compositing Review") and Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`, returned a structurally valid response, but the pre-persistence content-boundary validator correctly rejected it because the output advised modifying an Anchor — outside this capability's bounded advisory scope: `AgentRun` `2cec22c3-9b07-4f12-bb04-ceddd0677747` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=artist_iteration_guidance.v1`, `status=failed`, `error="Artist Agent guidance exceeded its bounded advisory scope: instructs modifying an Anchor"`) and `ContextSnapshot` `fe3ebaad-184f-4a33-97b1-f9c54b4be3d4` were both created; no `ArtistAgentGuidance` was ever persisted; no production object (Anchor, HumanGate, Decision, Version, ReviewNote, or supervisor review) was modified; no raw generated output or credential appears in the sanitised `AgentRun.error`. This was exactly one real call, with no retry. This finding directly motivated the Artist authority-boundary hardening in item 23 below.
23. **Step 5 — Artist Agent content-boundary hardening.** Following item 22, the Artist Agent's system prompt was rewritten with an explicit MAY/NEVER authority structure (may explain constraints, translate evidence, identify missing/ambiguous guidance, ask the Human Artist to seek CG clarification, ask CG↔VFX coordination, suggest pre-submission checks; must never instruct adding content to, or otherwise modifying, either Anchor, resolving a HumanGate, issuing a Decision, or officially judging/ranking a Version), including matched allowed/forbidden example pairs; the pre-persistence validator was strengthened to detect indirect Anchor-modification instructions (add/include/record/document/encode/formalise/capture/populate content in an Anchor) in addition to direct edit verbs, and to detect Anchor field names such as `allowed_refinements` even when the word "Anchor" is absent from the sentence, while continuing to allow legitimate Anchor-evidence statements ("The confirmed Execution Anchor states...") and supervisor-coordination requests; scanning was extended to cover `non_negotiables`, `allowed_variations`, and each feedback translation's `practical_action`/`underlying_intent`/`self_check`, not only `feedback_translations`/`iteration_priorities`/`cross_department_dependencies`/`questions_for_human_supervisor`; the deterministic generator was updated so that a Task with no recorded technical guidance produces a Human-CG-Supervisor clarification question and evidence gap, never an Anchor-edit instruction, and its output was verified to pass the same production validator used for real-provider output. No real DeepSeek or ftrack call was made while implementing this hardening. Focused Artist Agent tests grew from 57 to 77 passing; the full backend suite grew from 476 to 496 passing.
24. **Step 5 — Final compliant real DeepSeek Artist Agent acceptance.** After the hardening in item 23, a new real DeepSeek call was made against the same target (explicit `task_id=5a50131b-8297-4a9e-ba73-5c45144b3d3b`, Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`, confirmed Execution Anchor revision `1561d08d-64cc-4550-b25d-27210ebbe2b5`) and succeeded, passing both the evidence-resolution and content-boundary validators: `AgentRun` `70622310-d931-418a-b976-a0cea8661aec` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=artist_iteration_guidance.v1`, `status=succeeded`, `result_revision_id=null`, `error=null`), `ContextSnapshot` `3982b63d-14b1-4386-8bb7-7f1b914fec76`, and `ArtistAgentGuidance` `2fbd9c14-f47d-474b-b01d-0bb452c5f124` were created, with the `ArtistAgentGuidance` row's `created_at` preceding the `AgentRun`'s `completed_at`, confirming the guidance was durably persisted before the run was marked succeeded. Every evidence reference across the output (3 non-negotiables, 2 allowed variations, 3 feedback translations, 3 iteration priorities, 2 cross-department dependencies, 3 questions, 3 evidence gaps — all within the hard contract bounds) resolved to a real id in the saved `ContextSnapshot`; `evidence_gaps` explicitly disclosed that ICAS had not directly inspected footage, moving-image media, rendered frames, still images, scene/project/DCC files, or numeric production parameters; no output recommended modifying either Anchor, re-anchoring, HumanGate resolution, Decision creation, or an official Version pass/fail/ranking judgment; no unsupported numeric or software-specific value appeared. The confirmed Core Anchor, the confirmed Execution Anchor, both resolved HumanGates, and the existing VFX/CG Supervisor reviews were all left unchanged; no ftrack network call occurred. This is the accepted, final Artist Agent guidance for Step 5 acceptance. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry, the complete generated output, and the local evidence ids.
25. **Step 5 — Artist Agent Version-page browser acceptance.** The project owner manually verified, in a real browser session on the target Version page: the "AI iteration guidance — Artist Agent" section rendered; as the Human Artist, the explicit Task selector showed "Compositing Review" selected and the Generate control was visible; the persisted real DeepSeek guidance (item 24) rendered in full — executive summary, creative intent read, task goal, current iteration read, non-negotiables, allowed variations, each feedback translation's feedback/issue, practical action, underlying intent, and self-check, iteration priorities, cross-department dependencies, questions for the Human Supervisor, evidence gaps, and evidence references; the advisory statement explained the guidance is based on recorded text metadata only, with no visual inspection; AI provenance displayed `provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: artist_iteration_guidance.v1`, `run status: succeeded`, and the ContextSnapshot id and timestamp; switching to Human VFX Supervisor and Human CG Supervisor showed the same guidance read-only, with no Generate control for either role; no edit, apply, accept, reject, approve, or ranking control appeared anywhere on the section; no additional model call was triggered by this browser session; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry. As with the other browser-acceptance items above, this is owner-supplied and not independently provable from Git history alone.

26. **Step 6 — First real-provider structured-output failure and safe diagnostic hardening.** The first real DeepSeek call for the new Cross-role Assessment capability, targeted at Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b` ("Compositing Review") and Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`, returned a complete, non-empty, non-truncated response (`finish_reason=stop`, well under the configured token budget) that still failed opaque Pydantic structured-output validation, with no way to tell a schema-conformance problem apart from any other decode failure: `AgentRun` `8eda2584-73a8-448e-bdb4-05fc6c31bb1f` (`provider=deepseek`, `status=failed`), `ContextSnapshot` `de3acea9-869b-44cd-aaf4-5e8c16d0cdd8`; no `CrossRoleAssessment`, `ReAnchorProposal`, or `IntentSignal` was ever persisted. This motivated a shared Model Gateway hardening (applying to every capability, not only this one): `StructuredOutputValidationDiagnostics` now distinguishes `validation_stage=json_decode` (malformed JSON) from `validation_stage=schema_validation`, and for the latter reports `total_validation_errors` plus up to 10 `field_path:error_type` entries (e.g. `role_perspectives.2.role:literal_error`) — never Pydantic's `input`/`input_value`, its full message text, the raw response, the prompt, or any credential.
27. **Step 6 — Second real-provider failure (source_type enum) and contract-derived catalogue hardening.** With the new diagnostics active, a second real DeepSeek call failed cleanly and specifically: `AgentRun` `88f92372-5a64-4704-9482-2e60d439f5e8` (`status=failed`, diagnostics identifying `agreements.0.evidence.1.source_type:literal_error` and `re_anchor_proposal.evidence.3.source_type:literal_error`), `ContextSnapshot` `d0fc132a-3325-4357-913f-9f35fedc397c`; again no result object was persisted. This motivated deriving the prompt's allowed `source_type` catalogue directly from the contract's own `CrossRoleEvidenceSourceType` Literal (via `typing.get_args`, never a second hand-maintained list that could drift), placing that exact catalogue immediately beside every evidence object's shape (not only in one earlier paragraph), forbidding generic aliases (`"review"`, `"vfx_review"`, a role name used as a source_type, etc.), and requiring the `re_anchor_proposal` to use the exact Core Anchor and Role Agent source types or return `null` rather than guess.
28. **Step 6 — Third real-provider failure (genuine token-budget truncation) and capability-specific budget fix.** After the source_type fix, a third real DeepSeek call produced zero schema-validation errors but was genuinely truncated: `AgentRun` `95235b28-5905-4630-8cde-27e6d6ae36fc` (`status=failed`, `validation_stage=json_decode`, `finish_reason=length`, `configured_max_output_tokens=7168`, `completion_tokens=7168` exactly), `ContextSnapshot` `15f91a2d-bed0-42f9-9bfd-ccc31c4d1e98`; no result object was persisted. This motivated a narrow, capability-specific fix — `cross_role_assessment`'s `max_output_tokens` raised from `7168` to `8192` in the Prompt Registry only, matching the CG Supervisor Agent's own precedent (Step 4). No other capability's budget, the contract, the prompt version, the model, the shared runtime, or persistence semantics changed.
29. **Step 6 — First technically successful real-provider result, and the finding that motivated numeric hardening.** With the token budget raised, a fourth real DeepSeek call succeeded technically end to end: `CrossRoleAssessment` `5a6d16d5-2c85-4f22-8057-cbebc77ecc66`, `ReAnchorProposal` `fe177507-a3f6-43fe-b705-5ac9c7c3ad93`, `IntentSignal` `2bcae5a5-76e7-41e5-978f-e9bc8294adbf`, `AgentRun` `ee7b9225-af37-4c59-ae8f-e120fb26917e` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=core_cross_role_assessment.v1`, `status=succeeded`), `ContextSnapshot` `81479ee2-5279-4473-8d56-0d60f17c9740`; technical persistence, evidence resolution, `ReAnchorProposal` evidence-diversity, `IntentSignal` derivation, authority, and side-effect validation all passed. A subsequent owner browser review found that the advisory `ReAnchorProposal` had invented two production thresholds — "push-in speed must not exceed 0.5x normal conversation pace" and "contrast may increase up to 15% above baseline" — neither of which existed in the cited `ContextSnapshot` evidence; advisory framing does not make an invented production number evidence-grounded. This immutable result is preserved unmodified as historical evidence — it is not deleted, rewritten, corrected, or invalidated — and directly motivated the unsupported-production-numeric hardening in item 30.
30. **Step 6 — Unsupported production-numeric-value hardening.** A targeted, code-level detector was added to the existing content-boundary validator, scanning every narrative field (executive summary, shared intent read, role perspectives, findings, evidence gaps, and every `re_anchor_proposal` text field) for production-specific numeric expressions — percentages, multipliers/ratios, frame counts, fps, durations, exposure stops/EV, nits/IRE, pixels, angles, and simple ranges of each — while explicitly not matching UUIDs, timestamps, revision numbers, evidence ids, or ordinary prose. A detected expression is accepted only when the exact same value-and-unit string already appears in the text of an evidence object the *same output item* cites (no unrelated-snapshot-object credit, no arithmetic or unit-semantic conversion — e.g. 50% is never treated as equivalent to 0.5x); `re_anchor_proposal`'s own top-level text is checked against the proposal's own top-level evidence, and each `proposed_fields` entry is checked against that entry's own evidence only. An unsupported value is rejected before any persistence, with one fixed, sanitised error category (`"Cross-role assessment contained unsupported production-specific numeric values"`) that never contains the invented value, the field, or the raw text — the same preserved-ContextSnapshot/failed-AgentRun/no-partial-result discipline as every other content-boundary rejection. The deterministic generator was verified to already demonstrate the desired compliant behaviour (it may say a measurable limit is missing and ask a Human Supervisor to establish it, but never invents one) with no fixture-only exception added. No real DeepSeek or ftrack call was made while implementing this hardening. Focused Cross-role Assessment tests grew from 117 to 156; the full backend suite grew from 624 to 663.
31. **Step 6 — Final compliant real-provider acceptance.** After the numeric hardening in item 30, a new real DeepSeek call was made against the same target (Task `5a50131b-8297-4a9e-ba73-5c45144b3d3b`, Version `4f3fabe8-40f7-4a20-a419-c954b89d1ee3`) and succeeded, passing every validator including the new numeric-evidence check: `CrossRoleAssessment` `9ef47a2e-f51e-472f-a455-03b49c8d139b`, `ReAnchorProposal` `757682c6-b23a-42bf-a2a5-cb95314deb27`, `IntentSignal` `f412927c-de95-4eda-9b2c-965025355539`, `AgentRun` `8f11ceaf-ab54-4c11-8ca5-fcaeaf0e4a29` (`provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=core_cross_role_assessment.v1`, `status=succeeded`, `result_revision_id=null`, `error=null`), `ContextSnapshot` `9da4f5f7-04e9-47a8-a737-f7735d4e3130` (`created_at=2026-07-28T01:47:33.828174Z`). All three roles identified the final beat's faster push-in and stronger contrast as a risk to the restrained tone, and the placeholder Core Anchor content as insufficient; the output contained 3 role perspectives (in the required `vfx_supervisor`/`cg_supervisor`/`artist` order), 2 agreements, 1 tension, 1 local-optimum risk, 2 unresolved dependencies, 2 coordination priorities, and 4 evidence gaps, all within the hard contract bounds; every `source_type` was an exact contract Literal and every evidence id resolved to the same saved `ContextSnapshot`; no unsupported production-numeric expression appeared anywhere — `0.5x`, `15%`, and other frame/fps/duration/exposure-style examples were explicitly confirmed absent — with missing boundaries instead framed as requiring Human Supervisor definition. The `ReAnchorProposal` was present: it cited the current Core Anchor plus the VFX and CG review categories, was supported by the cross-role tension, remained advisory (proposing constraints, variation zones, and open questions, never an invented numeric threshold), and created or modified no Anchor revision. The `IntentSignal` was `attention_level=high`, `label=human_review_required`, `re_anchor_proposal_present=true`, full three-role coverage, one `anchor_clarity_gap` driver, and re-running `derive_intent_signal` against the persisted assessment reproduced the persisted signal exactly, with no separate `AgentRun` for the signal. Persistence followed the locked order (`CrossRoleAssessment` → optional `ReAnchorProposal` → required `IntentSignal` → `AgentRun` succeeded); side-effect verification confirmed only these five rows were created and no Core/Execution Anchor, HumanGate, Decision, Version, ReviewNote, VFX/CG review, Artist guidance, AlignmentAssessment, or WritebackRecord changed, and no VFX/CG/Artist Agent run or ftrack call occurred. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry and the complete generated output.
32. **Step 6 — Cross-role Assessment Shot-page browser acceptance, including the history-clarity fix.** The project owner manually verified, in a real browser session on the Shot page for Task "Compositing Review" / Version "D1_STEP3_VFX_REVIEW_001": the newest, compliant assessment (item 31) rendered first, labelled "Latest assessment" with its created timestamp, fully expanded — executive summary (beginning "All three roles agree..."), all three role perspectives, agreements, tensions, local-optimum risks, dependencies, priorities, evidence gaps, evidence, the Re-anchor Proposal, the Intent Signal, and AI provenance (`provider: deepseek`, `model: deepseek-v4-flash`, `prompt version: core_cross_role_assessment.v1`, `run status: succeeded`, ContextSnapshot id and timestamp) all rendered, with no invented `0.5x`/`15%` value anywhere in the latest result and no apply/accept/reject/materialise/confirm/create-draft control anywhere on the section; the older, historically-flawed assessment (item 29) moved under a collapsed-by-default "Previous assessments (N)" section carrying the explicit disclaimer "These are immutable historical outputs and may reflect earlier validation rules. They are not the current assessment.", each historical record individually collapsed with its own heading and timestamp, still deliberately inspectable on expansion (its old `0.5x`/`15%` content included) but never presented as part of the latest assessment; zero- and single-assessment states remained correct; the Human VFX Supervisor still saw Generate, Human CG Supervisor and Human Artist remained read-only. No additional model call was triggered by this browser session; browser actions did not modify any repository file. See `docs/VALIDATION_EVIDENCE.md` for the structured table entry. As with the other browser-acceptance items above, this is owner-supplied and not independently provable from Git history alone.

**Evidence-type discipline applied throughout this document:**

- **Automated test evidence** — proven by a passing test in this repository's test suite (backend `pytest`, frontend `vitest`). Reproducible by any reader by running the suite.
- **Repository evidence** — proven by inspecting committed code, migrations, or Git history directly. Reproducible by any reader with repository access.
- **Manual owner-supplied validation evidence** — reported by the project owner as having occurred, against real external systems (a real ftrack workspace, a real DeepSeek API call) that this repository's automated tests deliberately never touch (per this project's own testing rules — automated tests must never make a real ftrack or model-provider call). Not independently reproducible from the repository alone; recorded here as project history, not as a repository-verifiable fact.

---

## F. Design Concept completeness matrix

| Module | Sub-capability | Status | Basis |
|---|---|---|---|
| **Core Agent** | Intent decomposition | **Complete, merged to `main`** (PR #8, `cd24eaf`) | `agents/intent_decomposition_service.py`, migration `0015_intent_decomposition.py`; deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance (`docs/VALIDATION_EVIDENCE.md`) |
| | Core Anchor establishment | **Complete, merged to `main`** — Step 1A semantic objects, Step 1B decomposition-led upstream proposal, and Step 1D minimal persistent HumanGate are all merged (PR #10, `b1f3747`, for Step 1D) | `core_anchor_drafting` (direct path, still backend-compatible but demoted in the UI) plus the Step 1B decomposition-led path (`intent_decomposition` → Human VFX Supervisor review → explicit "Use in Core Anchor draft" → editable `CoreAnchorRevision` draft with `source_intent_decomposition_id` lineage) both exist and now each open exactly one pending `HumanGate` (migration `0017_persistent_human_gate.py`), atomically resolved by the existing confirm/reject actions and linked to the resulting `Decision`; confirmation and full semantic-content authorship remain human acts via the UI (Step 1A) |
| | Core Anchor semantic supporting objects | **Complete, merged to `main`** (backend + minimal UI, Step 1A; PR #7, `2d1101f`) | `Constraint`/`VariationZone`/`DriftRisk`/`AnchorReference`/`OpenQuestion` (migration `0014`, committed `173f32e`) plus the Human VFX Supervisor view/add/edit/remove/move/save/cancel UI inside the existing Core Anchor draft workflow (`CoreAnchorGate`, committed `4e544dd`); human-authored only — Core Agent does not populate these directly |
| | Context reconstruction | **Complete, merged to `main`** (PR #9, `e306087`) | `agents/context_reconstruction_service.py`, migration `0016_context_reconstruction.py`; produces an immutable, evidence-backed interpretation of a fresh `ContextSnapshot` (`ContextSnapshot` itself remains a different object — see section B); deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance (`docs/VALIDATION_EVIDENCE.md`) |
| | Alignment assessment | Completed (as Core Agent's own capability) | `agents/alignment_assessment_service.py`, deterministic + real DeepSeek provider |
| | Cross-role assessment | **Implemented and real-DeepSeek-validated, complete on `feat/step6-cross-role-alignment`, pending merge to `main`** (Step 6) | `agents/cross_role_assessment_service.py`, migration `0022_cross_role_alignment.py`; synthesises the confirmed Anchors and the three Role Agents' newest outputs for an explicit Task/Version into agreements/tensions/local-optimum-risks/unresolved-dependencies/coordination-priorities; deterministic provider for automated tests, real DeepSeek provider proven by manual acceptance across four calls and two rounds of hardening (`docs/VALIDATION_EVIDENCE.md`) |
| | Re-anchor proposal | **Implemented, complete on `feat/step6-cross-role-alignment`, pending merge to `main`** (Step 6, part of the `cross_role_assessment` output) | `CrossRoleAssessmentOutput.re_anchor_proposal` / `ReAnchorProposal` (migration `0022`); bounded advisory evidence only — no approval state, no HumanGate/Decision linkage, no apply/materialise endpoint; persisted only when the evidence-diversity/core-anchor-citation/supporting-finding rules in `_validate_re_anchor_proposal` all hold |
| | Intent Signal | **Implemented, complete on `feat/step6-cross-role-alignment`, pending merge to `main`** (Step 6) | `IntentSignal`/`derive_intent_signal` (migration `0022`); deterministic `low`/`medium`/`high` `attention_level` derived from an already-persisted `CrossRoleAssessment` by a pure function — not an Agent, no separate `AgentRun`; not an alignment verdict, quality score, Decision, or approval recommendation |
| **Role Agents** | VFX Supervisor Agent | **Implemented (`creative_review`), merged to `main`** (PR #12, `d68569e`) | Section C |
| | CG Supervisor Agent | **Implemented (`execution_review`), merged to `main`** (PR #13, `6aa3031`) | Section C |
| | Artist Agent | **Implemented (`iteration_guidance`), merged to `main`** (PR #14, `93fb730`) | Section C |
| **Dashboard** | Shared shot view | **Partial** — a single generic Shot Anchor page exists; it is not the role-aware "shared shot view" `docs/PRODUCT_SCOPE.md` §13.1 describes (intent signal, version/decision timeline are absent) | `ShotAnchorPage.tsx` |
| | VFX view | Not started | No role-specific dashboard view exists; the minimal Shot/Version pages are role-agnostic (they show the same content regardless of selected role) |
| | CG view | Not started | Same as above |
| | Artist view | Not started | Same as above |

**Important distinctions preserved explicitly, per instruction:**
- Core Anchor establishment is now **complete and merged to `main`** — drafting (direct and decomposition-led), full semantic-content authorship, and the minimal persistent HumanGate all exist and are merged; see section D for the exact commit/PR for each sub-slice.
- The current `ContextSnapshot` does **not** complete "Context Reconstruction" — they are different objects with different purposes (see section B).
- The current Alignment Assessment is explicitly a **Core Agent** capability, not a VFX Supervisor Agent capability.
- The minimal Shot/Version pages do **not** complete the role-aware Dashboard — they are one generic view, not four role-differentiated ones.
- The VFX Supervisor Agent (Step 3) is **not** the Core Agent and **not** the Human VFX Supervisor. Its `VFXSupervisorReview` output is advisory only: it cannot confirm/reject a Core Anchor or Version, cannot resolve a HumanGate, cannot create an authoritative `Decision`, cannot create an actual `ReviewNote`, and cannot write to ftrack. The Human VFX Supervisor retains all authority.
- The CG Supervisor Agent (Step 4) is **not** the Core Agent, **not** the VFX Supervisor Agent, and **not** the Human CG Supervisor. Its `CGSupervisorReview` output is advisory only: it cannot confirm/reject an Execution Anchor revision, cannot resolve a HumanGate, cannot create an authoritative `Decision`, and cannot write to ftrack; a code-level content-boundary validator additionally rejects any output that would recommend modifying/superseding/re-anchoring the Core Anchor before persistence, not merely by prompt instruction. The Human CG Supervisor retains all authority over Execution Anchor confirmation and rejection.
- The Artist Agent (Step 5) is **not** the Core Agent, **not** the VFX Supervisor Agent, **not** the CG Supervisor Agent, and **not** the Human Artist. Its `ArtistAgentGuidance` output is advisory only: it cannot establish, modify, confirm, reject, replace, or supersede either Anchor, cannot resolve a HumanGate, cannot create an authoritative `Decision`, cannot create an actual `ReviewNote`, and cannot write to ftrack; a code-level content-boundary validator additionally rejects any output that would add content to, or otherwise modify, either Anchor — including indirect or field-name-based instructions — before persistence, not merely by prompt instruction. Human supervisors retain all authority.
- Cross-role Assessment (Step 6) is a **fifth capability of the same Core Agent, not a fifth Agent.** Its `IntentSignal` is a deterministic, explainable projection derived from an already-persisted `CrossRoleAssessment` by a pure function — not itself an Agent, not model-generated, and it creates no separate `AgentRun`; it is an attention signal only, never an alignment verdict, a quality score, a Decision, or an approval recommendation. Its optional `ReAnchorProposal` is bounded advisory evidence generated within the same Core Agent assessment — it is not a `CoreAnchorRevision`, does not modify or replace an Anchor, has no approval state, no HumanGate or Decision linkage, and no apply/materialise action; a Human VFX Supervisor must independently choose to act on it through the existing Core Anchor drafting workflow. A code-level validator additionally rejects any output — inside or outside `re_anchor_proposal` — that would invent an unsupported production-specific numeric value (a percentage, multiplier, frame count, duration, exposure value, or similar) not present, value-and-unit-exact, in the cited evidence.

---

## G. Work Package completeness

| Work Package | Status | Detail |
|---|---|---|
| **WP-A — Intent Core & Workflow** | Partial | Completed: Core Anchor lifecycle (A1), Execution Anchor lifecycle (A2), stale propagation, Decision/WorkflowTransition/AuditEvent foundations, Human Gate UI for Core Anchor (A3), Version/ReviewNote (Step 4a), Alignment Assessment + its Human Gate + supersession (Step 4b/4c), Version/Assessment UI (Step 4d), Core Anchor semantic objects **backend** (Step 1A-Backend, migration `0014`, committed `173f32e`) and **minimal semantic editing UI** (Step 1A-UI, `CoreAnchorGate`) — **Step 1A as a whole is merged to `main`** (PR #7, `2d1101f`) — plus Core Agent Intent Decomposition and the decomposition-led Core Anchor draft workflow (Step 1B, migration `0015`) — **Step 1B is merged to `main`** (PR #8, `cd24eaf`) — plus Core Agent Context Reconstruction (Step 1C, migration `0016`) — **Step 1C is merged to `main`** (PR #9, `e306087`) — plus the minimal persistent HumanGate (Step 1D, migration `0017`) — **Step 1D is merged to `main`** (PR #10, `b1f3747`). **Step 1 as a whole (1A–1D) is complete and merged to `main`.** Plus Step 4 (merged to `main`, PR #13, `6aa3031`): the Execution Anchor's own frontend CG Human Gate UI (confirm/reject, mirroring `CoreAnchorGate`) and `HumanGate`'s dual-target extension to Execution Anchor revisions (migration `0020`) — the previously-open "no frontend Human Gate UI for Execution Anchor" gap is now closed. Plus Step 5 (merged to `main`, PR #14, `93fb730`): an "AI iteration guidance — Artist Agent" section on the existing Version page (Task selector + Generate for the Human Artist, full structured rendering, read-only for the other two roles). Plus, as of Step 6 (`feat/step6-cross-role-alignment`, pending merge to `main`): a "Cross-role assessment — Core Agent" section on the existing Shot page (explicit Task/Version selectors, Generate for the Human VFX Supervisor only, full structured rendering including the optional Re-anchor Proposal and the Intent Signal), plus a history-clarity fix that labels the newest assessment "Latest assessment" (fully expanded) and collapses older immutable assessments under a "Previous assessments (N)" section (collapsed by default, individually expandable, carrying an explicit "these are immutable historical outputs... not the current assessment" disclaimer) — no new route or Dashboard. |
| **WP-B — Agent Runtime & Evaluation** | Partial | Completed: `ContextSnapshot`/`AgentRun` foundation; deterministic-provider pattern; one real model provider (DeepSeek) proven for five Core Agent capabilities (Alignment Assessment, Intent Decomposition, Context Reconstruction, and now Cross-role Assessment), the VFX Supervisor Agent's `creative_review` capability, the CG Supervisor Agent's `execution_review` capability, and the Artist Agent's `iteration_guidance` capability; a shared, lightweight Agent Runtime (Step 2, migration `0018`) — `agents/runtime.py`'s `AgentExecutionSpec`/`execute_agent`, a shared Model Gateway (`agents/model_gateway.py`), and a static Prompt Registry (`agents/prompt_registry.py`: `core_anchor_drafting.v1`/`intent_decomposition.v1`/`context_reconstruction.v1`/`alignment_assessment.v1`/`vfx_supervisor_creative_review.v1`/`cg_supervisor_execution_review.v1`/`artist_iteration_guidance.v1`/`core_cross_role_assessment.v1`) now centralise the ContextSnapshot/AgentRun lifecycle, provider dispatch, structured-output validation, and result-persistence ordering across all five Core Agent capabilities **and** all three Role Agent capabilities — **Step 2 is merged to `main`** (PR #11, `bd2f670`). Step 4 additionally proved that the shared Model Gateway supports a capability-specific `max_output_tokens` override (`agents/prompt_registry.py`'s `PromptRegistration.max_output_tokens`, 8192 for `cg_supervisor_execution_review.v1` and, as of Step 6, `core_cross_role_assessment.v1` too — raised there from an initial `7168` after a genuine real-provider truncation, see section E items 28/31 — 6144 for `artist_iteration_guidance.v1`, default 4096 unchanged for every other capability) and safe, sanitised provider-failure diagnostics (`DeepSeekResponseDiagnostics`, extended in Step 6 to `StructuredOutputValidationDiagnostics` — a `validation_stage` of `json_decode` or `schema_validation`, plus up to 10 sanitised `field_path:error_type` entries for the latter) that never leak raw response content. Step 5 additionally established an explicit domain decision for capabilities that need a Task context but target a Version: since `Version` carries no `task_id` of its own, the caller supplies `task_id` explicitly in the request body rather than the service inferring it heuristically — recorded as this capability's design, not a claim that `Version` gained a permanent Task ownership relationship; Step 6 reused this same decision unchanged. Step 6 also established a contract-derived-enum-catalogue pattern (deriving a prompt's allowed value list directly from the contract's own `typing.Literal` via `typing.get_args`, rather than a second hand-maintained list that could drift) and an evidence-scoped numeric-value hardening pattern (a generated number is accepted only when the same value-and-unit already appears in evidence the same output item cites) — both reusable precedents for future capabilities. Not started: a formal evaluation harness (Step 2 explicitly excluded a scoring/evaluation platform, per section I). |
| **WP-C — ftrack Connector** | Partial | Completed: real Project/Shot/Task sync (reconciliation worker), real controlled write-back for Core Anchor confirmation, real Version/ReviewNote sync from ftrack (Step 8 — see Section K). Not started: write-back for Alignment Assessment decisions; Execution Anchor write-back; ftrack Action/embedded-Widget entry points (`docs/ARCHITECTURE.md` §3.4, `docs/PRODUCT_SCOPE.md` §14.4). |
| **WP-D — Dashboard & Interaction** | Partial (minimal) | Completed: a single generic Shot Anchor page and Version detail page, both functional and tested. Not started: role-aware multi-view Dashboard (`docs/PRODUCT_SCOPE.md` §13.1–13.5); drift timeline; audit/history view; cross-department Shot Assembly view. Deliberately deferred as non-essential enterprise scope for this stage: multi-tenant administration, generic dashboards, real-time infrastructure. |

No Work Package is marked "complete." Each has real, tested, committed slices and real, identified gaps.

---

## H. Locked future implementation order

This sequence is locked. Steps must not be skipped or silently reordered. Any scope change requires explicit project-owner approval and must be recorded as an update to this document before implementation begins. Completed work (sections C–G above) remains valid unless repository evidence proves otherwise — this baseline is not being redone.

```
0. Repository status and Roadmap baseline; merge current baseline        [Step 0A — done]
1. Core Anchor semantic objects
   + Core Agent Intent Decomposition
   + Core Agent Context Reconstruction
   + minimal HumanGate

   Internal sub-slice order (locked, does not change Step 1's position
   among 0-9):
     1A-Backend  Semantic objects backend                    [DONE — merged to main, committed 173f32e]
     1A-UI       Minimal Core Anchor semantic editors          [DONE — merged to main via PR #7, 2d1101f]
     1B          Core Agent Intent Decomposition                [DONE — merged to main via PR #8, cd24eaf]
     1C          Core Agent Context Reconstruction               [DONE — merged to main via PR #9, e306087]
     1D          Minimal persistent HumanGate                    [DONE — merged to main via PR #10, b1f3747]

   Step 1A, Step 1B, Step 1C, and Step 1D are all merged to main. Step 1
   as a whole is complete and merged to main.
2. Lightweight shared Agent Runtime                               [DONE — merged to main via PR #11, bd2f670]
3. VFX Supervisor Agent                                            [DONE — merged to main via PR #12, d68569e (e986ba5)]
4. CG Supervisor Agent
   + Execution Anchor CG Human Gate UI                              [DONE — merged to main via PR #13, 6aa3031 (2b89ac2)]
5. Artist Agent                                                     [DONE — merged to main via PR #14, 93fb730 (81a79be)]
6. Cross-role Assessment
   + Re-anchor Proposal
   + Intent Signal                                                    [DONE — implemented + real-DeepSeek-validated on feat/step6-cross-role-alignment, pending merge to main]

   Step 2, Step 3, Step 4, and Step 5 are all merged to main. Step 6 is
   complete on its feature branch (`feat/step6-cross-role-alignment`) —
   automated validation, four real-DeepSeek acceptance calls across two
   rounds of hardening (structured-output diagnostics, source_type
   enum conformance, a capability-specific token-budget fix, and
   unsupported-production-numeric-value hardening), a final compliant
   real-provider result, and browser acceptance (including a UI
   history-clarity fix) are all complete — pending commit/push/PR/merge
   to main. Step 7 must not begin until, in order: (1) the Step 6 docs
   commit is made; (2) the feature branch is pushed; (3) Step 6 is
   merged into main; (4) local main is synchronised and clean. Step 7
   is not planned or implemented by this document.
7. Role-aware Dashboard                                            [DONE — see Section J, dated 2026-08-01]
8. Necessary ftrack Version / Note / link extensions                [DONE — see Section K, dated 2026-08-02]
9. Evaluation, complete demonstration, and project close-out
```

**Note added 2026-08-01 (Section J below has the full record):** Step 7 (Role-aware Dashboard, delivered as three role-aware workspaces — VFX Supervisor, CG Supervisor, Artist — plus their shared Review Inbox architecture) is complete on branch `recovery/step7c2-functional-clean`. Step 8 (ftrack Version/Note/link extensions) is next; visual refinement is deliberately sequenced after Step 8 and before Step 9, not immediately after Step 7 — see Section J for the full reasoning and evidence. The Step 6 entry above is left exactly as originally recorded (this document predates verification of Step 6's merge status into `main` proper from this branch).

**Note added 2026-08-02 (Section K below has the full record):** Step 8 (ftrack Version/ReviewNote/link extensions — real controlled-workspace reconciliation, database-integrity verification, and owner visual validation) is now complete on branch `feat/step8c89-real-ftrack-acceptance`. Visual refinement is the next approved activity, before Step 9. **Step 9 has not started.**

---

## I. Detailed remaining-work outline

Each future step below states purpose, minimum prototype scope, dependencies, explicit exclusions, and the acceptance evidence required before the next step may begin. None of these steps are implemented by this document — this section is planning input only, matching the boundaries already approved in prior scope discussions.

### Step 1 — Core Anchor semantic objects + Core Agent Intent Decomposition + Context Reconstruction + minimal HumanGate

- **Purpose:** complete the Core Anchor's supporting domain objects and give Core Agent its two remaining, most foundational capabilities.
- **Minimum prototype scope:** `Constraint`, `VariationZone`, `DriftRisk`, `AnchorReference`, `OpenQuestion` domain objects (per `docs/DOMAIN_MODEL.md` §5 — implemented under the class name `AnchorReference`, not the generic `Reference`); `intent_decomposition` capability (Core Agent); `context_reconstruction` capability (Core Agent, distinct from `ContextSnapshot`); a minimal persistent `HumanGate` object/escalation record (today, "Human Gate" is a UI/workflow pattern, not its own persisted domain object — `docs/DOMAIN_MODEL.md` §9 names `HumanGate` as its own object, which does not yet exist as a table).
- **Dependencies:** none blocking; reuses the existing `ContextSnapshot`/`AgentRun`/Protocol-generator pattern.
- **Explicit exclusions:** no Role Agent work; no Dashboard changes beyond what is needed to surface the new objects minimally.
- **Acceptance evidence required:** backend tests for each new domain object and capability; no regression in existing Core Anchor/Alignment Assessment tests; updated status baseline in this document.
- **Current status (sub-sliced; see section H for the locked internal order):**
  - **Step 1A-Backend — committed as `173f32e` (feat: add core anchor semantic objects).** All five semantic-child tables exist (migration `0014_core_anchor_semantic_objects.py`), reachable through the existing Core Anchor draft/update/get/list/confirm/reject endpoints (no new endpoints). Automated evidence: 18 focused tests + full 206-test backend suite + 43-test frontend suite passing, migration upgrade/downgrade/upgrade verified, `ruff`/`mypy`/OpenAPI export/TypeScript contract regeneration/typecheck all passing (all automated/repository evidence, reproducible from this repository). Manual evidence: a real-`uvicorn`, real-HTTP acceptance pass against an isolated temporary SQLite database — see section E item 6 and `docs/VALIDATION_EVIDENCE.md`.
  - **Step 1A-UI — merged to `main` via PR #7, merge commit `2d1101f`** (`4e544dd` feat: add core anchor semantic editors; `cc18fc4` style: format intent module readme). The Human VFX Supervisor can view/add/edit/remove/move up/move down/save/cancel all five semantic collections on a draft revision, inside the existing `CoreAnchorGate` workflow; Human CG Supervisor and Human Artist see a read-only "Draft details" view instead of a disabled form; confirmed revisions render all five collections read-only. Automated evidence: 70 frontend tests passing (54 Shot Anchor + 16 Version page), web TypeScript typecheck/ESLint/Prettier/production build all passing, no backend/migration/contract/generated/Version-page/dependency file touched. Manual evidence: a real-browser acceptance pass — see section E item 7 and `docs/VALIDATION_EVIDENCE.md`.
  - **Step 1A as a whole (1A-Backend + 1A-UI) is merged to `main`.**
  - **Step 1B (Core Agent Intent Decomposition) is merged to `main` via PR #8, merge commit `cd24eaf`** (`fc233bd` feat: add core agent intent decomposition). Core Agent generates an immutable `IntentDecomposition` from an `IntentBrief`; the Human VFX Supervisor reviews the AI proposal and may explicitly use it to create a new, fully editable `CoreAnchorRevision` draft (migration `0015_intent_decomposition.py`). See sections D and E above and `docs/VALIDATION_EVIDENCE.md` for the full automated, real-DeepSeek, and browser evidence.
  - **Step 1C (Core Agent Context Reconstruction) is merged to `main` via PR #9, merge commit `e306087`** (`34a7ceb` feat: add core agent context reconstruction). Core Agent produces an immutable, evidence-backed interpretation of the exact local facts recorded in a fresh `ContextSnapshot` (migration `0016_context_reconstruction.py`), explaining why the current production context exists without ever judging Version alignment/drift, role performance, or recommending a re-anchor; every structured conclusion cites one or more evidence references back to real ids in that same snapshot. See sections D and E above and `docs/VALIDATION_EVIDENCE.md` for the full automated, real-DeepSeek, and browser evidence.
  - **Step 1D (Minimal Persistent HumanGate) is merged to `main` via PR #10, merge commit `b1f3747`** (`c35c3dc` feat: add persistent core anchor human gate). A first-class `human_gates` table (migration `0017_persistent_human_gate.py`) records that an authoritative human review is required for a `CoreAnchorRevision` draft and how it was resolved; every draft-creation path opens exactly one pending gate atomically with the draft; the existing Core Anchor confirm/reject actions remain the sole authoritative write path and now atomically resolve the gate, linking it to the existing `Decision` they already create; `Decision` remains the sole authoritative human record. See sections D and E above and `docs/VALIDATION_EVIDENCE.md` for the full automated, runtime-preparation, and browser evidence.
  - **Step 1 overall (1A + 1B + 1C + 1D) is complete and merged to `main`.**

### Step 2 — Lightweight shared Agent Runtime

- **Purpose:** extract the provider-selection/capability-registration pattern that is currently duplicated inline in `core_agent_service.py` and `alignment_assessment_service.py` into a small, shared, reusable layer before a second and third Agent copy that duplication a third and fourth time.
- **Minimum prototype scope:** capability registry (a lookup from capability name to its Protocol/generator pair); a lightweight Model Gateway (single interface for provider selection, reused by all Agents); a prompt registry/version field, already partially present per-capability today but not centralised; structured evidence-reference handling shared across capabilities; a basic evaluation harness (a way to run a capability against a fixture and inspect output — not a scoring system).
- **Dependencies:** Step 1's capabilities are good candidates to be the first consumers, but this step is really a refactor of the existing two Core Agent capabilities plus a shared seam for the Role Layer.
- **Explicit exclusions:** no new product-facing capability; no Role Agent implementation yet; no enterprise-grade queueing/retry infrastructure.
- **Acceptance evidence required:** existing Core Agent capabilities pass all existing tests unchanged after being migrated onto the shared runtime; no behavioural change to any existing endpoint.
- **Current status:** **Step 2 is merged to `main` via PR #11, merge commit `bd2f670`** (`ac9e6c0` refactor: add lightweight shared agent runtime). `agents/runtime.py`'s `AgentExecutionSpec`/`execute_agent` owns the ContextSnapshot-persist → AgentRun(running) → provider-dispatch → structured-output-validate → domain-result-persist → AgentRun(succeeded/failed) lifecycle for all four existing Core Agent capabilities (`core_anchor_drafting`, including its decomposition-to-draft deterministic transform which remains a pure field mapping with no second model call; `intent_decomposition`; `context_reconstruction`; `alignment_assessment`) **and** now for the VFX Supervisor Agent's `creative_review` capability (Step 3); `agents/model_gateway.py` is the one shared OpenAI-compatible DeepSeek call path (settings-driven provider/base URL/model/key, sanitised errors, no added providers); `agents/prompt_registry.py` is a small static registry — not a database-backed prompt CMS or remote prompt service; migration `0018_agent_runtime_metadata.py` adds nullable `model_name`/`prompt_version` to `AgentRun` (historical rows remain valid with null values; a deterministic run always has both null). No generic public "execute agent" API was added — each capability's own endpoints remain the only user-facing surface, and Core Anchor/HumanGate/Decision behaviour, authority, and contracts are all unchanged. Automated evidence: migration `0018` upgrade/downgrade/upgrade passed; 301 backend tests passed (was 277); 104 frontend tests passed (88 Shot Anchor page + 16 Version page); `ruff` check/format, all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, repository Prettier, and the Next.js production build all passed. Real-provider evidence: exactly one real DeepSeek Context Reconstruction call was made through this shared runtime — see section E item 14 and `docs/VALIDATION_EVIDENCE.md`. Browser evidence: see section E item 15.

### Step 3 — VFX Supervisor Agent

- **Purpose:** implement the first Role Agent.
- **Minimum prototype scope:** VFX feedback processing; feedback clustering/prioritisation; creative review pre-check; drift-risk identification; review-question drafting; reference and re-anchor *suggestions* only.
- **Dependencies:** Step 2's shared runtime (recommended, not strictly blocking); the existing `ContextSnapshot`/`AgentRun`/`ActorContext` pattern (already available, no dependency).
- **Explicit exclusions:** no final approval authority of any kind; cannot alter the Core Anchor; cannot resolve the VFX Supervisor's own Human Gate.
- **Acceptance evidence required:** a real `AgentRun` with `agent_type="vfx_supervisor_agent"`; a real capability constant and generator; tests proving the Agent's output is advisory-only and creates no `Decision`; UI (if any, per Step 7 scope) clearly labels output as VFX Supervisor Agent's, not Core Agent's.
- **Current status:** **Step 3 is merged to `main` via PR #12, merge commit `d68569e`** (`e986ba5` feat: add vfx supervisor agent). `agents/vfx_supervisor_review_service.py` (migration `0019_vfx_supervisor_review.py`) produces an immutable `VFXSupervisorReview` for one Version — `executive_summary`, `creative_direction_read`, `strengths`, `creative_concerns`, `review_priorities`, `proposed_feedback_notes` (each with `feedback`/`underlying_intent`/priority/evidence), `questions_for_human_supervisor`, and `evidence_gaps` — using only local textual evidence (Project/Shot/Version metadata, IntentBrief, IntentDecompositions, confirmed Core Anchor with semantic children, newest Context Reconstruction, newest Alignment Assessment, existing ReviewNotes, relevant Decisions, Task/Execution Anchor context, and HumanGate resolution facts where locally available); the repository performs no image/video analysis, so the Agent never claims to have visually inspected media and must honestly record that limitation in `evidence_gaps`; every evidence reference is validated to resolve to a real id in that run's own `ContextSnapshot`, with invented ids rejected before persistence; no pass/fail, definitive alignment/drift, role blame, re-anchor recommendation, or Intent Signal is ever produced. It runs through the same shared Agent Runtime as Core Agent (registered in the Prompt Registry as `vfx_supervisor_creative_review.v1`) — no second DeepSeek client path was added. Generation is Human-VFX-Supervisor-only (`POST /intent/versions/{id}/vfx-supervisor-reviews/generate`; CG Supervisor and Artist receive 403; agent actors are forbidden); all three human roles may read (`GET /intent/vfx-supervisor-reviews/{id}`, `GET /intent/versions/{id}/vfx-supervisor-reviews`, newest first) — no PATCH/DELETE/apply/approve/generic-execute/ReviewNote-create/ftrack-writeback endpoint. The existing Version page gained a "VFX Supervisor Agent review" section labelled "AI creative review — VFX Supervisor Agent", with a Generate action for the Human VFX Supervisor only, full structured rendering, explicit empty states, and no edit/apply/accept/reject controls — no new route, Dashboard, or dependency. Automated evidence: migration `0019` upgrade/downgrade/upgrade passed; 323 backend tests passed (was 301); 117 frontend tests passed (was 104); `ruff` check/format, all four CI `mypy` scopes, OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, repository Prettier, and the Next.js production build all passed; automated tests specifically proved evidence ids resolve to snapshot facts, invented ids are rejected, provider/validation/persistence failure creates no partial review, and generation creates no Core Anchor/HumanGate/Decision/AlignmentAssessment/Version/ReviewNote/WritebackRecord mutation and no Core Agent/CG Supervisor Agent/Artist Agent run. Real-provider evidence: exactly one real DeepSeek VFX Supervisor Agent call was made through the shared runtime — see section E item 16 and `docs/VALIDATION_EVIDENCE.md`. Browser evidence: see section E item 17.

### Step 4 — CG Supervisor Agent + Execution Anchor CG Human Gate UI

- **Purpose:** implement the second Role Agent, and close the existing frontend gap for the human CG Supervisor's own Human Gate (backend already exists, per section G).
- **Minimum prototype scope:** CG Execution Anchor drafting (reusing the already-scaffolded `cg_supervisor_agent` permission allowlist — see section C); technical-translation explanation; production-readiness risk; downstream-impact assessment; a CG Human Gate UI (confirm/reject Execution Anchor in the frontend, mirroring `CoreAnchorGate`); escalation when a technical issue may affect the Core Anchor.
- **Dependencies:** none blocking; the permission scaffold already exists and is the natural entry point.
- **Explicit exclusions:** cannot redefine the Core Anchor; cannot autonomously confirm production-ready state.
- **Acceptance evidence required:** a real `AgentRun` with `agent_type="cg_supervisor_agent"` from production code (not just a test); a working CG Human Gate UI with backend-enforced permissions, tested the same way `CoreAnchorGate` is tested.
- **Current status:** **Step 4 is merged to `main` via PR #13, merge commit `6aa3031`** (`2b89ac2` feat: add cg supervisor agent and execution anchor gate). `agents/cg_supervisor_review_service.py` (migration `0020_cg_supervisor_review_and_execution_gate.py`) produces an immutable `CGSupervisorReview` for one `ExecutionAnchorRevision` — `executive_summary`, `execution_direction_read`, `actionable_requirements`, `technical_concerns`, `coordination_concerns`, `implementation_priorities`, `proposed_execution_guidance` (each with `guidance`/`underlying_intent`/priority/evidence), `questions_for_human_cg_supervisor`, and `evidence_gaps` — using only local textual evidence, scoped to a compact `ContextSnapshot` (singular Project/Shot/Task/IntentBrief/IntentDecomposition/confirmed-Core-Anchor/Context-Reconstruction/Execution-Anchor-revision/Version/newest-VFX-review/Alignment-Assessment/Decision/both-HumanGate summary-only projections, never verbose nested evidence trees); the repository performs no footage/frame/render/scene/DCC-file inspection, so the Agent never claims to have inspected media and must explicitly disclose that limitation in `evidence_gaps`; every evidence reference is validated to resolve to a real id in that run's own `ContextSnapshot`, with invented ids rejected before persistence. Every list and string field on the output contract carries a hard `Field` bound (`executive_summary`≤700 chars; `actionable_requirements`/`technical_concerns`/`implementation_priorities`/`proposed_execution_guidance`/`questions_for_human_cg_supervisor`≤3 items; `coordination_concerns`≤2 items; `evidence_gaps`≤5 items; item `summary`≤280/`rationale`≤420 chars, guidance `guidance`≤320/`underlying_intent`≤420 chars, each with 1–2 evidence references; note strings ≤260 chars) — the schema itself, not just prompt text, bounds how large a compliant response can be. A pre-persistence, code-level content-boundary validator additionally rejects (before any `CGSupervisorReview` row is created): a missing explicit inspection-unavailable + media/scene disclosure in `evidence_gaps`; and any instruction to modify/supersede/re-anchor the Core Anchor, resolve a HumanGate, or create an authoritative Decision, appearing in `proposed_execution_guidance`, `implementation_priorities`, or `questions_for_human_cg_supervisor` — mentioning the Core Anchor as evidence, or asking the Human CG Supervisor to coordinate with the Human VFX Supervisor, remain explicitly allowed. It runs through the same shared Agent Runtime as Core Agent and the VFX Supervisor Agent (registered in the Prompt Registry as `cg_supervisor_execution_review.v1`, with a capability-specific `max_output_tokens=8192` — every other capability keeps the shared 4096-token default) — no second DeepSeek client path was added. Provider-failure diagnostics (`DeepSeekResponseDiagnostics`: finish reason, configured token budget, prompt/completion/total token counts where returned, response character count, whether content was empty) are recorded in a sanitised `AgentGenerationError` message on any parse failure, never the raw response content — this also fixed a pre-existing defect where a raw `pydantic.ValidationError` could leak a snippet of provider output into `AgentRun.error`. Generation is Human-CG-Supervisor-only; all three human roles may read; no PATCH/DELETE/apply/approve/generic-execute/ReviewNote-create/ftrack-writeback endpoint. Migration `0020` also extends `HumanGate` with a nullable dual-target FK pair (`core_anchor_revision_id`/`execution_anchor_revision_id`) under a DB CHECK XOR constraint (exactly one must be set), rather than a generic polymorphic `target_type`/`target_id`. The existing Shot Anchor page gained a CG Human Gate UI for the Execution Anchor's own HumanGate (confirm/reject, mirroring `CoreAnchorGate`) and a CG Supervisor Agent review rendering section — no new route or Dashboard. Automated evidence: migration `0020` upgrade/downgrade/upgrade passed (verified against test fixtures during implementation validation; not re-run against the real acceptance database in this documentation turn, to avoid disturbing the now-confirmed real Execution Anchor evidence — `alembic current` against the real database was independently confirmed at head `0020`); 419 backend tests passed; 125 frontend tests passed; 61 of those backend tests are the focused `test_cg_supervisor_review.py` suite (content-boundary and compact-snapshot cases included); `ruff` check/format and all four CI `mypy` scopes passed (all re-verified fresh during this documentation turn); OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, repository Prettier, and the Next.js production build were verified passing during the preceding implementation-validation turn and remain valid, since no product/contract/generated file has changed since (confirmed via `git status --short` matching the same file set). Real-provider evidence: two early real DeepSeek calls truncated before valid JSON and were root-caused and fixed (compact input + hard output bounds + safe diagnostics); a real call then succeeded structurally but exposed two content-boundary gaps, motivating a hardening fix; a second real call then passed both hardening checks — see section E items 18–20 and `docs/VALIDATION_EVIDENCE.md`. Browser evidence, including the project owner's real Execution Anchor confirmation: see section E item 21.

### Step 5 — Artist Agent

- **Purpose:** implement the third Role Agent.
- **Minimum prototype scope:** task-context briefing; anchor-to-action translation; output/reference comparison; submission-rationale preparation support.
- **Dependencies:** none blocking.
- **Explicit exclusions:** no Anchor authority of any kind; cannot choose the final Version on the Artist's behalf.
- **Acceptance evidence required:** same shape as Steps 3–4 — real `AgentRun` with the correct `agent_type`, tests proving advisory-only behaviour.
- **Current status:** **Step 5 is merged to `main` via PR #14, merge commit `93fb730` (`81a79be` feat: add artist agent iteration guidance).** `agents/artist_guidance_service.py` (migration `0021_artist_agent_guidance.py`) produces an immutable `ArtistAgentGuidance` for one Version — `executive_summary`, `creative_intent_read`, `task_goal`, `current_iteration_read`, `non_negotiables`, `allowed_variations`, `feedback_translations` (each with `feedback_or_issue`/`practical_action`/`underlying_intent`/`self_check`/priority/evidence), `iteration_priorities`, `cross_department_dependencies`, `questions_for_human_supervisor`, and `evidence_gaps` — addressing two specific problems: Artists receiving only "what to change" without the wider creative-intent reason, and Artists selecting a department-local optimum without visibility into the Core Anchor, Execution Anchor, supervisor feedback, and cross-department dependencies. Because `Version` has no `task_id` of its own, generation requires an explicit `task_id` in the request body (the Human Artist selects the Task context; no heuristic latest-Task inference exists); the supplied Task and target Version must share a Shot, and the supplied Task's Execution Anchor must already be `confirmed` — a missing/draft/rejected Execution Anchor is rejected with a 409 before any `ContextSnapshot` or `AgentRun` is created. The ContextSnapshot is scoped to concise projections of Project/Shot/the explicit supplied Task/target Version and its ReviewNotes/IntentBrief/newest relevant IntentDecomposition/confirmed Core Anchor and semantic objects/newest ContextReconstruction/the confirmed Execution Anchor revision/newest relevant VFX Supervisor review/newest relevant CG Supervisor review/relevant Decisions/both HumanGates' resolution facts — no unrelated Tasks, Versions, revisions, or Decisions, no repeated nested evidence trees, no binary media, thumbnails, footage/render contents, scene/project/DCC files, raw ftrack payloads, or secrets; every evidence reference is validated to resolve to a real id in that run's own `ContextSnapshot`, with invented ids rejected before persistence. Every list/string field on the output contract carries a hard `Field` bound (`executive_summary`≤650 chars; `non_negotiables`/`allowed_variations`/`feedback_translations`/`iteration_priorities`/`questions_for_human_supervisor`≤3 items; `cross_department_dependencies`≤2 items; `evidence_gaps`≤5 items; each evidence list 1–2 references). A pre-persistence, code-level content-boundary validator rejects a missing explicit inspection-unavailable + media/scene/parameter disclosure in `evidence_gaps`, and any instruction — direct or indirect — to add content to or otherwise modify either Anchor (including naming a known Anchor field such as `allowed_refinements` without the word "Anchor"), re-anchor, resolve a HumanGate, issue a Decision, or officially judge/rank a Version; citing an Anchor as evidence, describing it as incomplete, and asking the Human Artist to seek CG clarification remain explicitly allowed. It runs through the same shared Agent Runtime as the other three Agents, registered in the Prompt Registry as `artist_iteration_guidance.v1` with a capability-specific `max_output_tokens=6144`. Generation is Human-Artist-only (VFX Supervisor and CG Supervisor receive 403; agent actors are forbidden); all three human roles may read; no PATCH/DELETE/apply/approve/generic-execute/ReviewNote-create/ftrack-writeback endpoint. The existing Version page gained an "AI iteration guidance — Artist Agent" section with an explicit Task selector and a Generate action for the Human Artist only, full structured rendering, an explicit advisory statement, and no edit/apply/accept/reject/ranking controls — no new route, Dashboard, or dependency. Automated evidence: migration `0021` upgrade/downgrade/upgrade passed; 496 backend tests passed (was 476 before Step 5, 419 before that); 135 frontend tests passed (was 125); 77 of the backend tests are the focused `test_artist_agent_guidance.py` suite (evidence/compact-snapshot/content-boundary/authority cases included); `ruff` check/format and all four CI `mypy` scopes passed; OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, and repository Prettier passed; the Next.js production build passed after retrying one transient Windows `.next/trace` `EPERM` file-lock error (an environment condition, not a code defect). Real-provider evidence: a first real DeepSeek call was correctly rejected pre-persistence for advising an Anchor edit, motivating content-boundary hardening, after which a second, fully compliant real call succeeded — see section E items 22–24 and `docs/VALIDATION_EVIDENCE.md`. Browser evidence: see section E item 25.

### Step 6 — Cross-role Assessment + Re-anchor Proposal + Intent Signal

- **Purpose:** build the capabilities that depend on multiple Agents/Assessments already existing.
- **Minimum prototype scope:** a cross-department/cross-role assessment capability (`docs/AGENT_CONTRACTS.md` §8); a re-anchor proposal capability (Core Agent, `docs/AGENT_CONTRACTS.md` §4); Intent Signal with exactly the four documented states (Stable / Stretching / Drifting / Re-anchor Needed), explainable per `docs/PRODUCT_SCOPE.md` §6.6.
- **Dependencies:** meaningfully benefits from Steps 3–5 existing (more Assessment sources to aggregate), though a minimal version could read only Core Agent + human Decision data if sequenced earlier — this is a product decision, not a technical blocker.
- **Explicit exclusions:** Intent Signal remains derived/read-only, never independently editable; no automatic re-anchoring.
- **Acceptance evidence required:** a Signal Engine producing an explainable, inspectable state; tests proving the signal is derived, not authored.
- **Current status:** **Step 6 is implemented and real-DeepSeek-validated on `feat/step6-cross-role-alignment`, complete on the feature branch, pending commit/push/PR/merge to `main`.** `agents/cross_role_assessment_service.py` (migration `0022_cross_role_alignment.py`) adds a fifth Core Agent capability, `cross_role_assessment` — not a fifth Agent — that synthesises the confirmed Core Anchor, the confirmed Execution Anchor, the newest VFX Supervisor Agent review, the newest CG Supervisor Agent review, and the newest Artist Agent guidance for one explicit Task/Version pair into an immutable `CrossRoleAssessment`: `executive_summary`, `shared_intent_read`, exactly three `role_perspectives` in fixed `vfx_supervisor`/`cg_supervisor`/`artist` order, `agreements`, `cross_role_tensions`, `local_optimum_risks`, `unresolved_dependencies`, `human_coordination_priorities`, an optional advisory `re_anchor_proposal`, and `evidence_gaps`. Reusing the Step 5 explicit-context decision unchanged, generation requires an explicit `task_id` in the request body (no heuristic latest-Task inference); the supplied Task and target Version must share a Shot; a confirmed `CoreAnchorRevision`, a confirmed `ExecutionAnchorRevision` for that Task, and the newest `VFXSupervisorReview`/`CGSupervisorReview`/`ArtistAgentGuidance` must all already exist, each missing prerequisite rejected with a 409 before any `ContextSnapshot`/`AgentRun` is created. The deterministic, code-derived `IntentSignal` implements a three-level `low`/`medium`/`high` `attention_level` (`low_attention`/`attention_needed`/`human_review_required` labels) with explainable `drivers` each pointing to a real `assessment_section`/`assessment_item_index` — a deliberate implementation decision to use a locked, testable three-level precedence rule (high when a high-priority tension/risk exists or a proposal was generated; medium when a medium-priority tension/risk, an unresolved dependency, or a material evidence gap exists; low otherwise) rather than this section's originally-scoped four-state Stable/Stretching/Drifting/Re-anchor-Needed vocabulary, recorded here as this capability's actual shape, not a silent deviation; it is derived by a pure function (`derive_intent_signal`), is not an Agent, and creates no separate `AgentRun`. The optional `ReAnchorProposal` is bounded advisory evidence only, persisted only when its evidence spans at least two of the three role-output categories, cites the current confirmed Core Anchor revision, and is supported by a cross-role tension or local-optimum risk in the same response; it has no approval state, no HumanGate/Decision linkage, and no apply/materialise endpoint. It runs through the same shared Agent Runtime as the other four capabilities, registered in the Prompt Registry as `core_cross_role_assessment.v1` with a capability-specific `max_output_tokens=8192` (raised from an initial `7168` after a genuine real-provider truncation — see below). A pre-persistence, code-level content-boundary validator rejects an official pass/fail, a definitive aligned/misaligned or drift verdict, role blame, a Version ranking, an Anchor-modification instruction outside `re_anchor_proposal`, a HumanGate-resolution or Decision-authority instruction, a claim of direct media inspection, and — added during this Step's own hardening — an unsupported production-specific numeric value (a percentage, multiplier/ratio, frame count, fps, duration, exposure stop/EV, or spatial measurement not present, value-and-unit-exact, in evidence the same output item cites); the allowed evidence `source_type` catalogue is derived directly from the contract's own `Literal` (never a second hand-maintained list) and placed beside every evidence shape in the prompt. Generation is Human-VFX-Supervisor-only (CG Supervisor and Artist receive 403, agent actors forbidden); all three human roles may read; multiple immutable assessments may exist per Version/Task context, listed newest first; no PATCH/DELETE/apply/approve/generic-execute endpoint. The existing Shot page gained a "Cross-role assessment — Core Agent" section (explicit Task/Version selectors, Human-VFX-Supervisor-only Generate, full structured rendering including the Intent Signal and optional Re-anchor Proposal, no apply/accept/reject/materialise/confirm/create-draft control anywhere) plus a history-clarity fix distinguishing the newest ("Latest assessment", fully expanded, with its created timestamp) from older immutable records (collapsed by default under "Previous assessments (N)", each individually expandable, carrying an explicit "these are immutable historical outputs... not the current assessment" disclaimer) — no new route, Dashboard, or dependency. Automated evidence: migration `0022` upgrade/downgrade/upgrade passed; 663 backend tests passed (was 624 before the numeric hardening, 156 focused `test_cross_role_assessment.py`); 152 frontend tests passed (113 focused `ShotAnchorPage.test.tsx`); `ruff` check/format and all four CI `mypy` scopes passed; OpenAPI export, TypeScript contract regeneration, contracts/web TypeScript typecheck, ESLint, and repository Prettier passed; the Next.js production build passed. Real-provider evidence: four real DeepSeek calls across three rounds of hardening (opaque-validation diagnostics, source_type enum conformance, a capability-specific token-budget fix) were needed before a technically successful result; that result's advisory proposal was then found, in owner browser review, to have invented two unsupported production values, motivating a fourth hardening pass, after which a final real DeepSeek call passed every validator — see section E items 26–32 and `docs/VALIDATION_EVIDENCE.md` for the complete evidence chain, ids, and generated output. Browser evidence, including the history-clarity UI verification: see section E item 32.

### Step 7 — Role-aware Dashboard

- **Purpose:** build the multi-role Dashboard described in `docs/PRODUCT_SCOPE.md` §13.
- **Minimum prototype scope:** shared shot view, VFX view, CG view, Artist view, each showing only what `docs/PRODUCT_SCOPE.md` §13.1–13.4 requires at minimum; visual distinction between Production Fact, AI Proposal, and Human Decision (§13.5's requirement, applied throughout, not just in an audit view).
- **Dependencies:** benefits from Steps 3–6 existing (there is more role-specific content to show), but the visual-distinction requirement itself has no technical dependency.
- **Explicit exclusions:** no generic multi-tenant admin UI; no enterprise design system.
- **Acceptance evidence required:** one page or view per role, each tested; a visible, testable distinction between fact/proposal/decision.

### Step 8 — Necessary ftrack Version / Note / link extensions

- **Purpose:** close the ftrack-integration gaps identified in section G.
- **Minimum prototype scope:** validate the targeted ftrack `AssetVersion` and Note relationships against the real test workspace; per-Shot Version/Note sync; an ICAS link or ftrack Action entry point; still **no autonomous write-back** — every write-back remains human-requested, exactly as Core Anchor write-back already works.
- **Dependencies:** the existing `ExternalEntityLink`/`WritebackRecord`/reconciliation-worker pattern (already proven for Project/Shot/Task) is the template.
- **Explicit exclusions:** no autonomous write-back of any kind; no write-back of raw AI Assessments (`docs/PRODUCT_SCOPE.md` §14.5 forbids this explicitly).
- **Acceptance evidence required:** a real, owner-validated sync of at least one real Version/Note pair, following the same manual-validation-evidence discipline as section E.
- **Current status: complete.** See section K below for the full completion record. Real ftrack `AssetVersion`/Note relationship validation (Step 8A), a locked sync contract (Step 8B, ADR-0014), the trusted internal sync API/connector reads/worker reconciliation/frontend Task-scoped integration (Step 8C-1 through 8C-7), and a real controlled-workspace acceptance run with owner visual validation (Step 8C-8/8C-9) are all delivered. No autonomous write-back was introduced — this remains a pure read-sync into ICAS, exactly as scoped above.

### Step 9 — Evaluation, complete demonstration, and project close-out

- **Purpose:** prove the four-Agent architecture is real, differentiated, and authority-bounded, end to end.
- **Minimum prototype scope:** a four-Agent differentiation evaluation (proving each Agent's `AgentRun`s are distinguishable and correctly attributed); an authority-boundary evaluation (proving no Agent of any type can create a `Decision`, confirm an Anchor, or write back autonomously — extending the existing guarantee in `decision_service.record_decision` to be re-verified against all four Agents); an evidence-traceability review; one complete, real, manually-validated demonstration combining a real DeepSeek call and real ftrack sync/write-back in a single walkthrough.
- **Dependencies:** Steps 1–8.
- **Explicit exclusions:** no new product capability introduced at this step — it is evaluation and documentation of what Steps 0–8 built.
- **Acceptance evidence required:** an updated version of this document and `docs/VALIDATION_EVIDENCE.md` reflecting the final state, with the same evidence-type discipline (automated test / repository evidence / manual owner-supplied) applied throughout.

---

## J. Step 7 Completion (dated 2026-08-01)

**Recorded 2026-08-01, branch `recovery/step7c2-functional-clean`, HEAD `dbf2a30` at time of recording.** This section is additive — it records Step 7's completion state as of this date without altering any Step 0–6 entry above (Sections C–I remain exactly as originally written; where this branch's actual history differs from what Section H/I describe for Step 6's merge lineage, that is noted above in Section H rather than corrected in place).

### J.1 Step 7 scope and sub-step status

Step 7 ("Role-aware Dashboard" in Section H/I's original naming) was delivered as three role-aware production workspaces plus their shared cross-role architecture, sequenced as 7A → 7B → 7C1 → 7C2 → 7C3 → 7C4 → 7C5:

| Sub-step | Scope | Status |
|---|---|---|
| 7A | Roles/identity/permissions demo scaffold, information architecture and route planning (`docs/step-7/02-05_*.md`) | Done |
| 7B | Shared design foundation, App Shell, Demo identity, shared Signal/ftrack/authority components (`docs/step-7/07-12_*.md`) | Done |
| 7C-1 | VFX foundations: Role-selection Home, VFX Workspace Home/Review Inbox/Shots, work-item architecture (`docs/step-7/13-19_*.md`) | Done |
| 7C-2 | VFX Intent Workspace (Core Anchor lifecycle UI) | Done |
| 7C-3 | VFX Alignment, Versions, Activity tabs; VFX close-out | Done |
| 7C-4 | CG Supervisor Workspace: Execution Anchor, CG Review Inbox, Tasks, Dependencies/Escalations, Version Review, Activity | Done |
| 7C-5 | Artist Workspace: Artist Review Inbox, Tasks, Task Overview, Current Version, Feedback History, real Artist Agent guidance generation (including the `artist_iteration_guidance.v2` DeepSeek fix) | Done |

**Step 7 as a whole is complete on this branch.** The global functional audit (`docs/step-7/20_STEP_7C_GLOBAL_FUNCTIONAL_AUDIT.md`, corrected 2026-08-01) found no functional blocker across any of the three workspaces, any of the nine Agent capabilities, the Review Inbox architecture, the Anchor/Decision/HumanGate chain, or role-aware deep-link security — see J.5 below for the verdict in full.

### J.2 Final route sets by role

**VFX Supervisor** (sidebar: Workspace Home · Review Inbox · Shots): `/vfx`, `/vfx/inbox`, `/vfx/shots`, `/vfx/shots/[id]`, `/vfx/shots/[id]/intent`, `/versions`, `/alignment`, `/activity` (5-tab `ContextTabs`: Overview · Intent · Versions · Alignment · Activity).

**CG Supervisor** (sidebar: Workspace Home · Review Inbox · Tasks): `/cg`, `/cg/inbox`, `/cg/tasks`, `/cg/tasks/[id]`, `/execution`, `/version-review`, `/dependencies`, `/activity` (5-tab `ContextTabs`: Overview · Execution · Version Review · Dependencies · Activity).

**Artist** (sidebar: Workspace Home · Review Inbox · Tasks): `/artist`, `/artist/inbox`, `/artist/tasks`, `/artist/tasks/[id]`, `/current-version`, `/feedback-history` (3-tab `ContextTabs`, deliberately narrower than VFX/CG per 7C-5's own scope: Task Overview · Current Version · Feedback History — no Intent/Execution/Dependencies/Decisions tab for Artist).

Shared entry: `/` (Role-selection Home, three real role-entry cards), `/demo` (permanent redirect to `/`), `/dev*` (development-mode previews, out of product scope). Legacy pre-Step-7 engineering workflow (`/shots`, `/shots/[id]`, `/shots/[id]/versions/[id]`) remains fully functional and is where legacy `AlignmentAssessment` generation/Accept-Reject still lives (read-only compatibility history only inside the new `/vfx` workspace, per `docs/step-7/14_...md` §5.2).

No dead-end route was found in any of the three role sidebars, tab sets, or Review Inboxes.

### J.3 Core Anchor, Execution Anchor, and Artist Guidance completion

**Core Anchor** (`intent/core_anchor_service.py`): full draft → pending `HumanGate` → confirm/reject → `Decision` cycle is real and complete, backed by a DB partial-unique-index enforcing at most one confirmed revision per Anchor. Draft revisions are editable in place (`update_draft_revision`) until confirmed; confirmed/rejected/superseded revisions become immutable and are retained, never overwritten. Drafting itself (`core_anchor_drafting` capability) remains deterministic-only — no DeepSeek generator was ever built for this specific capability (confirmed via `core_agent_service.py`'s own module docstring); this is the one Agent capability in the whole system with no live-provider path to validate.

**Execution Anchor** (`intent/execution_anchor_service.py`): the same draft → pending gate → confirm/reject → `Decision` cycle, plus a capability Core Anchor does not have — `create_draft_revision_from_confirmed`, a "new draft from the currently-confirmed revision" endpoint, not just a blank draft. Drafting (`execution_anchor_drafting` capability, `cg_agent_service.py`) has a real `DeepSeekExecutionAnchorDraftGenerator` and **was live-validated during implementation**: `provider=deepseek`, `model=deepseek-v4-flash`, `prompt_version=cg_execution_anchor_drafting.v1`, a succeeded `AgentRun` and a real `ContextSnapshot`, all eight supported Execution Anchor fields (`technical_boundaries`, `parameter_ranges`, `delivery_conditions`, `production_ready_criteria`, `downstream_dependencies`, `publish_requirements`, `allowed_refinements`, `escalation_conditions`) persisted as a Draft, with Human CG Supervisor confirmation still required before it takes effect — nothing was auto-confirmed. This validation is reported by the project owner (the same evidence discipline as every other "Manual owner-supplied execution" / "Manual owner-authorised real-provider execution" row already in `docs/VALIDATION_EVIDENCE.md`); the durable repository record for it is added in that document's own Step 7 section (§ below in `VALIDATION_EVIDENCE.md`), closing what had been a real gap — implementation and provider validation both existed, but no committed evidence of either did until this documentation pass.

**Artist Guidance** (`agents/artist_guidance_service.py`, capability `iteration_guidance`): complete, and the one capability in Step 7 with first-hand, this-session live verification rather than only a project-owner report. A real `v1` DeepSeek call failed with `finish_reason="length"` and empty content, root-caused to the configured model's internal reasoning tokens consuming the entire completion budget before any visible JSON. Fixed via a new `artist_iteration_guidance.v2` prompt/schema (every previously-unbounded field now bounded, evidence-per-item capped at exactly one) plus a `disable_reasoning=True` flag scoped to only this capability's call into the shared `model_gateway.generate_deepseek()` — every other capability's behavior is unchanged. Live-verified this session: a real `v2` generation succeeded (`AgentRun 31cfa3ad-...`, ~14s, non-deterministic real content citing the correct Core/Execution Anchor revisions); confirming a newer Execution Anchor revision correctly flipped `guidance_state` to `outdated`; Regenerate succeeded and created exactly one new row (`AgentRun a3a15b8c-...`); the two pre-fix failed attempts created zero orphan rows (row count matched exactly before/after). Full record: `VALIDATION_EVIDENCE.md`'s Step 7 section below.

### J.4 Agent capability matrix summary

Of the nine registered Agent capabilities (`agents/prompt_registry.py`), the completion state as of this date:

- **Real-DeepSeek-validated with durable, reproducible-from-repository evidence:** `intent_decomposition`, `context_reconstruction`, `alignment_assessment` (legacy, superseded by `cross_role_assessment` for the new VFX Workspace, not deterministic-only), `creative_review`, `execution_review`, `cross_role_assessment`.
- **Real-DeepSeek-validated, evidence added this documentation pass:** `execution_anchor_drafting` (project-owner-reported, per J.3 above), `iteration_guidance` v2 (this session's own first-hand verification, per J.3 above).
- **Deterministic-only, no DeepSeek code path exists:** `core_anchor_drafting` — the sole remaining gap of this kind in the system.

Every capability's persisted result, regardless of provider, routes through the same shared `ContextSnapshot`/`AgentRun` execution envelope (`agents/runtime.py::execute_agent`) with atomic failure semantics: a provider, validation, or domain-persistence failure always leaves `AgentRun.status="failed"` with the `ContextSnapshot` preserved and no partial domain result — confirmed for every capability that has ever had a real-provider failure recorded (`execution_review`, `iteration_guidance`, `cross_role_assessment` all have at least one documented real truncation/validation failure with zero orphan rows).

### J.5 Global functional-audit verdict

`docs/step-7/20_STEP_7C_GLOBAL_FUNCTIONAL_AUDIT.md` (read-only audit, corrected 2026-08-01): **no functional blocker found** across any of the three workspaces, any of the nine Agent capabilities, the Review Inbox work-item architecture, the Core/Execution Anchor → HumanGate → Decision chain, or role-aware deep-link/`returnTo` security. Remaining items are documentation debt (this section and `VALIDATION_EVIDENCE.md`'s Step 7 addition close the largest of them) and small non-blocking UX gaps (a copy-wording mismatch on the Re-anchor Proposal link; no staleness indicator for CG Supervisor Review analogous to Artist's `guidance_state`) — see that document's §8/§10 for the complete, itemized list. Final recommendation: **functionally ready for visual refinement**, independent of when that pass is actually scheduled (see J.6).

### J.6 Visual refinement is deferred; Step 8 is next

**Product-sequencing decision, recorded here:** visual refinement is deliberately deferred until **after Step 8** (necessary ftrack Version/Note/entity-link extensions) and **before Step 9** (final evaluation, complete demonstration, project close-out) — not immediately after Step 7C, even though Step 7C is functionally ready for it. This is a scheduling decision layered on top of a readiness finding, not a contradiction of it: Step 7C's functional completeness does not depend on Step 8 happening first, and Step 8's ftrack extensions do not depend on the visual pass having happened first. The locked order from Section H therefore continues unchanged past Step 7: **Step 8 — Necessary ftrack Version / ReviewNote / entity-link extensions** is the next step, exactly as originally scoped in Section I above (validate the targeted ftrack `AssetVersion`/Note relationships against the real test workspace; per-Shot Version/Note sync; an ICAS link or ftrack Action entry point; still no autonomous write-back). Visual refinement follows Step 8; Step 9 (evaluation and close-out) follows the visual pass.

**[Note added for Step 8 completion: this sentence is left as originally written; it accurately described the state as of 2026-08-01. Step 8 has since been completed — see section K below. The sequencing decision itself is unchanged and now active: visual refinement is the next approved activity, before Step 9, exactly as this section already decided.]**

---

## K. Step 8 Completion (dated 2026-08-02)

**Recorded 2026-08-02, branch `feat/step8c89-real-ftrack-acceptance`, HEAD `1440685` at time of recording.** This section is additive — it records Step 8's completion state without altering any Step 0–7 entry above. Full detail: `docs/step-8/04_STEP_8_COMPLETION_BASELINE.md`; full real-acceptance evidence: `docs/step-8/03_STEP_8C_REAL_FTRACK_ACCEPTANCE.md`; durable evidence table entry: `docs/VALIDATION_EVIDENCE.md`'s "Step 8" section.

### K.1 Step 8 scope and sub-step status

Step 8 was delivered as a locked investigate → design → implement → validate sequence:

- **Step 8A** — real, read-only investigation of the `AssetVersion`/Note relationship graph against the controlled ftrack trial workspace (`docs/step-8/01_STEP_8A_FTRACK_VERSION_NOTE_RELATIONSHIP_VALIDATION.md`). No local database or ftrack entity touched.
- **Step 8B** — the locked Version/ReviewNote sync contract (`docs/step-8/02_STEP_8B_VERSION_NOTE_SYNC_CONTRACT.md`, `docs/decisions/ADR-0014-ftrack-version-note-sync-contract.md`): a nullable `Version.task_id`; `source_created_at`/`external_author_id`/`external_author_name` on both `Version` and `ReviewNote`; a separately-authenticated trusted internal sync boundary; a complete per-linked-Shot re-sweep reconciliation strategy with no Version/Note `SyncCursor`.
- **Step 8C-1 through 8C-3** — migration `0024` (the four/three new nullable columns), the `VersionSyncCreate`/`ReviewNoteSyncCreate` Python contracts and extended `VersionRead`/`ReviewNoteRead`, and the trusted `POST /internal/sync/versions`/`/review-notes` endpoints gated by `X-Internal-Sync-Token`.
- **Step 8C-4/8C-5** — the connector's targeted read functions (`read_asset_versions_for_shot`, `read_direct_notes_for_asset_version`, `read_review_session_object_notes_for_asset_version`), the sync-client extension, and the `reconcile_ftrack_versions_and_notes` worker job, plus two reliability corrections (fail fast on a systemic 401/403/5xx sync error; fail closed on a missing/blank internal sync token, rather than reporting a misleading zero-valued "success").
- **Step 8C-6/8C-7** — VFX/CG/Artist frontend integration: VFX's Shot Versions page stays Shot-wide by design; CG's Version Review and Artist's Current Version/Feedback History pages apply the locked Task-scoped compatibility filter (`task_id` matches the current Task, or is `null`); chronological ordering everywhere uses `source_created_at ?? created_at`; external author provenance renders as "Source author", never an ICAS Human role.
- **Step 8C-8/8C-9** — real controlled-workspace acceptance: a real Project/Shot/Task bootstrap, two real Version/ReviewNote reconciliation runs (creation, then a true no-op repeat), full database-integrity verification, and owner visual validation of all four page types. See K.2 below.

### K.2 Real reconciliation results

A real Project/Shot/Task bootstrap linked 9 real Shots and 9 real Tasks. The first real `reconcile_ftrack_versions_and_notes` run: `linked_shots_examined=9`, `asset_versions_discovered=32`, `direct_notes_discovered=40`, `review_session_object_notes_discovered=10`, `api_created=82`, `api_conflicts_or_failures=0`. Local database: `Version` `2 → 34` (32 ftrack + 2 pre-existing manual, unchanged), `ReviewNote` `1 → 51` (50 ftrack + 1 pre-existing manual, unchanged); every synced row had exactly one `ExternalEntityLink`; zero partial rows. An immediately repeated run: `api_created=0`, `api_already_exists=82`, database counts unchanged, zero duplicates — the locked no-op-on-repeat-sync contract confirmed empirically. Owner visual validation confirmed all four page types (VFX Shot Versions, CG Task Version Review, Artist Current Version, Artist Feedback History) render the real synced data correctly, with role-guarding (redirect-with-`returnTo`, then return-to-requested-page) exercised successfully. Full detail: `docs/step-8/03_STEP_8C_REAL_FTRACK_ACCEPTANCE.md`.

### K.3 What Step 8 does not claim

Consistent with `docs/FTRACK_INTEGRATION.md` §15/§17's claim boundary: this validates real technical integration with one controlled trial workspace, not DNEG's production ftrack configuration, permissions, or production-scale data/performance. Step 8 does not add Event Hub or Webhook sync, Actions/Custom Widgets, Component/media sync, any ftrack write-back beyond the pre-existing, separate, human-requested Core Anchor confirmation write-back, or any capability to detect a ftrack-side content edit made after first sync (no real modification-timestamp field exists for either entity). The real dataset's Project/Shot/Task bootstrap linked only one Task per Shot, so it does not itself visually demonstrate a Version being excluded because it resolves to a *different* real Task under the same Shot — that exact rule is supported by the passing Step 8C-6/8C-7 automated test suite.

### K.4 Next approved activity

**Visual refinement, after Step 8 and before Step 9** — exactly the sequencing decision already recorded in J.6, now active. **[Note added for Step 9A: this "visual refinement" activity is now formally incorporated into the expanded Step 9 sequence below (L.1) as Step 9C, not a separate pre-Step-9 phase. This is a scope-label refinement of J.6's own already-recorded sequencing decision, not a new decision and not permission to alter the established IA.]**

---

## L. Step 9 Sequence (dated 2026-08-02)

**Recorded 2026-08-02, branch `feat/step9a-presentation-baseline-audit`.** The previously planned single "visual refinement after Step 8 and before Step 9" activity (J.6, K.4) is now formally expanded into an ordered Step 9 sequence, per `docs/step-9/01_STEP_9_PRESENTATION_AND_COMPREHENSION_BASELINE.md` (locked baseline) and `docs/step-9/02_STEP_9A_CURRENT_STATE_AND_IMPLEMENTATION_MAP.md` (this pass's audit). This is a scope-label refinement only — it does not authorise altering the established IA, role boundaries, or page responsibilities locked in Step 7/Step 8.

### L.1 Sequence and status

| Sub-step | Scope | Status |
|---|---|---|
| **Step 9A** | Baseline, end-to-end, and real-data audit — routes, page/data/source matrix, Working Direction feasibility, evidence layering, Department Execution Overview feasibility, real ftrack media feasibility, Step 8 display-limitation verification, ordered Step 9B implementation packages | Complete — see `docs/step-9/02_STEP_9A_CURRENT_STATE_AND_IMPLEMENTATION_MAP.md` |
| **Step 9B-1** | Role-aware Working Direction (VFX Current Creative Direction, CG Current Execution Direction, Artist Current Working Direction) | **Complete** (implementation, automated validation, and owner visual validation) — see `docs/step-9/03_STEP_9B1_ROLE_AWARE_WORKING_DIRECTION.md` §17 |
| **Step 9B-2** | Production Evidence / Agent Interpretation / Human Decision layering (VFX Intent, VFX Alignment, CG Execution, CG Version Review, Artist Current Version, Artist Feedback History) | **Complete** (implementation, automated validation, and owner visual validation) — see `docs/step-9/04_STEP_9B2_EVIDENCE_AGENT_HUMAN_LAYERING.md` §20 |
| **Step 9B-3** | Department Execution Overview | **Complete** (implementation, automated validation, a post-first-review owner-validation correction pass, and final owner visual validation) — see `docs/step-9/05_STEP_9B3_DEPARTMENT_EXECUTION_OVERVIEW.md` §17 |
| **Step 9B-4** | Media / thumbnail / ftrack context | **Complete** (implementation, automated validation, real read-only acceptance, post-rotation security re-verification, and final owner visual validation) — see `docs/step-9/06_STEP_9B4_REAL_MEDIA_AND_FTRACK_CONTEXT.md` §18 |
| **Step 9C** | Design-skill visual unification pass (the previously-referenced "visual refinement") | Not started |
| **Step 9D** | Student usability testing | Not started |
| **Step 9E** | Synthetic role evaluation | Not started |
| **Step 9F** | Final Demo, recording, Evidence Deck, and portfolio closeout | Not started |

**Step 8 is complete** (Section K). **Step 9A is complete.** **Step 9B-1 is complete**, implementation, automated validation, and owner visual validation all done (`docs/step-9/03_STEP_9B1_ROLE_AWARE_WORKING_DIRECTION.md` §17). Step 9B-1 delivered: deterministic, role-aware, read-only Working Direction summaries for VFX/CG/Artist Overview pages; explicit Production Fact / Agent Interpretation / Human-Confirmed authority and source distinction on every summary line; a new, explicitly role-gated CG Execution Anchor Decision read endpoint; honest fallbacks at both the parent-object level (no confirmed Anchor) and the individual optional-field level (a confirmed Anchor with one empty field); and a compact, non-duplicative Overview presentation (collapsed "Detailed context" disclosures, deterministic excerpting of long source text). Owner validation covered both a complete-data Task/Shot and a real ftrack partial-data state.

**Step 9B-2 is complete** (`docs/step-9/04_STEP_9B2_EVIDENCE_AGENT_HUMAN_LAYERING.md` §20) — implementation, automated validation, and owner visual validation across all six priority pages. Step 9B-2 delivered: deterministic three-layer classification (Production Evidence / Agent Interpretation / Human Decision and Provenance) via a shared `EvidenceLayerSection` presentation component; a real Decision outcome statement and supersession provenance on CG Execution (reusing Step 9B-1's existing role-gated endpoint, no new backend surface); clear separation of pending actions (Review actions, Recommended next action) from completed Human Decisions; role-safe Artist authority references that state the role boundary explicitly rather than implying more provenance access than Artist actually has; deterministic, event-type-keyed (never actor-label-keyed) chronological semantic classification for Artist Feedback History's timeline; readable, human-readable internal `HumanRole` provenance everywhere a role is displayed (never a raw enum); and honest "no Decision recorded" statements on VFX Alignment and CG Version Review rather than a fabricated Human Decision. No new authoritative object, migration, Agent workflow, or route/sidebar/tab was added.

**A first owner visual validation attempt on Step 9B-2 found five presentation-semantic defects** (a CrossRoleAssessment card reading as Production Evidence on VFX Alignment; CG Execution's Human Decision section omitting the Decision outcome; CG Version Review's review/Agent/escalation controls visually nested inside Human Decision; Artist Current Version's authority wording overstating available provenance; raw human-role enum values in several places) — all corrected (`docs/step-9/04_STEP_9B2_EVIDENCE_AGENT_HUMAN_LAYERING.md` §18).

**A second owner visual validation attempt found the §18 corrections held, but raw `HumanRole` enum values (`vfx_supervisor`, `cg_supervisor`, `artist`) were still visible** in the VFX Alignment Finding "Affects" list, CG Version Review and Artist Current Version's internal Version/ReviewNote author provenance, and Artist Feedback History's server-generated Execution Anchor confirm/reject event text — contradicting the earlier claim that zero raw enums remained. Corrected: `humanRoleLabel`/`ROLE_LABEL` mapping is now applied to the "Affects" list and to the shared `getAuthorDisplayText` author-provenance helper, and a new frontend-only `feedbackEventSummary` helper composes Feedback History's Decision-event descriptions from the event's own structured `event_type`/`actor_human_role` fields instead of rendering the backend's raw-enum-embedded summary text — no backend, contract, or persisted-data change (`docs/step-9/04_STEP_9B2_EVIDENCE_AGENT_HUMAN_LAYERING.md` §19).

**A third and final owner visual validation attempt confirmed all corrections held**: all visible role labels were readable; external ftrack author handling remained unchanged (never converted into an ICAS role); all three classification layers remained correct on every page; no prior authority or chronology behaviour regressed. **Step 9B-2 is complete** (`docs/step-9/04_STEP_9B2_EVIDENCE_AGENT_HUMAN_LAYERING.md` §20).

**Step 9B-3 is complete** (`docs/step-9/05_STEP_9B3_DEPARTMENT_EXECUTION_OVERVIEW.md` §17) — implementation, automated validation, a post-first-review correction pass, and final owner visual validation all done. Step 9B-3 delivered a compact, read-only Department Execution Overview section on the existing VFX Shot Overview page:

- a VFX-only, Shot-scoped aggregate read (`GET /vfx/shots/{shot_id}/department-execution-overview`), explicitly requiring the VFX Supervisor role via the real `ActorContext` mechanism;
- one read-only row per real Task — never matched by department-name string, always Task-`id`-keyed;
- deterministic Execution Anchor lifecycle status (none/draft/awaiting confirmation/confirmed/rejected, never confusing draft with confirmed, never showing a superseded revision as current);
- Task-linked-first Version selection, honoring the existing nullable-`task_id` compatibility rule, with an explicit Shot-level fallback scope label so a nullable-`task_id` Version is never presented as if it were explicitly linked to the Task;
- role-explicit CG task focus (reusing CG's existing, already-tested Current-focus derivation — no second ranking algorithm — but displayed with CG-owned, role-neutral copy rather than CG's own unqualified second-person wording);
- advisory alignment concern kept visibly distinct from formal escalation (a real open escalation is never inferred from an Agent recommendation or a high-attention concern alone);
- open Dependency and deterministic last-updated context on every row;
- safe, honestly-labelled Shot-wide navigation ("View Shot Versions") to the existing, permitted VFX Versions page — never a role-blocked `/cg/...` link;
- complete-data (two-Task) and real ftrack partial-data owner validation, both passed.

No new authoritative object, migration, Agent workflow, or route/sidebar/tab was added; no VFX edit/confirmation authority over a CG-owned Execution Anchor was introduced.

**A first owner visual validation attempt found two provenance/scope defects** (`docs/step-9/05_STEP_9B3_DEPARTMENT_EXECUTION_OVERVIEW.md` §16): CG's own Current-focus copy was rendered unqualified and in CG's second person ("...awaiting **your** confirmation") directly on the VFX-facing page, which could read as addressed to the VFX Supervisor and could visually contradict a separately-shown high-attention advisory alignment concern on the same row; and the nullable-`task_id` Shot-level fallback Version was rendered identically to a genuinely Task-linked Version, understating that it is not actually tied to that specific Task in ICAS. **Both are corrected**: the real `current_focus_type` discriminator now drives role-explicit, role-neutral display copy ("CG task focus: ..." / "No current CG action is required for this Task.") instead of the raw CG title; the real `latest_version_scope` (derived only from the Version's own `task_id` column, never its name) now distinguishes "Task-linked Version" from "Shot-level Version fallback — not linked to this Task in ICAS"; and the per-row action label was changed from "View details" to "View Shot Versions" to honestly name its real, Shot-wide destination. No underlying selection rule, authorization boundary, or other already-passing behaviour changed — verified by the full regression suite passing unchanged (backend 930/930, frontend 1001/1001). A temporary local runtime interruption occurred during re-validation (the already-running local `apps/api` process had not picked up the corrected code and briefly served stale responses); the local services were restarted, after which both owner-validation pages returned successfully with the corrected content.

**Final owner visual validation has now passed** on both the primary two-Task Shot and the real ftrack partial-data Shot, confirming every item on the corrected checklist. **Step 9B-3 is complete.**

**Step 9B-4 implementation and automated validation are complete** (`docs/step-9/06_STEP_9B4_REAL_MEDIA_AND_FTRACK_CONTEXT.md`) — a transient, read-only ftrack media/thumbnail/source-context capability for existing Versions, added to the three existing Version-focused pages (VFX Versions, CG Version Review, Artist Current Version). Step 9B-4 delivered: three new, narrow, context-scoped read endpoints (`GET /vfx/shots/{shot_id}/versions/{version_id}/media`, `GET /cg/tasks/{task_id}/versions/{version_id}/media`, `GET /artist/tasks/{task_id}/versions/{version_id}/media` — VFX Shot-wide, CG/Artist Task-scoped with the existing nullable-`task_id` compatibility rule, never one unrestricted generic lookup); an honest four-tier `media_state` classification (`playable`/`thumbnail_only`/`external_context_only`/`unavailable`), never fabricating availability; a shared, minimal `VersionMediaPanel`/`VersionMediaResolver` presentation pair reused by all three pages, resolved only for the selected/open Version, never pre-resolved for a whole list; `no-store` response/fetch semantics throughout; and a new, explicit, workspace-scoped dependency from `apps/api` onto `intent-core-connector` (the same Connector `services/worker` already uses) — the only place `apps/api` ever opens a real ftrack session.

**A real, safety-critical defect was found and corrected mid-implementation, before this step's own real read-only acceptance was completed:** the first connector implementation resolved media via ftrack's documented `Location.get_url()`/`get_thumbnail_url()` methods, which this task's own live, read-only probe against the real controlled workspace showed embed the calling service's live ftrack API user and API key directly in the returned URL as query parameters — sending that URL to a browser would have exposed real, reusable ftrack credentials to every session loading a Version's media panel. **Corrected**: the connector now resolves only `AssetVersion.thumbnail_url`, a real, credential-free, server-signed schema field this workspace's own live probe confirmed exists (a `cdn-eu3.ftrackapp.com` Thumbor URL wrapping a one-time-scoped `signature`, never a reusable credential); no credential-free mechanism exists for a Component's playable-video URL in this workspace's real schema, so per the task's own explicit instruction ("downgrade... rather than proxying credentials through ICAS"), playable video is honestly never attempted — every real linked Version in this workspace tops out at `thumbnail_only` today. One credential-bearing URL briefly appeared in the local Claude Code session output while diagnosing the defect; no credential-bearing URL ever entered a Git-tracked file, commit, database row, or the browser-facing implementation. The unsafe connector path was removed before this branch's first commit. **The owner subsequently rotated the exposed ftrack API key**; all local services were restarted with the rotated credentials; a dedicated post-rotation verification pass confirmed authenticated read-only ftrack access succeeds with the new key, the real media acceptance counts are unchanged, zero credential-parameter matches across every browser-facing response, and zero occurrences of the old or new key anywhere in the repository or its full git history (`docs/step-9/06_STEP_9B4_...md` §7/§15).

Real read-only acceptance against the live workspace (post-fix, re-confirmed post-rotation): 34 sampled Versions, 32 `thumbnail_only`, 2 `unavailable` (the demo Shot's manual Versions), 0 `playable`, 0 `external_context_only`, 0 resolution failures, 0 credential leaks. No ftrack entity or local Step 8 acceptance data was mutated (every call was a read). No new route, sidebar item, tab, database migration, Agent workflow, or write-back capability was added.

**Correction to Step 9A's own media evidence:** `docs/step-9/02_STEP_9A_CURRENT_STATE_AND_IMPLEMENTATION_MAP.md`'s original §9 finding ("30/32 real Versions have a resolvable playable `.mp4` component URL") conflated two different claims — that the SDK can *locate* a video-shaped Component (true, unchanged, 30/32) and that the resulting URL is *safe to render in a browser* (never actually tested by that audit, and found false by Step 9B-4's own implementation). `02_STEP_9A_...md` §17 (new) records this reclassification in full, with every original number preserved, not erased. The corrected, browser-safe capability is: 32/32 real linked Versions safely resolve to `thumbnail_only`; 0/32 safely resolve to `playable` in this real workspace today.

**Final owner visual validation has now passed** for all four required contexts (real VFX Versions, real CG Version Review, real Artist Current Version, and the manual/local fallback), including a live Version-switch check confirming the thumbnail, title, description, and Review Notes all update together and never go stale. **Step 9B-4 is complete.**

**Step 9B (as a whole) is complete.** All four sub-steps — 9B-1 (Role-aware Working Direction), 9B-2 (Evidence/Agent/Human layering), 9B-3 (Department Execution Overview), and 9B-4 (real ftrack media context) — have each independently completed implementation, automated validation, and owner visual validation. Step 9B-4 additionally completed a real-workspace security incident, fix, owner-performed credential rotation, and post-rotation re-verification before being marked complete. **Step 9C (design-system visual unification) has not started**, nor have Step 9D–9F. **Step 9C is the next approved activity.**

**Non-blocking Step 9C/9D presentation-comprehension item, recorded for the later visual-unification pass, not fixed here:** on the VFX Versions page, the Shot context header may display the current Shot Version (e.g. "VERSION bc0040_comp_v002 (v2)") while the user has separately selected a different Version in the Version list below (e.g. "bc0040_layout_v002 (v2)"). The selected-Version detail panel, thumbnail, metadata, and Review Notes all update correctly on selection — this is **not** a Step 9B-4 media synchronization defect, confirmed by the owner's own live Version-switch check above. A later presentation pass should clarify the distinction between the Shot's own current Version and the Version currently being browsed — for example by renaming the header to "Current Shot Version" or otherwise making the browsing context more explicit. Not implemented in this closeout.

---

## M. Approved Product-Improvement Phase (dated 2026-08-03)

Package B implementation is now in progress on `feat/package-b-agent-journey-completion`. The
shared Agent Contribution semantic layer and first formal-route wiring pass are implemented;
focused frontend validation has passed and owner visual validation is pending. Package B is not
complete; Packages C and D have not started.

The 2026-08-04 owner-validation correction pass repaired contradictory readiness/action states,
added formal Core Anchor Draft and VFX Creative Review controls, persisted CG Review selected-
Version association, and wired captured Cross-role Assessment provenance where available. The
focused CG Review backend suite passed 61 tests; the six formal frontend page suites passed 97
tests; generated TypeScript contracts and web typecheck passed. Owner re-validation and the
remaining package/merge gate are still pending. Package B remains in progress.

Owner visual validation passed on 2026-08-04. The final merge gate passed with 1,138 backend/infra
tests (contracts 53, API 966, worker 36, Connector 81, infra 2), 1,018 frontend tests across
129 test files, migration upgrade/downgrade validation including CG Supervisor Review
`version_id`, a clean production build generating 18 static pages, full Ruff and mypy, generated
and checked Python/OpenAPI/TypeScript contracts, Web TypeScript, ESLint, Prettier, `uv lock
--check`, frozen pnpm lockfile installation, and `git diff --check`. Existing seeded Agent
outputs may truthfully show unavailable provenance when no historical AgentRun or
ContextSnapshot was recorded; newly generated outputs retain real provenance. Package B is
complete. Package C is the next approved package and has not started; Package D has not started.

This additive section preserves the Step 0–9 record above as history while replacing its
forward-looking surface-polish sequence with the approved four-package product-improvement phase.

- **Step 9B remains complete.** Its implementation and validation record is unchanged.
- The previous standalone Step 9C/9D surface-polish sequence is superseded and absorbed into the
  four packages below; it is no longer the active delivery sequence.
- The approved source of truth for this phase is
  `docs/product-improvement/00_ICAS_PRODUCT_IMPROVEMENT_BASELINE.md`.

| Package | Scope | Current status |
|---|---|---|
| **Package A — Anchor-first experience** | Persistent, role-appropriate Anchor context and Intent Signal orientation across the current role-aware journey | **Implementation and owner-validation correction complete on feature branch; repeat owner visual validation and final full regression pending** |
| **Package B — Visible Agent journey completion** | Discoverable Agent input → output → human-authority chains on current role-aware routes | **Not started** |
| **Package C — Coherent Golden Demo scenario** | Resettable/loadable three-department journey with separated Demo, live ftrack, and development-fixture data worlds | **Not started** |
| **Package D — UX and visual convergence** | Journey, terminology, navigation, hierarchy, usability, and visual convergence after the product structure is coherent | **Not started** |

Package A now has an implementation and owner-correction checkpoint on
`feat/package-a-anchor-first-experience`. Three role-scoped read-only Anchor Context endpoints,
three shared object Workspace Frames, all 13 formal object-route migrations, Anchor-aware
Home/Inbox/catalogue rows, and actionable current-route empty states are implemented. The owner
correction adds explicit session-memory disclosure controls; Overview-expanded/non-Overview-compact
defaults; action-first VFX/CG Homes; truthful Artist ready/waiting groups; semantically grouped
multi-object rows; and role-authorized bounded compact reads that replace the browser's prior
per-row full-context request fan-out. The projection reuses existing Anchor, HumanGate, Intent
Signal, Current-focus, Version, Guidance, and
Dependency records; it adds no table, authoritative state, Agent execution, Demo-seed behavior, or
ftrack call/write. Stored Execution-to-Core revision references are shown only when provable, and a
Draft is labelled Demo/fixture only if future persisted provenance can prove that source (today an
unprovable source is `unknown`). Focused validation passed: 7 backend API/service tests, 52 Python
contract-schema tests, 329 frontend tests, touched Python lint/type checks, generated-contract
 typecheck, web typecheck, and touched-file format/lint checks. Package A is **complete**: owner
 visual validation and the final full repository regression and production build passed. Package B
 has not started and is the next approved package; Packages C and D have not started.

Owner-correction focused validation passed separately: 10 Anchor Context backend API/service tests;
53 Python contract-schema tests; 370 unique frontend tests across 47 affected semantic, Home,
Inbox/catalogue, route-loader, and object-workspace test files; Ruff format/check for touched Python;
mypy for the contracts and API packages; generated-contract and web TypeScript checks; touched-file
ESLint/Prettier; and `git diff --check`. The complete repository pytest/Vitest regression and
 production build were intentionally not run and remained part of the post-owner-visual merge gate.

### Package C D1 journey direction correction (2026-08-04)

Package C remains in progress on `feat/package-c-golden-demo-journey`. The complete three-
department journey now uses the existing D1 Shot 010 through normal VFX, CG, and Artist routes.
There is no visible Golden Demo card, Golden session, separate Project, or Golden-only filtering.
`Reset D1 Journey` and `Load Completed D1 Journey` are production-disabled internal developer
endpoints. They use exact D1 Shot/Task ownership predicates, retain real provenance, preserve Shot
020 and other fixtures, and do not touch live/ftrack or unknown-origin records. Owner validation is
pending; Package C is not complete, legacy cleanup is deferred, and Package D has not started.

### Package C journey state-machine rebase (2026-08-07)

Package C remains in progress on `feat/package-c-golden-demo-journey`. `ICAS_PACKAGE_C_AUDIT_
REPORT.md` traced an owner-observed defect to two causes: role entry's Server Action silently
calling the pre-Package-C `ensure_d1_scenario` seed pipeline (which still targeted the same
canonical Shot 010/Compositing Task the Journey owns), and `journey-status` scoping its
"assessments" count by a Version-description marker while scoping "proposals"/"guidance" by Task
id. Role entry no longer calls any seed/ensure endpoint. `ensure_d1_scenario`'s rich generation is
retargeted to a separate, noncanonical Shot so it can no longer collide with the canonical Journey
Shot. `demo_seed.d1_journey` now resolves its canonical Project/Shot root through a minimal,
non-journey-state structural bootstrap, and one canonical graph selector — scoped consistently by
the three canonical Task ids everywhere — backs Reset, Load-Completed, and `journey-status` alike.
`journey-status` now reports the full J0–J4 classification (`journey_state`: `reset`,
`assessment_complete`, `reanchor_draft`, `r2_confirmed`, `completed`, `mixed`) alongside the
existing three-way `snapshot` field, plus assessment/proposal ids and attention levels as
diagnostic facts. A read-purity regression covers every normal VFX/CG/Artist GET path against a
canonical semantic snapshot; explicit transition tests exercise the real J0→J1→J2→J3 domain
actions and confirm downstream R1 outputs are never silently replaced.

One pre-existing product-behaviour gap was found while building the J0→J1 transition test (not
introduced by this rebase): the real "Generate Cross-role Assessment" action, under the
`deterministic` model provider every local/dev/test environment defaults to, can never reach "high"
attention or propose a re-anchor against the canonical J0 evidence, because
`DeterministicCrossRoleAssessmentGenerator` is documented to deliberately cap every finding at low/
medium priority. `journey-status` honestly reports this as `mixed`. Reaching the locked J1
narrative through the real action requires a live model provider or the D1-Demo-only generator
Reset/Load-Completed already use. This is flagged for an explicit product decision, not silently
patched.

Focused validation: full backend suite (980 tests) and full frontend suite (1,009 tests) pass,
Ruff format/check and mypy pass on touched files, web TypeScript typecheck and ESLint pass. Owner
re-validation of the Reset → role-entry → journey-status sequence is still pending. Package C
remains in progress; Package D has not started.

### Package C J3 → J4 formal completion (2026-08-08)

Package C remains in progress on `feat/package-c-golden-demo-journey`. Owner validation of the
downstream-retranslation step (accepted separately, commit `7104025`) exposed the final structural
blocker to completing the Journey through the real formal UI: the backend already exposed a real,
generic `POST /versions` capability, but no product action anywhere in `apps/web` could create the
next Production Version for a Task. `Version.task_id` (`ADR-0014 Decision 3`) is narrowly amended:
it is no longer grouped with ftrack-only provenance fields on the public `VersionCreate` contract —
a human may now explicitly submit a manually-created Version that belongs to one specific Task,
mirroring the domain model's own pre-existing, previously write-only-by-ftrack-sync column. The
Artist Current Version page now offers "Publish next Version from Execution Anchor R{n}" once a
Task's confirmed Execution Anchor is genuinely current against the confirmed Core Anchor and no
Version scoped to that Task yet carries that revision number — computed entirely from data the page
already loads, no new backend field. The action re-derives readiness and content server-side (never
trusts the client), creates exactly one Version, and never auto-creates Guidance, Reviews, or an
Assessment.

`demo_seed.d1_journey.load_completed_d1_journey` (the developer-only J4 shortcut) is rewritten to
call the same real service functions the formal role flow uses — Core R2 draft-from-proposal,
per-department Execution R2 generate/confirm (identity-gated to the D1-specific translation), a real
CG Review per confirmed revision, an Artist-submitted resolved Version per department, real Artist
Guidance, one required VFX Creative Review for the resolved Compositing Version, and the final
Cross-role Assessment — rather than its own bespoke bulk-fixture pipeline, so it can no longer
silently diverge from what J4 actually requires. `journey_state = "completed"` is redefined from
this real graph shape: `dependencies` stays frozen at its J0 baseline of 2 (the real flow never
touches TaskDependency at all — this was previously a shortcut-only artifact), and
`attention_levels == ("high", "medium")` now directly encodes both "the historical J1 Assessment
stays untouched" and "the final Assessment genuinely resolved lower than J1." `downstream_
retranslation`'s own range check is loosened to match: Versions/Guidance/CG Reviews may each
independently climb from the J0 baseline toward the J4 target as each department completes its own
post-confirm sequence, rather than assuming they stay frozen until every department finishes.

Audited and left unchanged as already-real and already-correct: confirming one department's
Execution R2 never auto-confirms another; a confirmed Execution R2 references the confirmed Core R2
revision; a superseded Execution R1 stays byte-for-byte historical; the CG Version Review and VFX
Versions Workspace pages already resolve real Task-scoped/Shot-wide Versions generically, so a
newly published resolved Version appears in them with no code change. One structural gap was found
and left unfixed, flagged rather than shortcut around: the polished `/vfx/shots/[shotId]/alignment`
workspace's "Generate Assessment" button is scoped to a Shot's *first* Assessment only (its own
readiness signal returns null once any Assessment already exists), so it cannot trigger the J4 final
Assessment; the pre-existing free-form Task×Version picker on `/shots/[shotId]` remains the real,
currently-functional path for a second/final Assessment, used unchanged by the new J0→J4 test.

New regression coverage: one full J0 Reset → explicit J1 Assessment/Proposal → R2 Core Draft/confirm
→ per-department Execution R2 Draft/confirm/CG-Review/Version-publish/Guidance →  VFX Creative
Review → final Cross-role Assessment → `journey_state = "completed"` integration test, driven
entirely through real HTTP endpoints (Reset is the only developer-triggered step); `POST /versions`
`task_id` validation tests (cross-shot rejection, unknown-task 404, default-null compatibility); and
Artist Current Version page tests for the new Publish action's visibility, click, and error paths.

Validation: full backend suite (999 tests) and full frontend suite (1,023 tests) pass; Ruff
format/check and mypy pass on touched Python files; web TypeScript typecheck, ESLint, and the
production build (`next build`, 18 static pages) pass; repo-root Prettier passes on every file
touched this turn (a small number of pre-existing, untouched files elsewhere in the tree still have
outstanding Prettier issues from earlier work, left alone as out of this turn's scope). Owner
validation of the full J3 → J4 UI walkthrough is still pending; Package C is not yet marked
complete.

### Package C reassessment in the formal Alignment workspace (2026-08-08)

Package C remains in progress on `feat/package-c-golden-demo-journey`. The J3 → J4 UI walkthrough
(commit `ab56036`) still had to leave the canonical `/vfx/shots/[shotId]/alignment` workspace for its
final step: that page's own readiness signal (`generation_ready_task_id`/`generation_ready_version_id`
on `VfxInboxItemRead`) was unconditionally nulled once any Assessment already existed for the Shot,
so the real "Generate Cross-role Assessment" action there could only ever fire once per Shot, ever.
`vfx_inbox.service._assessment_generation_check` now compares candidate Task/Version pairs against
the *Version's own `created_at`* on the Shot's latest Assessment, not just an exact-pair match: a
pair is "ready" only when its Version is strictly newer than the one the latest Assessment already
used, so a stale pairing that happens to still satisfy the raw prerequisite checks (an older Version
whose Task's *current* confirmed Execution Anchor picked up a fresh CG Review) is correctly excluded,
while a genuinely new resolved Version is correctly surfaced. The blanket "assessment exists →
always null" gate in `build_inbox_item` is removed; the check itself is now the single source of
truth for both the first-ever Assessment and every later reassessment.

The Alignment workspace page now renders both facts side by side once a historical Assessment exists
and a newer eligible pair is ready: the historical Assessment's own content stays fully visible,
labelled "Historical assessment," and a new "A newer eligible Version is ready for reassessment"
panel with a `Generate new Cross-role Assessment` action (the same real generation endpoint,
`GenerateAssessmentButton` now takes an optional `label`/`pendingLabel`) appears above it. Generating
does not delete or replace the historical Assessment -- it becomes the newest entry in the page's
already-existing "Assessment history" list, unchanged from before. The older free-form Task×Version
picker page is untouched and still exists for other flows; it is no longer required for the canonical
owner walkthrough.

Regression coverage added: a `vfx_inbox` test proving a newer eligible Version becomes ready once its
own CG Review/VFX Review/Artist Guidance evidence exists after an Assessment already exists; three
`AlignmentWorkspacePage` tests (reassessment hidden with no newer evidence, shown with the historical
Assessment still visible, and the real action called with the correct newer pair); and the existing
full J0→J4 integration test extended with the Alignment page's own `GET /vfx/inbox/{shot_id}`
read-purity and readiness assertions at the reassessment-ready transition, and a post-generation check
that readiness correctly returns to absent with no further qualifying pair.

Validation: full backend suite (1,000 tests) and full frontend suite (1,026 tests) pass; Ruff
format/check and mypy pass on touched Python files; web TypeScript typecheck and ESLint pass; the
production build (`next build`, 18 static pages) passes; repo-root Prettier passes on every file
touched this turn; `git diff --check` clean. Owner validation of the full J3 → J4 UI walkthrough
through only the Reset developer step plus the formal role pages is still pending; Package C is not
yet marked complete.

### Package A final merge gate (2026-08-03)

Package A owner visual validation passed. The complete repository regression then passed: 53
contract tests, 966 API tests, 36 worker tests, 81 ftrack-connector tests, and 2 infrastructure
tests (1,138 backend tests total); 129 frontend test files (1,017 tests total); and the production
web build (`next build`, 18 static pages generated). Ruff format/check, mypy for all four Python
projects, Python and TypeScript contract checks, web TypeScript typecheck, ESLint, Prettier,
`uv lock --check`, frozen pnpm lockfile installation, and `git diff --check` also passed. One
shared Anchor summary label regression found by the full frontend suite was restored to the
existing `Attention / state` contract before the passing rerun; no authority or product boundary
changed.

Package A is complete. Package B is the next approved package and has not started. Packages C and D
have not started. This closeout was recorded on `feat/package-a-anchor-first-experience`; no push
was performed.
