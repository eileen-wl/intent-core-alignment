"""Canonical shared contracts for the Intent Core Alignment System.

This package is the single source of truth for schemas shared across
``apps/api``, ``services/worker``, and ``services/ftrack-connector``.
TypeScript types in ``packages/contracts/ts`` are generated from the
API schemas exposed here via the FastAPI OpenAPI document — they are
not hand-authored separately.
"""
