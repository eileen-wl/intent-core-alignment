// Intentionally empty of product components. This package is reserved
// for shared Dashboard components once role-aware views are designed
// (docs/PRODUCT_SCOPE.md §13). Exporting one placeholder keeps the
// workspace package resolvable and typecheck-able before that work
// starts.

export const UI_PACKAGE_PLACEHOLDER = "@intent-core/ui" as const;
