# E2E tests

Reserved for Playwright browser tests against the running Dashboard
(`apps/web`). Empty until there is a role-aware view worth driving
through a real browser (`docs/PRODUCT_SCOPE.md` §13) — the current
`/shots` page is a wiring smoke test, not a product flow.
